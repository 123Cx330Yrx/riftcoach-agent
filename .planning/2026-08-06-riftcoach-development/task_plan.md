# RiftCoach 当前执行计划

## Goal

在既定阶段 0—8 和采用标准内，把可靠的模型分析接入真实 Agent 教练任务。
当前 ShowMaker 观摩任务不是整个产品；自然 Coach、本人 Training、四块联动及前端等仍在原路线内。
方法入口：`docs/plans/2026-09-22-agent-delivery-method.md`。事实入口：`docs/project_execution_state.md`。

## Current Phase

当前精确 checkpoint：`8e-productization / candidate-real-golden-slice / in-progress / offline-hardening-and-live-consumption`。
- Status: in_progress

ADR0109角色候选已取得来源修复后真实自然生成/工具/审查/文件读回证据，以及当前1.5.1合同下
一例原归因错误稿的真实首评→Flash改稿→GLM终评96/pass。但随后原正确全文被误报，配对未通过。
原15为1例host接受、1例失败、13例未跑；这不等于1/15稳定率，也不能拼成产品资格。
原知识日期错误稿未被本批自动修复；普通编辑成功不等于独立补漏能力，默认产品与8E边界不变。

## Active Work Package

**目标：** 当前1.5.1角色组合应在完整上下文下检出原15中的真实归因错误，Flash实际改正后由GLM终评；同来源的正确全文不得误报。这一工作直接验证Coach自动纠错链路，来源传递包已有真实证据，不能继续替代本项。

**起点与差距：** 来源传递已修，取消advisory替换文本的职责精简已实测；它没有消除正确全文误报。真错attribution:1首评83/needs_revision、实际改稿、终评96/pass均经全文与引用审查。claim-scope:1完整84/needs_revision错误扩大全称，并缺severity，恢复前停批。全部原15输入、旧裁决和未准入状态保留。

**实现路径：** scripts/run_role_qualification_pair.py复用RoleNoteReviewWorkflow、Runtime现有共享预算/回执转发、角色Provider、原资格read_role_calls/replay_case。先attribution:1首评→实际编辑→终评，再claim-scope:1正确全文；每个业务阶段和格式恢复前由host核对全部意见与来源，拒绝立即停批。只增加有界开发执行入口，不改模型/提示/产品状态机。

**验收：** 原15完整来源和报告hash不变，标签不进请求。真错是block14将伤害差距也主要归于辅助；中单内部伤害差669.235占全样本695.4183约96.23%，辅助仅扩大26.1833。应保留中单补刀8.805/9.01、全样本早死2.5/2.67、正确表格、观摩身份、K1—K5与条件训练。实际改稿不要求逐字抄正确稿；终评与正确控制须pass/无issue/score≥85，全部advisory和恢复意见仍检查。两例成功只形成同版本资格片段，不标原15或产品通过。

**预算与授权：** 沿用户既有全授权和本轮继续诊断修复授权，未重新声称逐字“两次批准”。本批原上限7调用/595456tokens/1500秒、保守预留估价8.6185984元；实际4调用/68811tokens/917.188秒、未缓存估价0.865566元，缓存输入4800、未知0，估价非账单。原批已关闭，余量不是下一批授权/预算，不续跑。当前没有新付费计划。

**失败决策：** 输入/接线/协议异常核对首次偏离；完整语义或引用失败保留原文并停止第二例，不追加自然生成/提示变体。仅当证据区分新机制才另拟检查。格式恢复也须先检查旧响应，不能把新判断称字段修复；产品门与默认不变。

**当前状态：** 实测绑定6a5afe9c / CI35903747333三项成功；49份原件/hash与公开投影已核对，真实成功可从3调用重建，失败仍失败。拒绝hash曾误抄65位，原runner以model_comparison_host_decision_invalid安全停，实质拒绝另有记录，未多发请求；不改原result。CLI在输入/CI/凭据前关闭；回执相对路径混用的确切缺陷已修，相关42项此前通过，关闭入口验证随本次收尾执行。

**方案去留与下一动作：** 停该版本原样扩测，保留分工接线与本次普通纠错成功；不把notes精简当语义充分修法。对新修法尚无足够证据，先完成最小隔离方案：固定完整来源与正确/真错文本，区分自然语言范围识别和完整评审输出职责的负担，并说明每种结果如何改变实现选择。方案须能放入现有五调用Agent任务或明确不能采用；离线表达可行不等于模型能力。未完成这项选择前，不新建付费变体、不补字段碰运气、不复活已失败的强制独立编辑/显式patch路线。当前对照同policy/schema/来源正文/metadata，无旧评历史；报告具体化与随机性未隔离，不称内部根因已明。

## Dependencies and Follow-through

| 后续工作 | 何时推进及验收 |
|---|---|
| 修法与同版本质量 | 根据上项证据选最小机制修复；原问题、正确全文、真错/混合例及既有身份/来源回归；不拼不同版本成功 |
| 产品资格绑定 | 新组合绑定原15输入、实际政策与传输；资格证据必须来自同组合真实回执和独立逐项审查，不能复用旧五例或两份固定报告 |
| 真实产品消费 | 质量达到既有要求后验证真实生成/工具/审查修订；复用已有 Evidence/事务/Worker/API，补当前组合真实 DB 和 UI 证据 |
| Agent 产品与前端 | 自然请求、训练采用/反馈、四块联动、整体审美/必要重做/英雄头像，按原依赖推进；可独立部分不被单一评审阻断永久冻结 |
| 完整退出 | 保留身份运维、两树整合、独立评估与学习；具体入口见 restart plan“全局后续”及 62 主题，不在此重排主阶段 |

## Next Step

当前精确 checkpoint：`8e-productization / candidate-real-golden-slice / in-progress / offline-hardening-and-live-consumption`。
具体下一动作及失败分支仅维护在上方 `Active Work Package` 的“当前动作”，此处不复制动态排程。
全周期按交付方法执行，工作包结束或出现关键反证时复核策略与下游依赖；普通工程调整沿用已有授权。

## History

2026-09-22 整理前的 3596 行计划完整保存在
`docs/archive/2026-09-22-execution-method/task-plan-before.md`，哈希见同目录 manifest.json。
阶段历史完成记录原样保留；当前阶段以 canonical 和学习账本为准。progress/findings 保留详细结果。
