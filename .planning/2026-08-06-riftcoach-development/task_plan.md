# RiftCoach 当前执行计划

## Goal

在既定阶段 0—8 和采用标准内，把可靠的模型分析接入真实 Agent 教练任务。
当前 ShowMaker 观摩任务不是整个产品；自然 Coach、本人 Training、四块联动及前端等仍在原路线内。
方法入口：`docs/plans/2026-09-22-agent-delivery-method.md`。事实入口：`docs/project_execution_state.md`。

## Current Phase

当前精确 checkpoint：`8e-productization / candidate-real-golden-slice / in-progress / offline-hardening-and-live-consumption`。
- Status: in_progress

ADR0109角色候选已取得来源修复后真实自然生成/工具/审查/文件读回证据，以及此前1.5.1合同下
一例原归因错误稿的真实首评→Flash改稿→GLM终评96/pass。但随后原正确全文被误报，配对未通过。
此前1.5.1原15为1例host接受、1例失败、13例未跑；后继1.5.2尚无真实资格，不继承旧成功。
原知识日期错误稿未被本批自动修复；普通编辑成功不等于独立补漏能力，默认产品与8E边界不变。

## Active Work Package

**目标：** 让Coach完整审查保留真实问题的依据与修法，同时停止在无人消费的非阻断建议中扩写新事实；用原15正例、明确真错及混合归因保护误报/漏检两端。

**起点与证据：** 5099e381 / CI35958393663三项通过后，上下文2×2四次目标误报均false，原正确稿96/pass；整份审查2可接受/2因建议新增引用或推断错误不接受。没有因素差异，按规则停4，未反向复测。62903tokens、未知0、未缓存估价0.813744元。旧失败与原15保留，不归因于某一文本变化。

**选定路径：** 保留advisory作为可选澄清的出口，仅提交block，不再生成explanation/source_ids/新数字。真实issues仍须完整解释、来源与修法，不能藏入标记。实际API/报告/编辑未消费advisory，journal保留标记及原段；此为职责收缩，不是隐藏旧输出或已证明语义修好。比删除整个出口更符合ADR0104历史反证。新RoleClarityReviewWorkflow接当前角色1.5.2 / Program3.1.2 / evaluation3.5.0；旧1.5.0/1.5.1只读、旧回执不继承。

**验收：** 原15完整请求/来源、真正错误字段与编辑请求保持；应用预算/Trace/历史回放/安全和恢复路径通过离线验证、独立复核与同提交公共CI后，直接作为新身份原15资格开头执行claim-scope:1、claim-scope:4、attribution:1。正确稿应原样通过，真错应检出并实际编辑/终评；主/独立检查全部问题、标记、来源和修改范围，不以schema合法代替语义。三个条件成功也不等于15项全过，随后补其余12项和真实产品消费。

**预算与授权：** 沿连续全授权执行新有界批，不借已关闭上下文批余量。最多12调用/997376tokens/2400秒：正确稿最多2调用/193536tokens/600秒，两真错各5调用/401920tokens/900秒。单次32768/300秒、SDK retry0；保守全按GLM价15.843328元，非账单或计费硬限，实际Flash编辑价格更低。产品五调用/401920/900秒保持。

**反证与失败决策：** 标记收缩可能让可选澄清再升级为issues，也可能把真错藏入无解释标记；原正/负/混合例正是对此的检查。首个错误判断、无据意见、错误改文、传输/身份/协议/预算/安全失败停止；格式恢复前仍须检查原始坏响应，不补字段或删除旧文本。失败时按首次偏离决定去留，不继续改同一句报告碰运气。

**当前动作：** 完成新执行器接线/离线与独立复核，冻结计划和公共CI后执行该批。其余产品依赖保持如下。

## Dependencies and Follow-through

| 后续工作 | 何时推进及验收 |
|---|---|
| 修法与同版本质量 | 根据上项证据选最小机制修复；原问题、正确全文、真错/混合例及既有身份/来源回归；不拼不同版本成功 |
| 产品资格绑定 | 新组合绑定原15输入、实际政策与传输；资格证据必须来自同组合真实回执和独立逐项审查，不能复用旧五例或两份固定报告 |
| 真实产品消费 | 质量达到既有要求后验证真实生成/工具/审查修订；复用已有 Evidence/事务/Worker/API，补当前组合真实 DB 和 UI 证据 |
| Agent 产品与前端 | 自然请求、训练采用/反馈、四块联动、整体审美/必要重做/英雄头像，按原依赖推进；可独立部分不被单一评审阻断永久冻结 |
| 完整退出 | 保留身份运维、两树整合、独立评估与学习；具体入口见 restart plan“全局后续”及 62 主题，不在此重排主阶段 |

## Next Step

当前精确 checkpoint：`8e-productization / candidate-real-golden-slice / in-progress / offline-hardening-and-live-consumption`。
具体下一动作及失败分支仅维护在上方 `Active Work Package` 的“当前动作”，此处不复制动态排程。
全周期按交付方法执行，工作包结束或出现关键反证时复核策略与下游依赖；普通工程调整沿用已有授权。

## History

2026-09-22 整理前的 3596 行计划完整保存在
`docs/archive/2026-09-22-execution-method/task-plan-before.md`，哈希见同目录 manifest.json。
阶段历史完成记录原样保留；当前阶段以 canonical 和学习账本为准。progress/findings 保留详细结果。
