# 完整重评的离线可行性裁决

状态：完整重评的结构可行性已离线验证；语义设计尚未合格，未准入真实调用。
归属现有 ADR-0102，不注册新产品 Runtime。

要解决的是首评状态与第二步合法操作不匹配。旧补丁无法修复整段重复且最多改 16 条；
结构修复仍不能证明解释对象正确。当前 high、预算、完整上下文标准和 offline_only 保留。

比较三个实现形态：旧有有界补丁；每个原文 block 固定一份判断；完整重评但守住旧引用覆盖。
程序已有 SourceIndex，可继续负责来源定位。每 block 一份判断不能表达同段混合事实/推断；
进一步句子切分也不等于自动识别断言，因此不先建立另一套语言分句器。

离线原型用同一 FirstWire 描述首评和重评。完整结果允许重新组织 claim；程序按同一 audit、
同一 block 的字符区间核对：每条旧引用都由新引用的并集完全覆盖。合并重复、拆分大段和
更新全部条目都可行，截掉错误尾句、换源、清空原审查内容则拒绝。原始判断/说明保留审计。
这只证明文字覆盖，不证明重评真正处理了每个命题，后者必须保留独立反例和人工语义审查。

旧 issue 默认必须逐字保留；撤销或变更必须逐项提供有效证据编号和理由，原问题留在审计中。
high prompt injection 仍为终止，不可通过重评清掉。新增问题及最终判断继续过完整最终校验。
首评缺来源、缺块、缺标题、JSON 不完整等明确终止；不吞后缀或猜缺失判断。

完成条件：用可执行离线测试证明重复重组、超过 16 项更新、超过 16 标题以及 issue 保护，
核对实际 v10 回执和两份冻结完整报告的输入尺寸，并展示至少一个“结构通过仍语义错误”
反例。总预算继续实际用量结算，不制造五次最大预留之和的新门槛。原型若仅结构可行，
结果必须标为该层可行，不能解除真实入口；后续语义设计需要独立裁决。

## 本轮结果与选择

选择保留“完整重评＋字符区间覆盖＋显式问题处置”作为下一步离线原型，停止扩建旧补丁。
这不是选择其生产采用或恢复实测。`golden_reassessment_feasibility.py` 没有 Provider、
运行器、注册或在线开关；旧候选的 live hold 继续生效。

| 项目 | 实际结果 |
|---|---|
| 重复整段 | 可拆为完整覆盖该段的不同片段，或将相同重复引用合并；旧说明完整留审计，不修改回执 |
| 更新容量 | 合成状态的 48 项判断、超过 16 个标题以及超过 12 个 issue 的处置均可表达；首评和重评使用相同结果容量 |
| 防止“删错过门” | 缺旧引用任何字符、换 block/audit、漏处置旧 issue、无效证据、事实数值错、high 注入、旧状态变化及损坏输出均拒绝 |
| 真实 v10 形状 | 明确标注的离线人工投影保留 12 条原引用覆盖，最终结构 pass；第二步输入上界 55686 |
| 正反例全请求 | 首评 45830/45902，第二步形状 55686/55762；另一历史已修订报告重评首评 45984、第二步 58062 |
| 十份历史第二步形状 | 55872–60866，均小于现有 63936 预检阈值；不是新模型响应或准确率 |
| 修订形状 | 完整修订请求测量 54004；所用评估是合成投影，仅尺寸证据，不是已接受的问题清单 |
| 大形状 | 48 条各 500 字说明的首评虽然 schema 合法，但完整后续请求超预算并在调用前拒绝；不保证任意状态可完成 |
| 语义反例 | 真实 c010 的“五局混合样本”说明原样保留时，结构仍 pass。说明源覆盖和引用存在不等于语义正确；准入仍 false |

测试共 99 项通过，其中新增原型行为/反例 23 项，其余覆盖当前首评、补丁、阻断及上下文
合同。首次测试发现测试断言未考虑 Pydantic 补齐 quote_ref 的 null 字段，修正的是断言，
没有放宽运行校验。首次成对尺寸脚本发现正反例 block 数不同，已将来源重绑定显式记为
尺寸投影，未改模型响应。历史 malformed 后缀仅由既有 sizing helper 显式剔除用于测量；
原型实际接收仍严格拒绝同一坏输出。

## 下一项具体工作

补齐“比较对象—证据—解释”的语义合同，再决定是否接入完整重评工作流：

1. 用本轮原始 c010 和已知标题错绑，明确语义验收检查的对象。原文引用和标题编号由
   程序负责；样本成员、分路、胜负组、指标和可复算值从确定来源生成。模型仍负责判断
   这些证据是否支持原文含义，不能把规则匹配或一个 source ID 称为语义证明。
2. 比较在第二步显式核对上述关系与当前自由说明的输入/输出成本，复用当前两步/五调用
   预算。不再堆一份独立的“解释复核说明”，也不增加模型调用来代替设计。
3. 把同方向但不同样本、同数字但不同指标、错误标题归属、跨段限定和真实未来外推一起
   用于离线合同检查。支持的正确观察应保留；反例应被识别或明确保留为必须人工验收的
   语义风险，不能靠消除说明或手改结果证明成功。
4. 只有整体方案能说明上述关系如何被审查、完整请求仍可执行，才接工作流并按原计划
   进行一次成对真实验证。这里没有新增用户审批要求，仍按既有授权执行。

可复现命令（无网络/Provider）：

```powershell
.venv\Scripts\python.exe -m scripts.check_golden_reassessment_feasibility --source-run data/runs/golden_slice/golden_20260910_compact_1cd694d --base-report data/runs/inference_development/inference-dev-c69cb3e-evidence-v7-report/revised-report.md --case-dir data/runs/inference_development/contextual-review-6bcaa86-decisions-v10/contextual_01 --history data/evaluation/results/golden_contextual_decision_readiness_v10.json --output <new-output-path.json>
```

结果：`data/evaluation/results/golden_reassessment_feasibility_v2.json`。
v1 是同轮未扩展历史形状前的有效中间记录；v2 是完整裁决依据。
