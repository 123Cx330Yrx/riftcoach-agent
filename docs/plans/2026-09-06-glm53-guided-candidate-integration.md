# RQ-238：查询指引接入候选执行路径

## 目标与当前依据

用户已决定采用 GLM-5.3 Flash，并授权继续接入工作。开发观察 guided_03 在实现
76966b5 上得到 3 来源、95 分、published；这是开发样本及模型评测证据，不是独立领域验收。
后续 b959854 的公共 CI 33979244817 已通过。guided_02 只留下 pending 占位记录，不能称为完整失败回执。

## 本批步骤

1. 复用现有 Context 快照合同，为指引建立固定 ID/摘要和候选执行器工厂；工厂重建快照并验证输入、策略、
   每案上下文承诺后才允许执行。不要修改已消费的 RQ-227/230/235/237 资产，也不新增一套复制的门控框架。
2. 开发探针消费同一工厂，添加生存调整、补刀经济与不可信资料三个代表场景。它们明确是开发集，不是新考卷。
3. 用离线测试验证指引实际进入 system 上下文、身份漂移在调用前被拒绝、零来源/注入/质量门不变；
   通过公共 CI 后运行每个新场景一次有界真实观察，不重复已通过的基线。

## 边界与验收

候选执行器 → 可信指引与 Skill 上下文 → 模型自主知识检索 → 实际 Evidence → 85 分质量门 → 类型化输出。
不改全局默认模型、GLM-5.2 回退、Portal/Account/Workbench/Auth 或生产媒体；不把代表场景成功宣称为正式准入。
本批完成意味着候选路径和开发探针不再使用不同的指引接线，代表场景具有真实结果。后续仍须完成独立领域验证、
RuntimePolicy/Trace/Skill 版本绑定及端到端产品接入；不能以此批替代产品实际采用。

## 验证记录

离线代表场景验证已完成：recent review、生存调整、补刀经济三种场景均经过同一候选绑定、真实本地检索、Evidence 与 Harness 发布链；30 项相关测试通过。尚未对新增场景发起真实 Provider 调用。
