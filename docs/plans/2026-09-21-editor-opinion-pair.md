# Frozen editor opinion-view pair — 2026-09-21

## Completed result and decision (latest)

Executed once on clean `483f91d7ffbdbc5ebe412a6fed437f7e43be4121`, after exact-SHA
Actions `35601045617` passed all three jobs. Both requests completed with `stop`:

| Condition | Actual decisions | Full report review | Tokens |
|---|---|---|---:|
| locator_only | apply / withdraw | Repairs false combined-CS parity in block6; preserves correct block4 | 17999 |
| full_opinion | apply / apply | Repairs block6, but repeats generic-to-universal false allegation and unnecessarily changes block4 | 14983 |

Total 23418 input + 9564 output = **32982 tokens**, 89.902 seconds, two requests,
unknown usage zero; historical Provider requests at least275. No final review,
product publication or second paid batch. The dedicated pair runner is now
`offline_pair_completed_identity_unresolved`; native/editor/product remain offline.

Parent and independent reviewer read complete public responses, original/actual
reports and factual/knowledge sources. Locator issue2's selected source1 does
not directly support all numbers and champion-role mapping in its explanation,
although the full input supports them. Its paraphrase of “strongly pulled down”
as “affected” is not exact. These limits remain recorded; correct dispositions
do not establish wholly precise citation practice. Added K2 supports the method,
not numeric facts. Blank-line compression retains25 backend blocks and does not
establish a rendering bug in the current plaintext/pre-wrap renderer. No new
substantive report fact error was found in either arm.

The observed condition difference is real, but the package/task-framing change
and one sample per arm prohibit causal or stability claims. Shared-policy
delivery did not suffice in the full-opinion arm. This is progress in diagnosis
and one concrete correct edit, **not** proof that the core first-reviewer false
positive is fixed, or that an editor candidate is qualified.

Reproducible public evidence:
`data/evaluation/results/golden_native_editor_pair_result_483f91d.json` and
`golden_native_editor_pair_adjudication_483f91d.json`. The exporter
`scripts/export_native_editor_pair.py` reconstructs frozen/prepared/issued
requests (including both budget-owned transformations), source selections,
journals, complete reports and usage. Manual adjudication binds every original
run file by hash (24 existing files). This run did not retain a raw Provider
response-body file; its private body cannot be claimed as hash-archived. If raw
files exist in a supplied run, the exporter hashes them without parsing or
exporting their private reasoning. Runner results retain their original manual-review-pending
state; the separate adjudication does not rewrite them.

## Identity finding and next engineering decision

The offline witness `golden_native_locator_identity_counterexample_v1.json`
places a true combined-CS allegation and a false “stable means exactly equal”
allegation in block6. Swapping them reverses the required per-issue decisions,
but both locator views are `[{block:6},{block:6}]`. Their host review hashes
differ: they distinguish identity without exposing the readable mapping between
an issue_id and its allegation. Both full constructed reviews validate. An
independent reviewer confirmed the witness; no model call was made for it.

Therefore locator-only cannot directly replace the existing per-allegation
editor contract. It may still support useful paragraph review; it has not been
shown to fail at detecting or repairing paragraph errors. The current real
pair uses distinct blocks6/4 and is unaffected.

The next deliverable is an **offline issue-identity and independent-edit contract
decision**, before another live experiment. Reuse the existing anchored-review
and editor prototypes; do not automatically introduce another model call:

1. Bind every original allegation to its original issue_id, exact original
   claim, whole-report context and source snapshot. No dropping issues or
   inventing a success status for an unaddressed allegation.
2. Decide what the editor needs to see to judge that particular allegation.
   A quote alone is insufficient when two allegations concern the same quote;
   a neutral reformulation must not silently change the allegation or introduce
   analyst answers. Retaining an original allegation can retain bias: removing
   suggested edits is a testable hypothesis, not a proven fix from this pair.
3. Exercise same-block/different-claim and same-claim/different-allegation cases,
   true-only/false-only/mixed cases, source-selection completeness and actual
   report diffs. Retain both real rejected responses as rejected evidence.
4. Demonstrate the full generation/review/one-edit/final-review path using the
   current budget and real assembly with scripted responses. Identity checks
   remain structural evidence, never semantic acceptance. If the proposed view
   cannot preserve meaning, reject it offline rather than pay to rediscover it.

Only an independently reviewed, source-bound and budget-reachable contract can
be proposed for a new frozen bounded batch after exact-SHA CI. No automatic
opinion-hiding adoption, rule patch followed by another run in this turn, GLM
downgrade, additional revision slot or weakening of the complete-context standard.

Stage8E remains in progress. Next product milestones still require same-version
quality qualification, source-bound generation/review/revision and Evidence,
actual Worker/DB transaction/API/Workbench consumption, then independent
evaluation and learning coverage. Frontend aesthetics/rebuild, portraits,
Coach/Review/Training/Evidence linkage, personal Training and all62themes remain
in `2026-09-16-astra-restart-plan.md`; the separate main checkout stays untouched.

The sections below preserve the pre-execution design and authorization record.

## Question and evidence boundary

The old native first reviewer and editor v1 both expanded a generic statement
into a universal one. Editor v1 also omitted newer domain policy. That delivery
gap is fixed offline at e37e2e0, but no semantic improvement is proven.

This diagnostic asks whether exposing the complete old opinion package versus
only paragraph locators yields different judgments of the same full report
under the same aligned semantic rules. It does not isolate rationale alone:
severity, category, source choices, score, verdict, explanation and suggested
correction are all omitted from the locator view. A locator also changes the
job from judging a particular old allegation to independently checking its
paragraph. One sample in each condition cannot establish a causal anchoring
mechanism, stable effect, reviewer independence or product qualification.

Both requests use the same new diagnostic policy and EditorOutput. The common
adaptation explicitly states that a locator is not evidence of an error, that
the full old review is retained by the host, and that review_sha256 binds that
host review rather than the visible view. It replaces inherited statements
claiming that all prior reasons are shown. Full-opinion is therefore not the
unaltered editor-v2 request, and the old v1 result cannot replace this condition.

All source/report input, output schema and model settings are the same. Only
proposed_review differs. The frozen original review, actual report and all
source hashes remain on the host. No expected disposition, analyst correction,
one-true-one-false hint, old failure outcome or case name enters either request.

## Frozen execution

`scripts/run_native_editor_pair.py` builds both requests before Provider I/O and
checks them against `golden_native_editor_pair_plan_v1.json`. Order is fixed:
locator_only then full_opinion. Each starts independently from the original
report. The first response cannot modify or enter the second request.

The entire pair shares one budget and hard cap of two calls / 401920 tokens /
900 seconds, with GLM-5.3-flash/high, 32768 output, 63936 input, 300 seconds per
request and SDK retries zero. Input ceilings are 42176 and 43924; including both
output reservations, the pair reserves 151636. These are two alternative edits,
not a product chain with two revision attempts; no final review or publication
exists here. Existing product limits and native/editor qualification gates
remain unchanged and offline.

Semantic mismatches are observations in this precommitted comparison, so the
other frozen condition still runs. Transport, schema, source identity or budget
failure stops immediately, preserving an incomplete pair. No retry, supplement,
change to the other request or second patched paid batch is allowed this turn.
This completion rule applies only to this pair, not the existing qualification
batch's first-failure stop. A create-only experiment directory prevents resume.

Current continuation and existing bounded diagnostic authorization cover the
pair after independent request/execution review, local tests and clean exact-SHA
public CI. The previous turn's no-further-paid boundary is historical; the old
failed batch will not resume. No new permission turn is required.

## Reading outcomes

Read each full response, selected sources and complete actual report; compare
both to the original review's specific allegations. Matching dispositions
alone cannot certify that the same issues were addressed or that a true error
was actually corrected. Keep structural completion and semantic acceptance
separate; all runner outputs have semantic_approval=false until independent
manual evidence is recorded separately.

| Full-opinion / locator outcome | Maximum supported conclusion |
|---|---|
| Fails / passes | This sample differs with the opinion-view condition; not causal proof. |
| Same error / same error | Removing old opinions did not avoid the error in this pair. |
| Passes / passes | Both current outputs are correct; old failures remain valid. |
| Passes / fails | Locator-only may lose useful information; inspect the actual findings. |

Preserve actual issued request bytes, Provider receipts, public response text,
full edited reports, manual decisions and known/unknown usage. Private reasoning
never enters public evidence. Neither condition is product admission or proof
that the initial reviewer was repaired.

After this pair, decide from evidence whether to retain the aligned editor
candidate or reject the opinion-hiding route. Do not add prompt instructions
and repeat paid runs in this turn. Stage8E's same-version qualification, product
Evidence/Worker/DB/API/Workbench, independent evaluation/learning and all deferred
frontend/portrait/four-part Training requirements remain in the restart plan.
