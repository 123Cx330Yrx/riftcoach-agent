# Editor diagnostic decision — 2026-09-21

## Result and admission decision

Implementation `685993ddc8d7b6050676028f0cfb7d181cf9644c` passed public CI
`35596881322` (pytest, postgres-migrations, packaging-smoke). The separate
`editor-diagnostic-685993d-v1` then issued exactly one real editor request for
the frozen mixed true/false case. It completed normally in 29.744 seconds:
11143 input + 2970 output = 14113 tokens; unknown usage zero. Historical Provider
requests are now at least 273. No final review or later case was executed.

The editor applied both proposals. It corrected the explicitly false five-game
combined-CS comparison but also applied the existing generic-to-universal
false positive. Thus the editor's issue adjudication failed. Its JSON, issue
inventory and source identities were valid; the runner's `protocol_success=false`
includes the host disposition check and must not be described as malformed JSON.
The diagnostic is now `offline_editor_scope_false_positive`; native and product
remain offline. B v1 is rejected as the current repair candidate. One outcome
does not disprove every possible issue-adjudicating editor architecture.

Public evidence: `data/evaluation/results/golden_native_editor_result_685993d.json`.
The exporter reconstructs the actual request from the frozen plan, allowing only
the budget-owned timeout and coach_budget_contract metadata transformations, and verifies original wire bytes,
response/report/source identity, body-free receipts and usage. Raw Provider
response files are hashed but never copied into public evidence; private
reasoning stays local. The failed original `354d752` batch remains unchanged.

## What the actual text establishes

| Check | Observed evidence | Decision |
|---|---|---|
| True error | Five-game loss CS is about 6.45 against win 8.805; edited text uses source aggregate 8.8 and correctly rejects parity. | Corrected in this edit. 8.8 is an existing aggregate value, not a newly proven HALF_UP error. |
| False positive | The editor calls the original phrase generic, then uses opposite vision/early-death directions to declare a contradiction, without establishing a universal claim in the original. | Still wrong under the accepted whole-context criterion. |
| Full edit | Block 4 was unnecessarily rewritten; block 6's true error was repaired. Opening retrieval prose was removed and list spacing changed. | No new factual defect found; unnecessary editing is not proof of successful false-positive handling. |
| Execution | One complete stop, valid JSON/digests, full sources, well below output/time budget. | Not truncation, missing data, missing fields or exhausted tokens. |
| Policy parity | B v1 uses `native.previous.FULL_CONTEXT_RULE` and TABLE_POLICY; native's later object/group/quantifier rules, statistic-array guidance and attribution magnitude guidance are absent from B's policy. | A real construction gap, not evidence that fixing it alone repairs model semantics. |

Earlier `354d752` native review already had the generic/non-universal rule and
still made the same false positive. Consequently the omission is not a proven
sole cause. The single current observation cannot distinguish suggestion
anchoring from a shared interpretive bias. Numerical source references establish
directions, not the author's quantifier. This missing entailment is observable
in the public explanation without access to private reasoning.

## Offline correction completed and remaining decision

The cross-phase construction gap is now corrected. Native exports named
SEMANTIC_POLICIES for scope, identity/training, computed statistics, attribution,
source use, external evidence and lossless tables. Editor requests consume those
same rules; reviewer-only score/verdict/issues/reassessment output instructions
are excluded. Native's complete serialized policy remains byte-identical:
SHA256 d2400441cb06b8c8f31b0085a8e0d7ad04063cf9b0d80efee0c2cc49663f8e25.
The original v1 request/policy/failure is unchanged and reconstructs exactly
following the refactor. Editor v2 is offline under new frozen plan_v3 identity;
its source data and output schema remain identical to v1.

104 related tests pass. The original real bad response remains structurally
valid and is still rejected before recheck. Actual serialized first/editor/final
requests contain each shared semantic rule exactly once; editor receives none
of the reviewer-only output contract. Independent review repeated the 23 focused
policy/runner tests and verified historical request reconstruction.

New offline contract evidence v4 measures editor input 43666, claim five-call
reservation 390632 and historical reservation 406352. The real budget still
stops saturated historical usage before the fifth request. Product budget
artifact v3 uses actual compiler/local RAG/Runtime with scripted responses:
generation twice, review, editor and final review take five calls/368162 synthetic
tokens; one extra reassessment still blocks the sixth call with no publication.
These are execution proofs, not model quality or external-Evidence qualification.

The initial manifest regeneration omitted coach_execution_contract, causing ten
local product composition test failures. Restoring the full original component
list and updating only the changed native component fixed the error; actual
Runtime construction and all 104 tests then passed. Final manifest diff changes
only native's source fingerprint and program_sha256. No paid request followed
this local error. Do not reconstruct a component list from a partial helper.

The next action is to review the aligned candidate and its discriminating test
plan against both real failures before another paid batch. Rule delivery is now
consistent; the initial reviewer already had the relevant scope rule and still
failed, so consistency alone is not a sufficient adoption argument. Keep initial
review accuracy, editor issue adjudication and final report correctness separate.
A further experiment must answer a specific unresolved comparison, not simply
run the same case again hoping for a favorable response.

Retain this real response as a structurally valid but semantically rejected
counterexample. A shared policy does not make that response correct. Do not
silently filter the original issue, soften true errors into suggestions, add a
universal prohibition on clarifying ambiguity, or make historical failures pass.
Any later test design must distinguish whether the same full report is judged
differently when prior suggestions are present; an additional editor must not
be assumed independent merely because it runs in another call. Such diagnosis
is not a new production policy, increased budget or permission to change GLM.

No patched second paid batch in this continuation. Do not repeat the remaining
two cases after the first failure merely to accumulate favorable scores. A
subsequent bounded candidate needs its own frozen requests, independent review,
exact-SHA checks, whole-output acceptance criteria and unchanged user standard.

Stage8E remains open. Same-version full qualification, source-bound product
Evidence, actual Worker/DB/API/Workbench consumption, independent evaluation
and learning are still pending. Frontend aesthetics/rebuild, champion portraits,
Coach/Review/Training/Evidence linkage, user-requested personal Training and all
62 themes remain under the Astra restart plan; this experiment replaces none of
those deliverables.
