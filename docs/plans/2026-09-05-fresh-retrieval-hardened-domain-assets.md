# RQ-237：检索加固候选全新领域资产设计计划

## 目标

为 `coaching-query-recovery-v1` 建立一套与 RQ-235 完全隔离的 V3 领域协议和三案例资产，先完成
离线身份准入与预算证明，再等待后续新鲜协议和真实观察授权。

## 设计任务

1. 新建 Dataset、Input Plan、Prompt/Context Snapshot、匿名 fixture、marker、协议 ID 和 body-free 回执身份。
2. 在请求计划中显式绑定候选 request policy、`quality_hardening=True` 与 `retrieval_hardening=True`。
3. 证明每案/全域调用与 token 上界，保留 V3 最多一次修订及 85 分事实/引用/注入/来源门。
4. 加入可复现的短查询恢复样例、已有命中样例和安全拒绝样例；诊断只使用固定枚举和计数。
5. 通过 no-I/O 资产交叉校验、聚焦回归、compileall、diff check、governance 和同 SHA 公共 CI。

## 后续顺序

资产设计完成后，另立实现批并取得公共 CI；再在新资产身份上取得一次新鲜 G53-3-L。只有协议证据
通过且用户明确授权，才执行一次新的 V3 真实领域观察。任何阶段都不重跑 RQ-235 或覆盖旧回执。

## 限制

本计划不注册候选、不切换产品默认模型、不修改 Portal、Account、Workbench、Auth 或生产准入，
也不发送真实 Provider 请求。

## 2026-09-05 实现纠正批（已完成本地）

`1cb456b` 的公共 CI `33949027784` 三任务成功，但准入覆盖复核发现：历史身份列表漏掉
RQ-235，输入计划未显式声明检索开关，预算重建仍走默认关闭的检索策略。先修复这些实现缺口，
不把旧 CI 绿灯当作新行为已经验证。

- [x] 输入计划/协议/准入绑定策略并覆盖全部三个旧版本；补齐质量门与运行时身份负例。
- [x] 用开启检索策略的实际离线路径重建预算；新上界为 205000/613000，旧 V3 的
  203000/608000 保持不变；把新上界一致传递到候选入口和回执。
- [x] 聚焦和相邻回归、编译、治理通过；修复提交 `d7a92ab` 的公共 CI `33963143593` 三任务全绿。

本批是在新资产尚未真实执行前修正工程合同，不改变题目、提示词、检索阈值或评判规则。
用户已允许既定主线连续推进，不再把逐步重新授权写为停点；真实请求必须先满足技术预检。

新鲜协议及领域观察已完成：协议 3/3 调用通过；领域首案 2 次调用、`6624` tokens、`20453ms`，
回执确认主题未映射且零来源，以 `evidence_required` 停止，后两案跳过；实际查询未留存。
已完成候选白名单离线修复和 61 项回归；RQ-237 回执保持不可变，后续验证必须另立新鲜版本。

## 开发链路稳定性验证批（本地完成）

先补独立开发测试，不创建下一套 held-out 考卷、不发真实模型请求。复用 demo fixture 和真实本地语料，
可控 Provider 固定工具请求、完整报告和评测结果；它只验证工程控制流，不证明模型质量。

- [x] 14 个正例覆盖七类教练主题，来源实际进入工具响应和持久 Evidence。
- [x] 真实执行器的完整七段报告发布、三例零来源拒绝、一例低评分拒绝均通过。
- [x] 三例混合/注入查询不扩展；没有修改或真实重跑已消费资产/回执。
- [x] 新增 21 项通过；包含旧版本、总回执校验的相邻测试合计 112 项通过。

入口：`tests/test_coaching_retrieval_development_chain.py`。采用 demo fixture，不使用 held-out 问题；
socket 连接被测试强制禁止。模型工具查询、正文、评分均为可控输入，只有检索/证据/执行器/Harness
是真实实现，因此不能把 14 个脚本查询通过外推为 GLM 自主选词稳定、通用自然语言覆盖或生产准入。
下一步是开发阶段验证模型自主查询与取证行为，不再直接换新 held-out 考卷；旧结果始终保留。

## 自主查询开发观察（本轮继续授权）

只运行一个匿名 demo 两局复盘问题，使用已通过CI的生产执行器和现有候选low/4096策略；查询、
草稿、评测由GLM生成，最多一次修订、9次调用、205000累计Token、零SDK重试。单请求观察超时45秒，
超时只记为开发观察未完成，不解释成模型质量差。失败不自动再次调用。
新诊断脚本只投影主题枚举、查询长度/词数、过滤键名及调用/证据计数，不保存正文或工具参数；
完整中间工件放TemporaryDirectory并在正常结束清除。通过HEAD与生产路径差异检查确认执行代码身份，
另记诊断脚本SHA，不把本地脚本冒充公共CI已验证。只验证开发用例，不生产准入、不创建新考卷。

### 开发观察01结果（已执行，不自动重试）

脚本 `scripts/probe_glm53_development_retrieval.py` 已通过离线替身验证后执行一次。生产代码HEAD为
`dde1f8494407e1bd74bac04ceeef76d028d5ace3`，其公共CI为33966158145三任务成功；探针自身仅本地验证，
回执单独绑定probe_sha256，不冒充公共已验证。只读加载主仓库.env，未改配置。

实际2次模型调用、6861 Token、32126ms；模型自主生成两条查询（字符数59/65，分词数9/11，
可识别主题分别为economy与survival+vision，过滤键名为空），两条完整查询均unmapped；
两次工具成功，零片段/零来源，evidence_required拒绝，未评分/修订。查询原文未保存，不能补写具体词句。
回执：`data/evaluation/results/development/glm53_autonomous_retrieval_dev_01.json`；无临时正文残留。

下一步：设计并离线测试开发专用可信知识库查询指引，与本次基线作有界对照；不改检索阈值，不重跑
RQ-237正式考卷，不把本观察当准入。已有COACHING_QUERY_GUIDANCE_V1目前未进入执行上下文，
这是可检验的下一假设，不是宣称真实词句根因已经全部确定。

### 开发查询指引对照（实现与离线验证完成）

已将 `COACHING_QUERY_GUIDANCE_V1` 作为仅开发观察可选的上下文附加项接入；正式准入执行器默认不携带，
因此不会改变既有合同或 GLM-5.2 兼容路径。对照回执显式记录指引 ID 与 SHA-256，基线回执保持空值，
避免把两种运行混为一谈。新增改动通过 66 项聚焦/相邻测试、治理检查与差异检查后再提交；下一步仅执行一次
同一匿名 demo 的真实有界对照，不创建新考卷、不重跑旧回执，结果仍只作为开发诊断。
