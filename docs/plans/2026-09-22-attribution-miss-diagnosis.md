# ADR0104 归因漏检：事实、竞争解释和下一决策

## 已确认事实

执行 HEAD 651a207 / CI 35697455886 三项通过。原归因负例首评一次合法
tool_calls 返回 93/pass、issues=[]、两条范围措辞 advisory；block14 的
“全样本补刀与伤害差距主要来自辅助局拉低”完全没有被报告。
未编辑、未复评；不是把目标错误降成 advisory，而是漏掉它。
1 次调用，12149 输入 / 3412 输出、未知0。原回执不改写。
完整公开证据：`data/evaluation/results/golden_native_partitioned_tool_public_651a207.json`。

实际请求含全部报告和来源，已经提供原精度 role_contrasts：

| 指标 | 五局胜减败 | 中单胜减败 | 两者之差 |
|---|---:|---:|---:|
| cs_per_min | 2.351666666666666666666666667 | -0.205 | 2.556666666666666666666666667 |
| damage_per_min | 695.4183333333333333333333333 | 669.235 | 26.1833333333333333333333333 |

移除辅助局后伤害差距仍有约669，不能说约695的差距主要来自辅助局。
来源投影与独立逐局重算一致；请求也已有逐指标区分“影响”和“主要差距”的政策。
因此不能再以“补上归因提示或算术”作为已诊断的修法。
可选措辞建议在本次没有误升为阻断，但三个已通过开发例不能抵消本例漏检。
当前候选批停止，剩余11个输入不继续付费，也不进入产品准入。

## 尚未区分的解释

- 全文覆盖不足：模型能辨认这类归因，但自由列问题时漏审了目标复合句。
  支持：合法输出集中在两条范围建议；反对/不足：仅凭没输出不能知道内部是否检查。
- 定点语义/证据运用不足：即使指定该段，也未正确把两个指标与量级分别核对。
  支持：原请求早已有计算和规则却漏检；反对/不足：尚无此版本定点对照。
- 指标索引、上下文负荷和随机波动仍可能参与；没有干预证据前不称根因。
  本次已排除的是输入缺失、预算耗尽、传输截断和解析丢弃目标issue。

## 最小区分检查

冻结 `golden_partitioned_focus_plan_v1.json`：原错误稿与既有正确稿，各一次调用。
唯一任务改动是指定第14段作为判断对象，完整原文/来源仍逐字发送，工具Schema不变；
不发送预期标签、分析者算术、旧评估或修订答案。预期仅存host。
模型Flash/high、每次32768/300秒、共2调用，复用401920/900秒共享上限；
无修订、无字段恢复、无自动重试。协议/传输失败停止，两次合法语义输出都保留。
源fixture为已提交真实请求，与冻结输入hash一致，不依赖本机data/runs。

| 人工审查的结果 | 改变的技术决策 |
|---|---|
| 真错检出、正确对照通过 | 说明定点判断有可用证据；下一步评估通用覆盖机制，而不是继续堆归因规则。一次配对不能证明因果或稳定率。 |
| 真错仍漏检 | 不采用“只补覆盖定位”的方案；进一步隔离证据布局/语义判断，不再原样重跑全文。 |
| 正确对照误报 | 聚焦不足以保持采用标准；查实际含义/来源冲突，不把全部归因设为错误。 |
| 协议/执行失败 | 不回答语义问题，停止配对并定位请求或传输；不扩预算。 |

定点诊断使用人工定位，不能冒充Agent自主发现，也不能把通过结果拼入15例完整资格。
若后续要增加逐段任务/输出，须先证明不会重现旧逐claim巨量输出、超时与五调用预算问题；
复用现有运行时，不先搭建新的多Agent审查框架。

## 实际结果及选定下一步

cb69f82 / CI35698979457三项通过后，冻结配对完成：错误稿80/needs_revision，唯一issue在block14，
正确稿95/pass且无issue/advisory。2次调用，24425输入/2313输出=26738，未知0，无恢复/改稿。
逐条来源核验确认其使用原计算的695.418、669.235、26.183，问题及修正方向正确；解释中括号列的是
显示舍入数，不能把由此产生的0.005差异当作实质归因错误。结果未用于替换报告。
实际请求仅比prepared多出共享预算metadata，messages/tools完全一致。
完整公开请求/响应/journal、transport和文件hash存golden_partitioned_focus_result_cb69f82.json；
原machine result的manual_semantic_acceptance=false保留，人工定点裁决另列。

采用上表第一分支：试验紧凑逐段结果，保留完整上下文，减少自由列问题时跳段的机会；
不是断言根因已被证明，更不是保证逐段列表可证明语义完整。具体替换与限制见ADR0105。

## 自主全文试验的中断与下一诊断边界

ADR0105首例a501c33/CI35700582136未完整返回；300秒截止、未知用量1，详见独立中断artifact。
原任务判断仍未知，不据此撤回定点观察，也不据此宣称逐段机制有效或无效。
原body-free进度未区分tool deltas，已新增时间/数量记录；不能据content_chars=0推断无工具输出。
短GET代理/直连均401只证明当前可达，不能证明历史长连接或Provider正常；不改变全局代理。
下一次付费前须冻结能区分长流/终止缺失的诊断干预，不做同请求碰运气重试。

### 路径对照（执行前冻结）

`scripts/diagnose_block_review_route.py` 重建原中断请求，逐字节校验原reservation SHA256
`863766f614d73ff4eb5d3c54e1128d14ec6d4d48ca5efd445faf59681f4bc067`。
保留完整来源/报告/Schema/Flash-high/32768/300秒/SDK retry0。只在诊断进程及其子进程
暂时设置代理环境，退出恢复，不改Git或全局代理，不开启失败候选或产品入口。

最多两次独立首评：先直连，完整且协议合法后再经12000代理。首次执行或协议失败停止；
不修订、不重评、不重试。两次输入各44884保守上界，预留总量155304，最多600秒，低于既有
401920/900总预算。先通过新脚本相关测试与精确HEAD公共CI，再读取凭据执行。

- 直连也不完整：代理不是新失败的必要条件；根据工具活动/结束/用量定位，停止第二臂。
- 直连完整、代理不完整：支持尝试直连后续，但单对照有时间/生成随机性混杂，不宣称确定因果。
- 两者完整：历史中断原因仍未知；检查全部意见，判断是否可恢复新批质量验证。
- 无论网络成功与否，完整响应里的漏检/误报仍保留；这个对照不是修订闭环或产品资格。

离线已核对原字节重建、代理覆盖和异常恢复、超时未知费用与停止、公有响应不含私有推理。
旧传输/逐段/定点相关42项及本诊断5项通过；020e52b公共CI35703296766三项已通过。
