# 角色实测结果与知识时间漏检

执行基线：`346213cba34d39466e8d3939ca2ab895569a0bc5`；公共 CI `35814004366` 三项通过。
用户在具体两批预检之后回复“允许，继续吧”，授权至多 2 次 Flash 对照与一次至多 5 调用的混合应用任务。
已完成这两个任务，实际 5 次调用；没有自动追加付费重试。原 15 输入资格、产品默认和 8E 状态不变。

## 实际获得的证据

| 实测 | 模型原结果 | 人工全文/来源核验 | 证明范围 |
|---|---|---|---|
| 原归因错误稿、与此前 GLM 完全相同请求 | Flash 84 / needs_revision，仅 block 14 | 接受：补刀与伤害不能合并归因；七个来源支持意见 | 此输入下 Flash 也能正确检出 |
| 正确完整控制稿、与此前 GLM 完全相同请求 | Flash 95 / pass，四条 advisory | 接受：四条建议来源均核对，未把可消解的范围措辞升级阻断 | 此输入下 Flash 也能避免此前误报 |
| 实际 Agent 任务 | Flash 两调用，触发三次 knowledge.search 后生成；GLM 一调用 96 / pass；开发目录 published | 不接受报告质量：block 27 的知识检索日期无依据 | 实际生成/工具/审查/同源文件发布与计价走通，语义仍未过关 |

Flash 两例共 31,706 tokens、321.875 秒（含人工核验）、全未缓存价估算 0.0398708 元。
混合任务共 36,765 tokens、204.500 秒、估算 0.190476 元；运行 Trace 与逐调用回执核算一致。
合计输入 56,241、输出 12,230、68,471 tokens，未知用量 0，估算 0.2303468 元；不是厂商账单。
混合任务初评直接通过，`revision_exercised=false`，没有实测改稿、重评或改后终评。
Memory 使用 observed 身份的空快照，不证明有内容的长期记忆召回；来源为冻结 ShowMaker 资料，不是新拉取比赛。

这次相同输入对照不支持“GLM 已实测优于 Flash”。保留用户采用的角色组合为待验证候选，
不能将此前两例 GLM 成功解释成分工优越性、稳定准确率或产品准入。

## 首次偏离和可排除的解释

原报告 block 27：“引用知识（检索时间 2026-09-10，均为 evergreen 版本）”。
三次实际本地知识检索均在 `2026-09-23T04:22:11Z` 左右完成，`cached=false`。
K1—K5 只提供文档 `updated_at=2026-07-23`，并未提供 `retrieved_at`。
外部快照在 9 月 10 日检索的时间不能支持知识检索时间这个断言。

| 边界 | 核对结果 |
|---|---|
| KnowledgeProvider → knowledge.search | 工具返回文档更新时间、版本、内容；当前协议没有知识查询时间字段 |
| 工具结果 → KnowledgeEvidence | 五条引用和更新时间保留；没有一个已提供的检索时间被转换器丢掉 |
| 生成输入 → 原报告 | 用户观摩范围及知识信息都送达；Flash 的第二个返回首次写入无依据日期 |
| 原报告 → GLM 审查 | 全文非空行和五条知识引用全部保留；source 25 为知识目录、26—30 为 K1—K5；GLM 返回 pass/无问题 |
| 原报告 → 开发文件发布 | 生成稿、draft、final 字节相同；EvidencePublicationStore 读取校验、manifest/Trace/usage 绑定通过 |

因此不是断流、输出额度耗尽、引用编号错位、审查拿到旧稿或发布时替换文本。
已知缺口是无依据的时间断言与漏检，以及工具执行时间仅在 Trace、没有作为知识来源元数据提供给模型。
尚不能把“某句提示相互干扰”或模型内部注意机制断言为已证明的原因；补时间字段也尚未证明足以消除漏检。
原 `published` 状态和 96 / pass 原样保留，独立人工审查记 `needs_revision`；不能重写回执为模型已检出。

按用户已采用的完整上下文标准，条件观摩练习的局部措辞优化不自行升级为第二个阻断。
`goal_source` 出现在用户报告中属于展示清理项；本次确认的事实问题是知识时间归属。

## 下一工作包

**目标：** 同一知识来源的文档更新时间、实际查询时间、缓存来源时间各有明确含义；
模型没有获得的时间保持未知，不借用 Riot / OP.GG 日期。生成与审查使用同一份可归属的来源数据。

**实现前约束：** 复用现有 ToolRuntime、knowledge.search、KnowledgeEvidence 和 source catalog；
先核对已有时钟与缓存行为，确定最小的元数据传递方式，避免另建工作流或把整个 Trace 塞进提示。
缓存命中不能把旧检索时间刷新成当前时间；文档更新与检索发生时间不能混用。
本节的实现方向已由下方离线修复完成；模型漏检是否改善仍待验证，不把人工删字参考稿或增加一个字段当作自动修复已经有效。

**验证：** 沿真实工具返回、生成输入、证据汇总、审查输入、发布来源逐层检查同一时间身份，
覆盖缓存、缺失时间及不同来源日期；保留现有同位置/完整上下文与业务回归。
本次原报告、原 GLM 返回和只移除无依据日期的人工参考稿已经保存。参考稿只消除此项问题，
不表示已被模型修改、重新审查或授予整篇质量资格。
必要的新模型观察须在实际改动、公共检查、请求预检和有界预算准备完成后安排；不原样重复本次请求。

之后仍需同组合原 15 个不同输入、真实改稿及终评、Worker/DB/API/Workbench 消费、独立评估与学习。
Coach/Review/Training/Evidence、个性化训练、前端审美/必要重做/英雄头像、Memory/RAG、身份运维与两树整合仍在原路线内。

## 首次时间修复与v1预检（历史，2026-09-23）

- 实际native/role候选改用knowledge.search 2.1，主机UTC时间独立于provider diagnostics、查询as_of和文档updated_at。缓存保留原payload；过期重新执行才生成新时间。旧默认仍为2.0。
- KnowledgeEvidence.retrievals逐次绑定provider/retrieved_at/chunk_ids，保留重复chunk的多次来源与混合未知时间；生成、审查、改稿及retrieval_evidence.json使用同源记录。时间非法则拒绝，不借用第三方日期补全。
- 审查和落盘历史字段顺序各自保留。首次改成公共投影时发生的旧hash回归已修正；重建manifest曾漏执行合同指纹，也已补回并用实际组装入口验证。没有改历史输入摘要来放行。
- 143项相关测试与2个子用例通过，覆盖旧业务和新缓存/时区/同源边界；旧空及完整Evidence与提交前序列化逐字节一致，原15冻结输入hash未变。两份候选manifest含新增执行源码指纹。公共CI另按提交核实。
- 使用原三条查询重新执行真实本地知识检索（无Provider请求），五条引用内容与原实测投影完全相同，时间是2026-09-23T07:31:33Z附近的新查询时间。持久数据明确这是新查询，绝不回写旧Trace或把新时间声称为旧工具曾提供。

预检脚本：`python -m scripts.run_knowledge_time_review_pair`。它复用原RoleReviewWorkflow和有界observe，默认只预览，不读凭据、不联网、不写运行目录。执行使用新的具体计划授权hash、干净当前HEAD公共CI、只创建一次的目录，首例必须经host核对全部issues/advisories才能进入第二例。

1. 原错误全文：报告字节不变，增加上述实际检索记录；既有来源、政策、工具协议不变。应检出无依据的知识检索日期且不制造其他错误。
2. 人工参考全文：只删除原日期短语，其余保持；用于核对不会将正确全文误判，不冒充模型实际改稿。

两例共至多2次GLM-5.3/high、600秒，单次300秒/32768输出，SDK retry0；输入预留48730/48706，总token上限162972，按现有单价全未缓存估算上界2.614496元（非账单硬限）。计划及候选身份、来源和请求hash见golden_knowledge_time_preparation_v1.json。旧两项授权已消费完毕，本次未执行新付费请求。

结果决定：原稿仍pass则否定“补齐时间足以修好该例”的假设，停止该批，定位已提供时间与实际审查意见的差异，不重复同请求或扩充15例；原稿检出但来源/建议错误则保留该独立失败；第二例误报则完整上下文标准仍未达到。两例都通过只支持该开发对照，再进入真实生成/改稿/终评路径与既有资格；不能推断稳定率、分工优越性或8E完成。

## v1实测结果与单条引用修复（2026-09-23）

用户批准后，在883c23e / CI35832963559三项成功上执行v1。首例正常完整返回91/needs_revision，
仅block27知识检索日期一条issue，正确指出不是9月10日，而是来源记录的9月23日；两条advisory
仍是非阻断建议。输入13578、输出4378、未知0，估价0.231208元，含host核验380.610秒。
不是断流、输出耗尽或再次漏检。此次单例也不能证明稳定性或真实自动改稿。

host拒绝的独立原因：issue选择source26—30（K1—K5），其值仅含updated_at=2026-07-23，
实际retrieved_at位于source25的knowledge总目录。编号存在且没有偏移，但它们没有携带精确日期
断言的依据。沿用既有“编号存在不代表能证明本句”标准，不新增措辞门槛、不事后替模型换引用。
block8的补刀数字与英雄名称均来自冻结资料，block11的同位置均值关系正确；其K5依据在完整输入
但未另选source30，作为非阻断建议的来源局限记录。官方9月10日是检索时间，不是发布日期。
首例host拒绝后已终止，第二例未发，原失败回执保留在golden_knowledge_time_result_883c23e.json。

**修法选择：** 为可独立引用的知识条目提供它自身的来源时间，复用生成上下文已有的chunk关联规则。
app/harness/knowledge.py的citation_retrieval_fields由生成上下文和知识投影共用；每条只包含与本chunk
关联的provider/retrieved_at。重复检索、未知null均保留，空检索继续留在顶层；旧无时间输入不加字段。
这样审查、改稿和Evidence消费者解析单条引用就能获得依据，不依赖额外记住全局目录编号。
这修复的是工程来源粒度不完整，尚不证明模型今后一定正确选用或理解来源。

**验证：** 真实失败所选26—30离线解析已有对应时间，删除新增字段后与旧来源值完全一致；
缓存/过期/重复/混合未知、生成上下文、实际组装的首评/改稿/终评/落盘通过，旧15输入hash可恢复。
相关193项测试和2个子用例通过；两个活动manifest按各自组合构造，历史资格与默认产品不改。
指纹更新期间role误用native构造器导致本地组装测试失败，已修正；不是Provider新失败。
Luna因服务不支持该模型返回404，未完成独立审查；主Agent已复核调用和消费路径。

**新v2预检：** golden_knowledge_time_preparation_v2.json绑定原失败摘要及新组合指纹，复用原runner
但另用knowledge-time-citation-review-pair-v2目录。原v1计划不覆盖，旧授权hash不能启动新批。
沿用相同报告/人工参考、来源内容与编号、GLM/high政策和有界observer；唯一输入变化是各citation
附上对应的检索记录。最多2次/600秒/164182tokens，未缓存上界估价2.624176元，SDK retry0。
计划hash：8a4b189416a7fede2c7690f9a7dde1c993504d00294739135e4bfb7d48a8dde2。
需本次提交公共CI通过及该新批具体授权；首例不合格即停。若仍有引用/语义错误，保留反例并检查
哪一条断言和所选来源不符；不能靠给所有citation复制全部来源来消除失败。两例都通过后，
再验证真实Flash改稿/GLM终评及原15资格；不把正确人工参考稿当作模型实际编辑结果。

## v2实测与方法级决策（2026-09-23）

用户“确认继续”后，在干净6c3a3db / CI35835943718三项成功上执行预检hash
8a4b189416a7fede2c7690f9a7dde1c993504d00294739135e4bfb7d48a8dde2。
首例147.797秒正常完整tool_calls，95/pass、issues=[]，再次漏检原block27的9月10日知识检索时间。
输入13768、输出3345、合计17113tokens，4992缓存输入已含于输入，未知0；按全未缓存价估算
0.203804元，非厂商账单。host核验后拒绝，批耗时189.016秒；第二例未发送，无改稿/终评。
原始响应hash为03ae0f355cc698863543f0b9fe4f189ec30d7d163229807c61971b192183c5c8。

两条advisory分别核对：block8建议把“5局中最低的中单”写成“四局中单最低”，8.38及洛克名称
来自所选原行，属于可接受的可读性建议；block14新增中单早期死亡胜2.5/败0.5，独立原行计算
(3+2)/2及(0+1)/2均正确，不是报告错误，但选中9/13为补刀均值，1为混合汇总、21为样本边界，
无法支持其新增同位置早期死亡均值。不能替模型改引用，也不能把可选建议改判成报告阻断。
主拒绝原因仍是日期漏检，另记录评估自身的引用支持失败。

### 已完成的诊断

| 解释 | 证据与裁决 |
|---|---|
| 时间仍在转换中丢失 | 排除：实际原始请求与预检逐消息相等，26—30每条都携带其9月23日retrievals；报告block27原字节仍在 |
| 编号整体错位或换了报告/规则 | 排除：每ID解析到原key/path；v1/v2完整消息对照仅citation.retrievals和派生catalog摘要不同，政策/工具/报告/其余来源相同 |
| 传输/输出/时限不足 | 排除：完整tool_calls及用量，约148秒、3345输出，未触及300秒/32768 |
| 补足元数据即可修好 | 本例否定其充分性：元数据已在输入，仍返回空issue；工程修复保留，但不赋予质量资格 |
| 新字段一定导致退化 | 未证明：两版本各一次观察，不能区别输入变化效应与模型波动，也不能计算稳定率 |
| 模型全文覆盖和选源不可靠 | 当前输出直接支持：明确错误被漏掉、建议文字正确但所选指标不相干；内部注意机制及通用修法仍未知 |

方法问题不再归结为最后一个字段。现有机制只验证输出结构、编号存在、回执和预算；
无法从一个“合法pass”证明模型已逐项正确判断。任何继续方案都必须处理实际语义质量，
不能让更多工程测试、格式恢复或来源视图替代它。

### 接下来做什么，以及暂不采用什么

| 方案 | 当前选择及原因 |
|---|---|
| 再调整时间字段或加一句提醒，重复同两例 | 不安排。可用信息已充分，现有证据不支持继续把输入缺字段当主要故障 |
| 给现有流程再加一次全文审查 | 不直接接入。正常2次生成/工具+首评+改稿+终评已占5调用；加层会改变共享预算与900秒闭环，且两个相同审查可能共同漏检 |
| 程序负责确定性来源事实，模型负责分析与行动 | 进入离线职责核查，尚未采用。已有KnowledgeEvidence及EvidenceBundle包含来源时间/版本/可用状态，可复用；减少无必要的自由改写，但不能据此宣称一般审查质量已解决 |

下一工作包的输入已固定：本次原错误全文、删日期人工参考、两次真实评估及完整来源，
原15冻结输入继续保留。先沿实际七节Markdown、生成/改稿/终评、发布/消费合同做一个最小
职责方案，而不是先建第三套候选。必须满足以下边界才值得实现并准备新的真实验证：

- 来源事实由已有同源记录导出；未知、多次检索、过期状态保持原义。分析建议的证据和语义仍需审查。
- 单纯在原报告后追加正确来源栏并不能修复正文错误；单纯删掉错误来源段会改变待审问题。两种捷径均不采用。
- 程序生成的字段和模型正文若矛盾必须仍能拒绝。跨段日期、身份/位置、指标归因、训练条件和长期外推不从验收中消失。
- 保留完整模型原始输出与实际成稿，不能把程序组装的正确文本伪称模型修正；原错误报告仍为漏检反例，不重新标pass。
- 不增加模型调用上限；若职责调整需要实质改变报告合同，先有可审查设计、迁移与验证依据，再记录相应决策。默认及角色候选当前未切换。

本批付费任务已结束。run_knowledge_time_review_pair的execute在读取资料/凭据/Provider之前关闭，
包括带旧授权hash的新空目录也不能重启；离线preview继续能重建原请求。7项相关预检/关闭回归通过。
目前没有新付费计划，不以“剩余一次”追加诊断。真实自动改稿/终评、15例、实际产品消费仍未完成。

证据：golden_knowledge_time_citation_result_6c3a3db.json保留12份原文件hash、全部公开回复/回执、
独立选源核验、算术及请求等同性检查。v1的单例检出与v2的漏检分别保留，不拼成成功率或配对成功。

## 可复核材料

- `data/evaluation/results/golden_flash_source_pair_result_346213c.json`：22 份原文件 hash、完整两条返回及逐项来源审查；与旧 GLM 请求内容完全相同。
- `data/evaluation/results/golden_role_application_result_346213c.json`：38 份原文件 hash、完整业务报告与回执、单独的 host 审查和参考改文。
- 私有 reasoning 不导出；第二次生成请求携带的 Provider reasoning 回放字段从公共投影移除，原始请求 hash 保留。公共请求投影不冒充私有 wire 的原字节。
- 本次运行原文件在 `data/runs/model_comparison/role-explicit-source-flash-comparison-v1` 和 `data/runs/role_development/*/role-development-20260923-v1`，不修改历史失败或默认工作台人工稿。
