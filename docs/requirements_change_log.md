# RiftCoach 需求与约束变更账本

本文档保存会影响后续多轮开发的长期要求。采用追加式记录：新要求可以补充或
显式取代旧要求，但不得静默改写历史。产品路线细节仍以 `docs/roadmap.md` 为准。

| ID | 日期 | 状态 | 长期要求 | 对执行方式的影响 |
|---|---|---|---|---|
| RQ-001 | 2026-08-01 | 生效 | 主路线固定为阶段 0-8，共九个阶段 | 未经用户批准和 ADR，不得增删、合并、重排或改名 |
| RQ-002 | 2026-08-01 | 生效 | 开发必须稳扎稳打，一次推进一个可理解、可验证的小子阶段 | “继续”默认只授权当前状态文件中的唯一下一步 |
| RQ-003 | 2026-08-01 | 生效 | 按纯 Agent 初学者水平讲清问题、原理、实现、数据流、测试和边界 | 编码可以由 Codex 完成，但不能只报告文件和测试数字 |
| RQ-004 | 2026-08-01 | 生效 | 用户需要真正理解 Agent 的底层逻辑，以便面试和简历陈述 | 每个子阶段都要留下教学说明和可验证证据，避免无法解释的技术名词 |
| RQ-005 | 2026-08-01 | 生效 | EchoMind、AGI-Saber、Sea/OpenResearch 等是选择性来源，不是必须照搬的底座 | 每项吸收必须对应 RiftCoach 的真实需求、边界和测试 |
| RQ-006 | 2026-08-01 | 生效 | 不盲目堆叠 SDK、LangGraph、Multi-Agent、向量数据库等技术 | 先记录 Bad Case、备选方案、收益与成本，再通过 Eval 和 ADR 决定 |
| RQ-007 | 2026-08-05 | 已完成，当前状态见 RQ-024 | Skill 先建立一个真实样板，稳定后再扩展首批其他 Skill | 实施顺序已完成；当前真实用户 Skill 为 `recent-form-review` 与 `single-match-review`，不用假 Skill 冒充业务完成度 |
| RQ-008 | 2026-07-22 | 生效 | GitHub 开源和部署是横向交付检查点，不替换 0-8 主路线 | MIT、CI、README 等按成熟度维护；Web/API 在对应能力具备后上线 |
| RQ-009 | 2026-08-06 | 生效 | 不得把多个已规划子阶段静默压缩成一个实现批次并宣称完成 | 即使代码提前覆盖后续内容，也必须回到原检查点逐项复核和确认 |
| RQ-010 | 2026-08-06 | 生效 | 必须用仓库文件维持跨上下文连续性 | 每次接受子阶段或长期需求变化后，更新状态、活动计划、进度和冲突文档 |
| RQ-011 | 2026-08-06 | 生效 | Prompt Engineering、Eval、Observability、安全和成本等基础能力不能等用户偶然提到才补 | 每个子阶段开始和结束时交叉检查能力矩阵，不凭对话临时想起 |
| RQ-012 | 2026-08-06 | 生效 | 对当前完成度必须客观，不把小开发集、合成测试或设计意向说成生产能力 | 状态文档必须分别写明实现、证据、限制和未完成项 |
| RQ-013 | 2026-08-01 | 生效 | 用户观点、PDF、网页端 GPT 建议和参考项目都是待分析证据，不是自动需求 | 标注提案、确认、撤回和条件采用；最新明确纠正优先，不能照单全收 |
| RQ-014 | 2026-08-01 | 生效 | 项目必须分开维护代码进度、理解进度、参考资料审计进度和简历/公开交付成熟度 | 不得以测试通过代替用户理解，也不得以本地实现冒充 GitHub 或部署已同步 |
| RQ-015 | 2026-08-04 | 生效 | GLM 是当前唯一真实 Provider 基线；DeepSeek、Qwen 等是未锁定候选 | 显式 Provider 选择、任务级自动模型路由和 Multi-Agent 分开设计；只有同任务同评测后才选第二 Provider |
| RQ-016 | 2026-08-02 | 生效 | Pi、Claude Agent SDK 等属于条件性 Runtime 采用实验，不是项目默认底座 | 阶段 5F 用真实切片、对照、成本和 ADR 决定采用、局部采用或拒绝；它们不能替代 Harness、Tool Runtime 和领域核心 |
| RQ-017 | 2026-08-05 | 生效 | Prompt/Context Engineering 跨阶段演进，并有明确 V1 落点 | 阶段 2 Prompt V0；5B Skill 指令；5D Context Assembly/结构化输出/不可信边界；5E 版本、Trace、Usage、预算；6-8 继续加入 Memory、Meta、Compaction 和隔离上下文 |
| RQ-018 | 2026-08-05 | 由 RQ-024 取代 | 首批业务目标是 `recent-form-review`、`single-match-review`、`report-fact-check`；一个样板先行不等于缩减为一个 | 历史三 Skill 分类经源码审计修正；保留两个领域 Skill，事实审查继续由已有 Harness Evaluator 承担 |
| RQ-019 | 2026-08-05 | 生效 | 后续阶段可以通过稳定契约深化前面的 V1 能力，但不能借“后续完善”掩盖当前未完成 | 每次深化都记录消费者、接口变化、回归证据和能力边界，不整层推翻重写 |
| RQ-020 | 2026-08-04 | 生效 | 3G-4 及后续真实 Provider 工作被延后，不是取消 | 等真实 Skill/Agent 任务形成后，以领域评测重新触发；近期路线不得擅自插回连续 Provider 接入 |
| RQ-021 | 2026-08-06 | 生效 | 1198 页完整 GPT 导出用于定向查漏；后续专项导出和本任务确认记录更适合判定当前路线 | 不按文件更新时间或篇幅判断权威性；完整历史中的旧方案必须结合后续纠正标注状态 |
| RQ-022 | 2026-08-06 | 生效 | 上下文连续性不能只依赖模型记忆或人工自觉读取文档 | 当前执行状态提供机器可读元数据；每轮恢复和子阶段收尾运行治理预检，冲突时停止功能开发 |
| RQ-023 | 2026-08-06 | 由 RQ-024 取代 | 首批三个真实 Skill 均保留，但用户任务与内部质量步骤必须分开调用 | 该方案在功能代码开始前经源码审计修正；事实审查已有完整 Harness Evaluator，不再重复包装为内部 Skill |
| RQ-024 | 2026-08-06 | 生效 | Skill 数量由独立工作流价值决定，不为维持历史数字复制已有 Harness 能力 | 首批 Skill 修正为 `recent-form-review` 与 `single-match-review`；事实审查保留为强制 `EvaluatorStep`，取消未实现的 invocation mode 与 `report-fact-check` Skill；未来只有真实独立用例和 Bad Case 才重新评估内部 Skill |
| RQ-025 | 2026-08-06 | 生效 | Router 评测必须区分历史基线、可校准 development 与规则冻结后的 independent holdout；holdout 失败不得反向调规则 | 数据集强制声明角色、污染记录、案例数量和候选 Skill 版本快照；默认 CLI 拒绝 holdout，污染后必须退休并升级版本 |
| RQ-026 | 2026-08-07 | 生效 | 5C V1 暂缓 LLM Router fallback；一个小型合成语义 Bad Case 不足以引入模型调用 | 保留确定性 Router 与冻结规则；优先类型化入口和澄清。只有新鲜数据出现多个独立失败族、Provider 结构化输出与质量/延迟/成本/故障评测通过后，才用新 ADR 重新评估 |
| RQ-027 | 2026-08-12 | 生效 | 用户授权 5D-6b 内后续有明确实验目的、脚本硬预算和脱敏边界的真实 Provider 测试不必逐次询问，并要求完整验证 | 只覆盖当前已批准检查点内的有界测试，不等于无限调用、盲目重试、扩大厂商数量或越过阶段；每次仍须先离线设计/TDD、设置调用上限、失败即停并记录成本与证据 |
| RQ-028 | 2026-08-13 | 生效 | 保持稳扎稳打，但在不跨检查点和安全边界时可以一次完成更大的连贯能力切片 | 每轮优先交付可运行、可测试、可讲解的完整纵向切片，而不是人为拆成过小函数；扩大批次不得越过 canonical 唯一下一步、跳过 TDD/CI 或混入第二 Provider、领域 Skill 等未授权范围 |
| RQ-029 | 2026-08-14 | 已执行，由 RQ-034 收口 | D5 唯一第二 Provider 候选应选 DeepSeek V4 Pro 正式版，而不是仅因便宜选择 Flash | ADR-0018 取代 ADR-0017 的候选模型与金额停止线；协议门和领域门均绑定 `deepseek-v4-pro`，DeepSeek 停止线为 `$0.10`；Flash 只保留为以后成本/时延分层候选，本次更正不授权真实调用或 held-out |
| RQ-030 | 2026-08-14 | 模型分层归属生效；5F 候选部分由 RQ-047 取代，当前模型候选由 RQ-034 收口 | DeepSeek V4 Pro 曾作为 5D-7 单候选；当前 V3 已关闭，但 Flash/Pro 分层仍不放入 5F，最早在 5P 后、默认在阶段 6 有真实成本/时延证据后重开 | 5F 的候选范围改由 RQ-047 收缩为 Pi-only；未来仍以全新同任务评测比较 Pro-only、Flash-only 与 Flash 默认/Pro 有界升级，证据不足时保持单模型；ADR-0019 的模型分层归属、旧 Pro 协议与领域结果保持只读 |
| RQ-031 | 2026-08-15 | 迁移顺序生效；立即下一步由 RQ-035/RQ-036 取代 | GLM-5.3 已有官方模型文档，允许作为新的同厂商模型迁移候选，但不得直接替换 GLM-5.2 | API 可用后仍按 G53-0 可用性/合同审计、G53-1 Zhipu thinking profile 离线 TDD、G53-2 公开 CI、G53-3 最多 3-call 协议门、G53-4 新 Dataset/输入计划领域门推进；旧 GLM-5.2、DeepSeek Adapter、结果和 held-out 只读隔离，不覆盖、不重跑；通过领域门前不改默认模型、不实现自动路由。 |
| RQ-032 | 2026-08-15 | 资源合同原则生效；DeepSeek V3 由 RQ-034 关闭 | 任何新的真实领域 Provider 门在读取 Key 或构造 Provider 前，必须证明资源合同能到达必需的 Agent 工具往返与独立 Evaluation；真实 Usage、tokenizer-free 长度投影和未知值必须分层表达 | DeepSeek V2/V3 结果保持不可变，长度投影不得冒充官方 Token 或继续生成 V3 预算；该原则改由未来全新 Provider 实验继承，并与 RQ-034 的安全错误 provenance 前置条件同时满足。 |
| RQ-033 | 2026-08-15 | 已执行（首错停止） | 用户明确确认执行一次真实 DeepSeek V4 Pro development Usage 校准，固定为 2 profiles × 4 stages、最多 8 calls、每请求 output 64、64000 observed tokens、`$0.10`、零重试和首错停止 | 真实入口先通过 `6aa8c43` / Actions `31868747216`，同 SHA prepare-only 为零调用；正式 replay 在第 1 次请求未形成规范化 `ChatResponse` 后以 `provider_response_invalid` 停止，后 7 次未发送。结果不可覆盖或补跑；实际 Usage/费用 unknown，不创建预算/V3 held-out，不据此判断模型质量。 |
| RQ-034 | 2026-08-15 | 关闭与安全前置条件生效；立即下一步由 RQ-036 取代 | 当前 DeepSeek V3 资源校准与领域采用尝试关闭；保留低层协议事实，但不准入领域质量或产品默认模型 | 不生成 budget/held-out，不补跑 V1/V2/calibration；未来任何真实 Provider 门必须先离线实现跨厂商 `failure_code` 与允许列表安全 `provider_error_code` 双层 provenance，禁止原始响应/异常落盘。G53-0 仍是未来迁移入口，但不再阻塞 5D-7 收尾。 |
| RQ-035 | 2026-08-15 | 生效 | GLM-5.3 普通 API 尚未正式可用；DeepSeek Pro 当前尝试保持关闭，不立即切换 Flash；GLM-5.2 继续作为开发基线 | 将 G53-0 标为 deferred，不读取 Key、不调用未上线模型；先完成 RQ-034 要求的安全错误 provenance 离线合同和公开验证。未来只有明确成本/延迟或同任务对照需求、且基础设施先修复后，才重新设计 Pro/Flash 对照实验。 |
| RQ-036 | 2026-08-15 | 生效 | 用户确认不让 GLM-5.3 外部发布时间阻塞项目，先执行 5D-7 review；模型采用可以诚实地以 reject/unknown 收尾 | ADR-0028 将 5D-7 的退出标准固定为评测、实验控制和采用决策能力，而不是强制一个 Provider 通过。当前无领域 Provider 准入；G53 deferred、Flash/Pro 分层和旧结果边界保持不变。5D-7 review 通过后唯一下一检查点为 `5D-exit-review`，不自动进入 5E。 |
| RQ-037 | 2026-08-17 | 本次顺序授权生效 | 用户明确授权：当前 5E-2 Task D 完成并通过公开验证后，无需再次等待“继续”，直接进入唯一下一检查点 5E-3 | 该授权只跨越当前完成门进入 5E-3，不授权静默合并 5E-3/5E-4、跳入 5P/5F、调用真实 Provider 或改变技术采用门；进入 5E-3 后仍按教学、TDD、持久状态和独立验收执行。 |
| RQ-038 | 2026-08-17 | 本次顺序授权生效 | 用户进一步明确：进入后续唯一子阶段并完成时，同样可以直接继续下一个 canonical 检查点，无需重复询问 | 每次只自动推进一个已完成当前阶段后唯一明确的下一子阶段；仍须先更新持久状态、讲解原理、执行 TDD/门禁和公开验证，绝不跨越未完成的决策门或合并多个子阶段。 |
| RQ-039 | 2026-08-17 | 当前暂停要求生效，覆盖 RQ-038 的自动继续 | 用户要求完成本轮 5E-4 验证与提交后结束 | 完成 5E-4 exact-SHA 公共闭环和必要状态回写后停止；只把 canonical 交接到 `5P-entry-design`，不开展 5P 设计、代码、Provider I/O 或后续子阶段，等待用户再次明确“继续”。 |
| RQ-040 | 2026-08-17 | 生效，解除 RQ-039 的当前暂停 | 用户再次明确“继续下一步”，只恢复 canonical 的 `5P-entry-design` | 先完整审计并设计 5P Prompt Program V1 与早期产品切片；本轮不自动实现 FastAPI、不读取 Key、不调用 Riot/Provider、不进入 5F。设计、治理和 exact-SHA 公共验证完成后，唯一下一步为 5P-1 typed product/compiler。 |
| RQ-041 | 2026-08-17 | 已执行 | 用户再次明确“继续”，只授权 canonical 的 `5P-1-product-contract-compiler` | 完成严格产品 DTO、typed Skill selection、Artifact binding 与 Manifest-derived Runtime policy 的教学、TDD 和公开验证；不实现 Prompt Program/FastAPI、不读取 Key、不调用 Riot/Provider。5P-1 闭环后只交接到 5P-2，等待下一次明确继续。 |
| RQ-042 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5P-2-prompt-program-runtime-composition` | 只实现版本化 Prompt Program、component fingerprint/drift gate、verified Runtime identity 与薄 composition root；不安装 FastAPI、不实现 Application Service、不读取 Key、不调用 Riot/Provider、不进入 5P-3 或 5F。完成本轮后必须同步持久状态并等待 exact-SHA 公共验证结果。 |
| RQ-043 | 2026-08-17 | 已执行 | 用户再次明确“继续下一步”，授权 canonical 的 `5P-3-domain-application-service` | 只提升 Summary/Report domain services、建立 RecentReviewApplicationService 与安全错误映射，并允许首个正式消费者对 5P-2 secure execution factory 做窄幅向后深化；不安装 FastAPI、不实现 receipt/query、不读取 Key、不调用 Riot/Provider、不进入 5P-4 或 5F。 |
| RQ-044 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5P-4-file-backed-run-receipt-query` | 只实现 body-free immutable receipt、文件 Store、严格 RunQueryService、Trace/manifest/final Artifact 交叉校验与 Application Service receipt 接缝；不安装 FastAPI、不实现 HTTP、SQL/Session/Memory、恢复扫描或 5F。完成本轮后只交接到 5P-5，等待下一次明确继续。 |
| RQ-045 | 2026-08-17 | 已执行 | 用户明确“继续5P-5”，授权 canonical 的 `5P-5-thin-fastapi-adapter-no-io-vertical-slice` | 只实现薄 FastAPI HTTP Adapter 与 Fake/fixture 本地无 I/O 纵向切片：POST recent、GET run、GET report、GET health、严格 OpenAPI/错误映射和 TestClient 门禁；不读取 Key、不调用 Riot/Provider，不实现 SQL/Session/Memory/SSE/后台任务/公网部署/5P-6/5F。 |
| RQ-046 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5P-6-product-slice-evaluation-exit-review` | 只审查 5P entry 与 5P-1 至 5P-5 的功能/NFR/安全/资源/公开证据，建立 exit matrix、必要的当前范围内最小修补和退出裁决；不读取 Key、不调用 Riot/Provider，不实现 5F、阶段 6、SQL/Session/Memory/SSE/鉴权/前端。 |
| RQ-047 | 2026-08-17 | 已执行 | 用户确认继续并接受 `Pi-only`，把 5F 的第三方 Runtime 候选收缩为 Pi | 5F 只设计和实测官方 Pi Runtime；Claude Agent SDK 只保留书面替代/排除分析，不安装、不调用、不进入代码级对照；先做无 I/O 同切片协议实验，再由 ADR 决定 adopt、partial-adopt 或 reject；不改变默认 Runtime、不读取 Key、不调用真实 Provider。入口设计已由 `ce97975` / Actions `32013948784` 公共验证。 |
| RQ-048 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5F-1-pi-source-license-contract-audit` | 只读审计官方 Pi release/source/package identity、MIT license、Node/runtime requirements 以及 Agent/Provider/Tool/event/state/abort/timeout/Usage 接缝，形成是否允许 5F-2 的证据裁决；未安装 Pi、写 adapter、读取 Key、调用 Provider 或改变主 Runtime。审计已由 `5901b09` / Actions `32016852979` 公共验证，canonical 只交接到 5F-2 准备状态。 |
| RQ-049 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5F-2-offline-protocol-adapter-spike` | 官方 Pi Agent Core 0.84.2 的隔离 no-I/O 协议实验已由 `f62f078` / Actions `32022258177` 完成 exact-SHA 公共验证；状态收尾又由 `1454f59` / Actions `32022784855` 公共验证。未读取 Key、调用 Provider/Riot、接入主 Runtime/Harness/FastAPI 或进入 5F-3。 |
| RQ-050 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5F-3-contract-security-harness-evaluation` | 评测专用 Pi→Harness adapter、strict Signal projector、完整 parity/安全/NFR/维护成本退出矩阵已由 `3d9a081` / Actions `32025522606` 完成 exact-SHA 公共验证；裁决 `harness-compatible-but-runtime-gate-failed`，Context/extended terminal/live timing 硬门失败，5F-4 未进入。未读取 Key、调用真实 Provider/Riot或接入主 Runtime/FastAPI/default composition；最终采用裁决仍属于 5F-5。 |
| RQ-051 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `5F-5-adoption-decision-exit-review` | ADR-0037/exit matrix/review 已裁决 `partial-adopt-evaluation-assets-only`：产品拒绝 Pi，冻结保留可执行评测资产与采用门方法；`f8dea66` / Actions `32028206103` 完成 exact-SHA 公共验证。5F 正式关闭，只交接 `6A-entry-design`，未补做 5F-4、读取 Key、调用 Provider/Riot、接主 Runtime/FastAPI 或实现阶段 6。 |
| RQ-052 | 2026-08-17 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `6A-entry-design`；随后选择数据库方案 A、任务执行方案 3、hard-crash 方案 A，并逐节确认全部设计与 6A-1 至 6A-7 顺序 | ADR-0038、正式 design/implementation plan、本地 `929 passed/110 subtests` 与全部门禁已由 `c0b5af0` / Actions `32041343696` exact-SHA 公共验证；entry design 正式关闭，只交接 6A-1 准备状态。本轮未实现 SQL/Session/Memory/SSE/Auth/前端、未启动 DB 或调用外部 I/O。 |
| RQ-053 | 2026-08-17 | 已执行 | 用户明确“开始”，授权 canonical 的 `6A-1-postgresql-foundation` | SQLAlchemy 2/Alembic/psycopg 配置、task ORM row、initial migration、Compose 与真实 PostgreSQL CI 门已由 `854e52d` / Actions `32043214500` exact-SHA 公共验证；两个 job 均成功。未实现 Repository、claim、Worker、异步 API、Session/Memory/SSE/前端或外部 Riot/Provider I/O。 |
| RQ-054 | 2026-08-18 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `6A-2-task-contract-repository` | Provider-neutral task contract、canonical fingerprint、owner-scoped idempotent create/query、容量语义与 PostgreSQL Repository 已由 `012b066` / Actions `32046532695` exact-SHA 公共验证；`pytest` 与真实 PostgreSQL task job 均成功。未实现 claim、Worker、Application/Artifact 集成、异步 API、Session/Memory/SSE/前端或外部 Riot/Provider I/O。 |
| RQ-055 | 2026-08-18 | 已执行 | 用户再次明确“继续下一轮”，授权 canonical 的 `6A-3-atomic-claim-polling-worker` | PostgreSQL `FOR UPDATE SKIP LOCKED` 原子 claim、deterministic order、worker ownership/terminal CAS、polling backoff/jitter、graceful shutdown 与 Fake Executor Worker 控制流已由 `55e369e` / Actions `32097561436` exact-SHA 公共验证；真实 PostgreSQL job 成功。未接真实 Application/Artifact、FastAPI、lease/heartbeat/retry/cancel、Session/Memory、Riot 或 Provider。 |
| RQ-056 | 2026-08-18 | 已执行 | 用户再次明确“继续”，授权 canonical 的 `6A-4-application-artifact-integration` | SQL 预留 `run_id` 贯穿 Application/Runtime/receipt、真实 Recent Review Task Executor、receipt-proven terminal coordination、保守 reconciliation 与受限人工 recovery CAS 已由提交 `41ac9c1` / Actions `32102522662` exact-SHA 公共验证；`pytest` 与真实 PostgreSQL job 均成功（PostgreSQL 测试 `40 passed`）。不实现 6A-5 异步 FastAPI、lease/heartbeat/retry/reclaim、Session/Memory/SSE/前端，也未调用真实 Riot/Provider；唯一下一步为 6A-5 准备状态。 |
| RQ-057 | 2026-08-18 | 已执行 | 用户再次明确“继续吧”，授权 canonical 的 `6A-5-async-fastapi-composition` | POST 202 task receipt、owner-scoped task/run/report、可信 ActorContext、FastAPI lifespan、PostgreSQL/Alembic readiness 与 production-like API composition 已由提交 `2492951` / Actions `32106378542` exact-SHA 公共验证；`pytest` 为 `1047 passed, 21 skipped, 110 subtests passed`，真实 PostgreSQL job 为 `41 passed`。未实现 JWT/OAuth、Session/Memory、SSE、前端、lease/retry/reclaim 或 6A-6，也未调用 Riot/Provider；只交接 6A-6 准备状态。 |
| RQ-058 | 2026-08-18 | 已执行 | 用户明确“继续下一步”，解除 `6A-6-security-lifecycle-nfr` 的等待确认 | 默认关闭 CORS、日志/Secret allowlist、7/90/30 天 retention、terminal hidden-before-cleanup 与补偿、active delete conflict、owner/global capacity、结构化 observability 和真实 PostgreSQL 性能基线已由 `31d5e60` / Actions `32138025724` exact-SHA 公共验证；完整 pytest `1077 passed, 27 skipped, 110 subtests passed`，真库 `51 passed`，8 样本 create/query p95 `6.220ms`、queued→claim p95 `23.359ms`。未实现正式 Auth/HTTPS、Session/Memory、SSE、前端、lease/reclaim/cancel/resume 或真实 Provider/Riot I/O；只交接 6A-7 准备状态。 |
| RQ-059 | 2026-08-18 | 已执行 | 用户再次明确“继续吧”，授权 canonical 的 `6A-7-packaging-exit-review` | FastAPI API + PostgreSQL + polling Worker package、claim 前 composition、隔离 no-I/O Linux smoke 与 6A exit review 已由 `adf53e5` / Actions `32146760003` 三 job exact-SHA 公共验证；smoke 外部 Riot/Provider calls 为 0。6A 正式关闭，只交接 `stage-6-session-memory-entry-design` 准备状态；正式 Auth/HTTPS、Session/Memory、SSE、前端、lease/reclaim/cancel/resume、直接公网部署及新框架仍未实现。 |
| RQ-060 | 2026-08-19 | 设计授权已由后续 RQ-064 扩展 | 用户明确“继下一步”，授权 canonical 的 `stage-6-session-memory-entry-design` | 本检查点只做 Session/Memory 初学者教学、现有 owner/task/run/API 与参考源码审计、2—3 种方案比较，以及数据模型、写入门、来源/置信度、冲突/更正/导出/过期/删除、隔离、隐私/NFR/测试和原子实施顺序的设计冻结。该条当时要求设计后再次授权；后续 RQ-064 仅把自动执行范围扩展为 entry design→6B-1→6B-2。不引入 Redis/Chroma/向量库/LangGraph 等新依赖，不提前实现正式 Auth/HTTPS、SSE/前端、阶段 7 MCP、阶段 8 Multi-Agent/恢复，也不读取 Key 或调用 Riot/Provider。 |
| RQ-061 | 2026-08-19 | 生效；关系策略随后由 RQ-062 确认 | 用户明确提醒：RiftCoach 当前不能查询中国大陆国服 API，只能处理 Riot 官方 API 可路由的外服账号；用户把一个外服 Riot ID 选作“自己的账号”至多是用户声明 | Account-V1 的 Riot ID→PUUID 只能证明公开账号存在，不能证明当前 RiftCoach owner 控制该账号。V1 必须把这种关系标记为未验证的 self claim，禁止显示“已验证本人”、禁止借此获得非公开数据，并让玩家相关的私人 Session/Memory 按 `owner_id × player_subject_id` 隔离；owner-global 偏好仍只按 owner 隔离。未来只有“正式 RiftCoach 用户认证 + 安全绑定到该 owner 的 RSO OAuth/OIDC 回调 + `/accounts/me` 返回 PUUID 与 subject 精确匹配”才可能升级为已验证授权关系；是否同时提供 `public_observed` 模式及其具体能力，仍在当前身份模型设计节确认，不提前实现。 |
| RQ-062 | 2026-08-19 | 设计决策生效；当时未授权实现，后续范围见 RQ-064 | 用户确认推荐的外服玩家关系策略：MVP 同时支持未验证自我认领与受限公开观察，verified 留给未来正式 Auth + RSO | 底层分开 `relationship_role=self|observed` 与 `verification_status=unverified_claim|not_applicable|rso_verified`：`claimed_self=self+unverified_claim` 可保存 owner-player 目标/计划/进度但必须显式未验证；`public_observed=observed+not_applicable` 只允许公开比赛分析、owner-local 观察备注/趋势，不生成被观察者本人的私人偏好或第一人称训练完成度；`verified_self=self+rso_verified` 当前没有创建路径。任一关系都不增加 Riot 数据权限、不跨 owner 合并私人 Memory。下一设计门仍需单独确认 conversation 绑定/切换规则；本确认当时不授权代码，后续只由 RQ-064 授权的 6B-1/6B-2 范围实施。 |
| RQ-063 | 2026-08-19 | 设计决策生效；当时未授权实现，后续范围见 RQ-064 | 用户确认 V1 conversation 采用方案 A：创建时固定一个玩家，不同 PUUID 必须新建 conversation，显式中途切换留待真实需求 | Conversation 必须属于 trusted owner 并在创建时引用该 owner 的一个有效 player subject/relationship，生命周期内 subject 不可变且无切换 endpoint；相同 PUUID 的 Riot ID 改名可继续，不同 PUUID 返回安全 mismatch 并要求新建 conversation。消息、Context、复盘 task/run 和 Memory Candidate 均继承服务器保存的 owner/conversation/subject，客户端正文、自由文本或模型不得覆盖。未来实现应用层校验与 PostgreSQL owner-scoped composite FK/约束，并测试跨 owner、跨 subject、迟到 task 和改名/重指向；本确认当时不授权代码，后续 RQ-064 仍只自动实施至 6B-2，Conversation 实现 6B-3 不在自动范围。 |
| RQ-064 | 2026-08-19 | entry design 已执行；自动继续 6B-2 的部分被 RQ-065 收紧 | 用户明确授权本轮由 Codex 在既有边界内选择最佳剩余设计方案，无需逐项等待审批；完整说明决策后开始正式执行，第一步完成验证和推送后自动继续第二步 | 原自动执行链为 `entry design → 6B-1 → 6B-2`；entry design 已由 `bc11afe` / Actions `32222531783` 三 job exact-SHA 公共完成。当时 6B-1 仍获授权，但其公共闭环后自动进入 6B-2 的部分已由较晚的 RQ-065 取消；6B-1 现已按 RQ-065 闭环。其余技术与安全边界继续生效。 |
| RQ-065 | 2026-08-19 | 已执行 | 用户把本轮目标明确收紧为“完成 6B-1 就截止，下一轮再继续” | `6B-1-player-identity-link-foundation` 已由最终提交 `ed8fa58` / Actions `32229024069` 的 pytest、真实 PostgreSQL 与 Linux package 三 job exact-SHA 公共闭环；当前已停止，并把 `6B-2-async-player-link-worker-api` 标为 prepared/waiting authorization。本轮未实现 6B-2。 |
| RQ-066 | 2026-08-19 | 已执行；在 6B-3 前停止 | 用户在 6B-1 独立公共闭环后的新一轮明确“继续开工”，授权 canonical 的 `6B-2-async-player-link-worker-api` | 窄 Riot Account Resolver、专用 PlayerLinkWorker、owner-scoped POST/GET Link API、composition/CLI 与 Fake Resolver Linux no-I/O smoke 已由 `0c13a58` / Actions `32301852042` 三 job exact-SHA 公共闭环；开发、测试和 CI 未读取真实 Key 或调用 Riot/Provider。当前只准备 6B-3 并停止，未实现 Conversation/Memory。 |
| RQ-067 | 2026-08-20 | 已执行；文档门由 `63435d9` / Actions `32308631289` 公共闭环；6B-3 已进入 | 用户要求重新确认教学与工程说明缺口是否确实从 6B 才开始，并从最早真实缺口补齐此前列出的全部持久材料；补齐并验证后无需再次确认，直接进入 6B-3 | 先做覆盖矩阵驱动的只读审计，不以文件数量或聊天中的“已讲过”判定完成；对真实缺口补设计/实现复盘、初学者心智模型、实际代码地图、数据/控制流、事务/失败/安全边界、需求→源码→测试→CI→限制证据矩阵、运行示例和面试安全表述，同时更新 README、学习索引及 AGENTS/治理门。文档批已独立通过治理、比例回归、提交/推送和 exact-SHA 公共 CI；6B-3 现按教学→设计复核→TDD→实现→实现后复盘→公共验证推进。 |
| RQ-068 | 2026-08-20 | 已执行 | 用户明确“继续 6B-4”，授权 canonical 的 `6B-4-conversation-bound-recent-review-identity` | Review Task schema 2.0、Conversation-bound API、服务器派生 identity、可信 PUUID Summary path、legacy 1.0 兼容与 existing Runtime/Harness no-I/O 纵向已由 `d63f908` / Actions `32347834279` 三 job exact-SHA 公共闭环；本批未进入 Memory。 |
| RQ-069 | 2026-08-20 | 已执行；6B-5 由 `dd7c9c8` / Actions `32376405150` 公共闭环 | 用户明确“继续 6B-5”，授权 canonical 的 `6B-5-memory-candidate-write-gate` | Candidate source/target/provenance/gate、PostgreSQL 0005/Repository、四态终态、安全 DTO 与事务内 typed materializer 接缝已完成；真实 PostgreSQL `126 passed`，package Candidate pending→reject 且外部调用为 0。具体 Preference/Profile/Review Memory/Plan/Progress 仍留给 6B-6/6B-7；生产 composition 在真实 materializer 注册前继续 fail closed。不实现 assistant terminal、Memory-aware Context、正式 Auth/RSO、SSE、前端、LangGraph、Multi-Agent 或新 SDK，不读取 Key，不调用 Riot/Provider。 |
| RQ-070 | 2026-08-20 | 已执行；6B-6 由 `5531c81` / Actions `32387026797` 公共闭环 | 开始 `6B-6-preferences-profile-review-memory` 的设计批，并在设计冻结后按 TDD 实现三类 typed target、真实 materializer、版本冲突保护与 owner-scoped 查询 | 三张 typed PostgreSQL 表、严格 Candidate envelope、self/observed 权限、版本链、事务内 materialization 和只读查询已完成；公共 pytest `1402 passed/100 skipped`、真实 PostgreSQL `142 passed`、Linux package accept→Preference query 且外部调用为 0。Training Plan/Progress、Memory Context、assistant terminal、Auth/RSO、SSE、前端、Redis/Chroma/向量库、LangGraph、Multi-Agent、新 SDK 与真实 Riot/Provider 调用继续 deferred。 |
| RQ-071 | 2026-08-21 | 已执行；6B-7/8/9 均已 exact-SHA 公共闭环 | 用户明确要求本轮一次性连续完成 6B-7、6B-8、6B-9，不再逐步骤等待批准，同时每一步仍须稳步完整、最终详尽复盘 | 三项依次由 `f6d8922` / Actions `32397290175`、`aacc11a` / Actions `32403187972`、`cbc7cbd` / Actions `32408101770` 完成独立教学、设计、TDD、实现、八维证据、本地门禁和三 job 公共闭环。6B-9 公共 pytest `1490 passed, 116 skipped, 1 warning, 110 subtests passed`，真实 PostgreSQL `164 passed, 1 warning`，Linux package schema 1.6 验证 export→conversation-only delete 与 Preference/Plan 存续，外部调用 0。RQ-071 不授权阶段 7 MCP、真实 Riot/Provider、正式 Auth/RSO、SSE/前端、Multi-Agent 或新基础设施。 |
| RQ-072 | 2026-08-21 | 生效；仅授权当前入口设计检查点 | 用户明确“那开始 stage7”，授权 canonical 的 `stage-7-standard-mcp-dynamic-meta-entry-design` | 只推进 Stage 7 入口教学、现有接缝审计、标准 MCP/动态 Meta 方案比较、OP.GG 条件准入清单、ADR/design/implementation plan 和持久状态同步；本检查点不安装 SDK、不实现 MCP Client/Server、不调用 OP.GG/Riot/Provider、不读取 Key、不把普通 HTTP 称为 MCP。入口设计公共闭环后才进入 `7-1-mcp-client-contract`，仍须逐检查点 TDD、八维证据、本地门禁、独立提交与 exact-SHA 公共 CI。 |
| RQ-073 | 2026-08-21 | 已执行；7-1 由 `37f16bc` / Actions `32439753589` 公共闭环 | 用户在 Stage 7 入口设计公共闭环后明确“继续下一步”，授权 canonical 的 `7-1-mcp-client-contract` | 本检查点只以 pure TDD 实现标准 MCP 的 initialize、capability、tools/list、tools/call、schema snapshot/drift、大小限制和 body-free 安全错误合同；不安装 MCP SDK、不实现 stdio/HTTP transport、不调用 OP.GG/Riot/Provider、不读取 Key，也不提前实现 MetaEvidence、RiftCoach MCP Server 或真实互操作。八维证据、本地门禁与 exact-SHA 三 job 已完成；当前只交接 7-2 prepared/waiting authorization。 |
| RQ-074 | 2026-08-21 | 生效；授权当前唯一检查点 | 用户明确“继续7-2”，授权 canonical 的 `7-2-mcp-transport-and-discovery` | 本检查点在已闭环的 7-1 pure contract 之上，以 TDD 实现 fixture/in-memory 与隔离 stdio/subprocess transport、session 生命周期、deadline/disconnect/restart/capability gate 和动态 tools/list discovery；发现的 descriptor 只映射到既有 `ToolDefinition` 并交给 `ToolRuntime`，Adapter 不复制 retry/cache/breaker/fallback。当前不安装 MCP SDK，不实现普通 HTTP/Streamable HTTP（除非标准与部署证据明确）、不接 OP.GG/Riot/Provider、不读取 Key、不实现 MetaEvidence、RiftCoach MCP Server 或真实外部互操作；fixture/subprocess 只证明本地合同。 |
| RQ-075 | 2026-08-21 | 已执行；7-3 由 `64311a1` / Actions `32455219404` 公共闭环；准入解释由 RQ-076 修正 | 用户在确认 `opgginc/opgg-mcp` 是所指官方候选后明确“继续正常继续你的下一步”，授权 canonical 的 `7-3-opgg-meta-adapter` | 先完成官方 endpoint/protocol/transport/tool-schema/license/freshness/rate-limit/deployment 准入审计；证据通过后按 TDD 实现 bounded/data-only `MetaEvidence`、严格 OP.GG per-tool normalization 与既有 Context trust boundary。该授权不外推 7-4/7-5，不读取 Key，不调用 Riot/LLM Provider，不写 Memory/Candidate/Plan/Progress；候选审计探针不冒充双向 exact-SHA 互操作退出证据。 |
| RQ-076 | 2026-08-21 | 生效；修正 7-3 准入语义 | 用户质疑把 OP.GG 缺少完整 patch/TTL/outputSchema 直接解释为“不接”，强调该 MCP Server 很有价值并要求正常接入 | 将“完全准入/完全拒绝”改为分级 provenance：真实接入官方 Streamable HTTP MCP，固定并锁定只读 lane-meta 工具与字段，用无 `eval` 的安全 grammar 规范化；本地记录 retrieval TTL，同时明确 `upstream_patch/source_freshness=unknown`。该证据允许 current snapshot recommendation，但禁止 exact patch attribution、historical patch comparison 和 upstream freshness claim；继续不写 Memory、不覆盖玩家事实、不绕过 Harness。 |
| RQ-077 | 2026-08-21 | 生效；作为后续 Riot + OP.GG 证据融合边界 | 用户明确指出 Riot 官方能力不只包含对局数据，还包括版本更新等多类数据，并确认 OP.GG 应与 Riot 官方来源结合 | 后续证据融合必须把 Riot Developer API 的账号/排位/比赛等事实、Data Dragon 的版本化静态定义和官方 patch/game update 内容，与 OP.GG 聚合 Meta 分层建模；OP.GG 不能覆盖 Riot 官方事实，缺 patch 的 OP.GG 快照也不能借用同日 Riot patch 冒充精确绑定。7-3 只建立 OP.GG partial `MetaEvidence` 与 Context 接缝，不提前调用 Riot 或实现两源 join；后续按 champion/position/region/queue/patch 等明确键组合。 |
| RQ-078 | 2026-08-21 | 已执行；7-4 由 `431c584` / Actions `32480827952` 公共闭环 | 用户明确“ok，那你继续完完整整开发”，在 7-3 exact-SHA 公共闭环后授权继续 `7-4-riftcoach-mcp-server` | 只实现 RiftCoach 对外 MCP Server 的 strict protocol/session、owner-scoped read-only Application Facade、四个受限工具、body-free 错误/响应和 fixture TDD；不接受 owner_id/PUUID/Key/Prompt/Provider body，不直连 Repository，不实现公网 transport、真实外部 Client 或 7-5 双向互操作。 |
| RQ-079 | 2026-08-21 | 已执行；7-5 与 Stage 7 由 `fac6fe0` / Actions `32484257736` 最终公共闭环 | 用户明确“那继续7-5”，授权 `7-5-mcp-interoperability-exit-review` 连续完成，不再逐步等待批准 | 固定官方外部 MCP Server/Client 身份、版本、许可、transport、时间窗和脱敏摘要；以 TDD 建立标准 stdio RiftCoach Server runner，使用隔离且锁版的官方 MCP SDK Client 调用一个 owner-scoped 只读工具，并对 OP.GG 做一次有界 Streamable HTTP initialize/list/call。实现 `a88fbc4/32483521108`、clean-SHA 双向门与 evidence `fac6fe0/32484257736` 均已通过；不扩大为公网部署、正式 Auth/TLS、Riot+OP.GG join、原始 body 持久化或 Stage 8 实现。 |
| RQ-080 | 2026-08-22 | 已执行；entry design 由 `3431e8b` / Actions `32564500421` 公共闭环 | 用户明确“那开始吧”，授权 canonical 的 `stage-8-multi-agent-reliable-runtime-productization-entry-design` | 已完成 Stage 8 初学者教学、现状/接缝审计、8A–8F 与 8-Core/8-Advanced 入口设计、ADR、实施计划、前端/MotionSites 资源采用门、Riot+OP.GG EvidenceBundle 边界和治理顺序；本批没有实现 Multi-Agent、DAG、cancel/resume、恢复、SSE、正式 Auth、前端、购买付费资源、公网部署或真实 Riot/Provider/LLM 调用。当前只交接 `8a-advanced-adoption-gate` prepared/waiting authorization。 |
| RQ-081 | 2026-08-22 | 已执行；8A 由 `12ad835` / Actions `32567642315` 公共闭环 | 用户在 Stage 8 entry design 与独立状态 SHA 均完成 exact-SHA 公共闭环后明确“开始”，授权 canonical 的 `8a-advanced-adoption-gate` | 本检查点只完成真实 workflow Bad Case/收益假设、候选矩阵、同切片基线、权限与上下文隔离、预算/指标/停止条件、不可覆盖实验身份、离线 gate TDD/评估器、ADR 和八维证据；允许把一个候选与一个更小的 comparator 交给 8B，但不在 8A 实现或运行 Multi-Agent/DAG、安装 LangGraph/第三方 Runtime、调用 Riot/OP.GG/Provider、读取 Key、开始 8C–8F 或写正式前端。 |
| RQ-082 | 2026-08-22 | 已执行；8B 由 `783a329` / Actions `32572610725` 公共闭环 | 用户在 8A exact-SHA 公共闭环后明确“继续推进”，授权 canonical 的 `8b-conditional-multi-agent-experiment`，无需逐小步重复审批 | 三路 implementation `180bc8b` 与 result/ADR/evidence `783a329` 均 exact-SHA 三 job 公共成功；同一 clean SHA 的 development 后唯一 holdout 形成 `reject_multi_agent`，结果 SHA `944258...445e8`、external I/O 0。ADR-0053 拒绝产品 Multi-Agent 并优先普通受限并行设计；8B 已关闭，只交接 `8c-reliable-runtime-core` prepared/waiting authorization。 |
| RQ-083 | 2026-08-22 | 生效；授权当前唯一检查点 | 用户在 8B 与独立状态 SHA 完成 exact-SHA 公共闭环后明确“继续啊，咋停了”，授权 canonical 的 `8c-reliable-runtime-core`，无需逐小步重复审批 | 本检查点按教学、方案比较、ADR、专用设计、TDD、八维持久证据、本地门禁、独立提交与 exact-SHA 三 job 连续推进；范围只含 durable lifecycle event、replay cursor、lease/heartbeat/fencing、cancel request、safe checkpoint/recovery、late-result/duplicate-terminal rejection、owner/global backpressure 与观测。不得覆盖或重跑 8B holdout；不进入 Multi-Agent、DAG/第三方 Runtime、Redis/Celery/Kafka、SSE/前端、8D Riot+OP.GG fusion 或真实 Provider/Riot/OP.GG I/O。 |
| RQ-084 | 2026-08-23 | 已执行；8D 由 `a274b7f` / Actions `32598480400` 公共闭环 | 用户在 8C clean implementation exact-SHA 公共闭环后明确继续正常下一步，授权 canonical 的 `8d-riot-opgg-evidence-fusion-core`；无需逐小步重复审批 | 已完成初学者教学、方案比较、ADR/设计/实施计划、typed Riot/Data Dragon/official patch/OP.GG partial EvidenceBundle、provenance/freshness/join/conflict/gap、no-I/O adapter 与 public projection；三 job 全绿且 coverage complete。本批未读取 Key、调用真实 Riot/OP.GG/Provider/LLM、进入 8E/8F 或重跑 8B holdout。 |
| RQ-085 | 2026-08-23 | 生效；横向作品集要求，不阻塞当前 8D | 用户明确 README 研究不能局限于少数高星项目，需广泛参考 Agent、RAG、开发者工具、基础设施及“星数不高但 README 很好”的项目；架构图/流程图/产品展示图可按信息目的混用 AI 绘图、截图、SVG、Mermaid 等形式 | 研究样本、许可证/事实核验、视觉素材候选和 README rubric 持续积累到 8F；不把样本研究插入或阻塞 8D，也不把 AI 概念图冒充真实截图；最终 README 用真实代码/测试/截图证据编排图文表。 |
| RQ-086 | 2026-08-23 | 已授权；8E preflight 进行中 | 用户授权“一次真实 Riot + OP.GG 验证，并进入 8E preflight”，同时明确前端要慢慢做；用户要求账号不能硬编码为 ShowMaker，需允许输入外服 Riot ID，也要能选择自己的账号或指定职业选手/高手作为观察对象 | 8E 先执行独立、有界、body-free 的 Riot/OP.GG external validation gate，再做脱敏 EvidenceBundle replay；不把真实网络调用混入 8D 公共 CI。玩家档案使用 `Riot ID + routing_region + relationship_role`，支持 `self + unverified_claim` 与 `observed + public_observed`，owner 可保存多个档案并让 Conversation 固定一个 player subject；不静默支持 CN、不自动跨区重试、不硬编码任何账号。前端拆成小批次，先合同/状态/可访问性，后动效。 |
| RQ-087 | 2026-08-23 | 已执行并完成修复后 live 验证；实现 `83fde7d` / Actions `32615340228` 公共闭环，成功 replay 由 RQ-088 继续 | 用户在得知前次授权窗口已完成后明确“那我新授权，把问题修复好”，授权继续处理真实 OP.GG `mid` schema-drift | 诊断窗口复用既有 body-free Riot 结果，只执行一次 OP.GG `mid` call，不重调 Riot、不调用 LLM、不保存 raw body。live 诊断将失败收敛到 `Mid.rank_prev_patch` 的 `Name` 节点；ADR-0058 只在 `rank_prev`/`rank_prev_patch` 接纳精确小写 JSON `null` 并归一化为 `None`，其他 Name/字段/表达式继续 fail closed。RQ-088 生效后进行一次修复后 replay，严格 adapter 通过并创建 body-free EvidenceBundle；具体 Meta join 仍按事实降级。 |
| RQ-088 | 2026-08-23 | 生效并已用于 ADR-0058 最终 live 验证；evidence `efaccd9` / Actions `32615821339` 公共闭环 | 用户明确指出真实调用不应过度保守：Codex 判断确有必要时可直接调用，不需要每次等待单独授权 | 对排障、采用裁决和验收确有必要且次数有界、费用/隐私风险可控的只读真实调用，Codex 可直接执行并记录调用次数、费用、输入边界、脱敏结果和停止条件。明显高费用、批量抓取、发送敏感/私人数据、不可逆外部写入或扩大产品权限的动作仍需单独确认。本条已用于一次 OP.GG `mid` body-free replay：Riot/LLM/Key calls 0，结果 passed 并生成 bundle，未自动重试。 |
| RQ-089 | 2026-08-23 | 已执行；8E Batch B 由 `e844bdd` / Actions `32622696087` 公共闭环 | 用户要求继续 8E 玩家档案/显式路由批，并指出此前各阶段本机 PostgreSQL、Docker、Linux 条件导致的 skip 应尽可能补齐，不能长期只依赖 skip | 已安装并配置本机 Docker Desktop、持久 PostgreSQL 17 测试容器和用户级 `RIFTCOACH_TEST_DATABASE_URL`，让真库与 Linux Compose smoke 可在提交前本地执行；历史本机 skip 不改写为未验收，因为相应 exact-SHA 公共 PostgreSQL/Linux job 已独立补证。Windows symlink 仍可单项 skip，不为清零而开启系统级 Developer Mode；每批继续保留 exact-SHA 公共 CI 作为独立关闭门。 |
| RQ-090 | 2026-08-23 | 已执行；8E Batch C 由 `7975dc3` / Actions `32629160732` 公共闭环 | 用户再次明确“继续”，授权 canonical 唯一下一内部批 Batch C，并沿用无需逐小步审批但必须完整教学、设计、TDD、持久证据、本地门禁和 exact-SHA 公共闭环的工作方式 | EvidenceBundle PostgreSQL 不可变快照/refresh revision/expiry 投影、8C durable event→cursor SSE 和 `published/degraded/rejected/not_ready` 产品状态/API 已完成三 job exact-SHA 公共验证；本批没有新的 Riot/OP.GG/Provider/LLM 调用，也未重跑 8B holdout。授权范围止于 Batch C；Batch D React 当前只 prepared/waiting authorization，Auth/RSO、HTTPS、备份与公网部署仍未实现。 |
| RQ-091 | 2026-08-23 | 已执行；8E Batch D 由 `f7ebedd` / Actions `32636771507` 公共闭环 | 用户明确“开始/继续下一步”进入 Batch D，并再次纠正前端研究不得过度依赖 MotionSites 或任何单一素材来源；应广撒网、统一横评、精挑少量高价值做法，再围绕 RiftCoach 自主重构 | Batch D 按既定小批顺序先冻结信息架构、design tokens、产品/客户端状态矩阵与可访问性合同，再实现 fixture-backed 静态近期复盘工作台并验证桌面/移动、键盘和 reduced-motion。研究池必须同时覆盖素材/Prompt 站、组件与动效库、真实成熟产品、游戏数据界面及 Riot/LoL 官方视觉语言，以品牌契合、信息可读性、动效价值、响应式、无障碍、性能、许可和实现成本统一评分；不得整页照搬、把付费素材当唯一依赖或让视觉来源改写产品合同。本批不接真实 API/SSE/Auth，不实现 HTTPS、备份、部署或公网发布。 |
| RQ-092 | 2026-08-23 | 生效；校准 RQ-091 的视觉取舍 | 用户认可许可、性能、移动端和可关闭动效等采用门，但明确不能因此过度简约；应在 actually suitable 的前提下主动追求更 beautiful、fashion、cool，并优先选择兼具工程适配与视觉完成度的做法 | 采用门改为“两层决策”：许可清楚、状态真实、键盘/reduced-motion、移动降级和性能预算是硬门；所有过门候选再按视觉完成度、当代感、品牌记忆点和 LoL 语义择优。RiftCoach 不接受普通后台式保守设计，也不以极简本身为目标；允许为少量高影响环境层、聚焦光、能量边缘和精致转场承担可验证的合理复杂度，同时避免每个组件互相抢戏。 |
| RQ-093 | 2026-08-23 | 生效；重申五模块与既有前端研究连续性 | 用户提醒当前实现不能遗忘旧任务已完成的前端来源分析、五模块蓝图及未穷举的视觉/作品集要求，并要求定向回查对话记录而非只靠摘要 | Stage 8 前端五模块继续保持：电影感 Riot ID 入口、近期复盘工作台、Rift Timeline、Evidence/Agent Trace 抽屉、Training Plan/Progress；Batch D 先完整实现近期工作台，并以 Drawer/Training 薄纵切验证共享设计系统，不表示删除其余模块。研究来源继续覆盖 Riot/LoL 官方语言、OP.GG/Mobalytics/Blitz 等游戏数据产品、成熟真实产品、Motion/React Bits/Aceternity/Uiverse/Anime 等组件动效库、MotionSites/Design Prompts/Aura 等 Prompt/原型资源及 Image2/Photoshop 生成与精修工具；每项仍须通过 RQ-091/RQ-092 采用门。ECharts/复杂 SVG 留给真实 Timeline DTO，Image2/Photoshop 优先服务后续电影感入口；8F README/作品集继续按 RQ-085 广泛研究并使用真实截图、架构图、图文表组合。 |
| RQ-094 | 2026-08-23 | 生效；修复 Stage 8 裁决到开工区间的持久上下文缺口 | 用户发现先前分析中的三个前端方向与 combination 没有被当前任务准确记住，要求重新复盘“五项裁决→正式开工”区间，并担心其它决策也被遗漏 | 最终视觉三方向固定为 `Rift Awakening` 电影感入口、`Esports Intelligence` 电竞分析工作台和仅作受限 Hero 实验的 `Void Holographic Lab`；组合是 `Cinematic Portal → Broadcast Workbench`。`Hextech Tactical Editorial` 是共享视觉语言，`Rift Command Center` 是工作台施工切片，均不取代三方向。每 checkpoint 必须给一次问题/取舍/数据流/证据/限制/下一步短复盘，连续批次再给总复盘。Stage 7 V1 不重开，但 8F 前必须另设 OP.GG useful-breadth gate，至少评估 champion analysis 与 lane matchup guide，synergies 仅按真实阵容消费者决定，不按 18 个目录工具追数量。当前 `degraded/unjoined` live replay 不等于完整融合纵向；8F 前还必须完成一次实际覆盖 Riot match、Data Dragon、official patch、OP.GG、个性化训练建议和前端 Evidence 追溯的 body-free golden slice，不覆盖旧负面证据追绿。 |
| RQ-095 | 2026-08-23 | 已执行；design `4057c93` / Actions `32639561338` exact-SHA 三 job 公共闭环 | 用户在上下文差异复盘后明确“继续做你觉得接下来应该做的事” | 先持久化 RQ-094 纠偏，再完成当前 canonical 唯一下一项：比较并冻结 owner-scoped latest-review locator、Recent Summary HTTP、typed Evidence HTTP、same-origin exact decoder、generation/AbortController、单 EventSource、受限 Markdown、真实 Training 字段与分层测试；完成 ADR/design/implementation plan、治理同步、独立设计提交和 exact-SHA 三 job。该授权不进入产品代码实现，不调用 Riot/OP.GG/Provider/LLM，不读取 Key，不进入 Auth/RSO、部署、完整入口/Timeline/Training、OP.GG breadth gate、fusion golden slice 或 8F。设计门已由公共 pytest 1752、真实 PostgreSQL 194、frontend unit 35/e2e 12/typecheck/build 与 Linux package smoke 同 SHA 通过；当前只把 live integration implementation 交为 prepared/waiting authorization。 |
| RQ-096 | 2026-08-23 | 已执行；implementation/evidence `f441061` / Actions `32647933692` exact-SHA 三 job 公共闭环 | 用户在 design exact-SHA 闭环后继续要求“做你觉得接下来应该做的事”，授权完整执行既有 `2026-08-23-8e-live-workbench-integration-implementation.md`，无需逐 Task 再批 | Tasks 1–12 的 TDD、后端/前端/真库/Linux/八维证据和公共闭环均完成；公共 pytest 1796、真库 200、frontend unit 66/e2e 17 与 package schema 1.6 同 SHA 全绿。授权未扩大到 Auth/RSO、部署、电影感入口、完整 Timeline/Training、OP.GG breadth、fusion golden slice 或 8F；这些仍未完成。 |
| RQ-097 | 2026-08-23 | 生效；仅授权当前唯一检查点 `8e-batch-e-security-deployment-entry-design` | 用户最新“那继续做你觉得接下来应该做的事”恢复 Batch E 安全/部署入口设计；本轮只冻结威胁模型、Auth/RSO 分离、部署拓扑、CORS/CSP/HTTPS/限流、Secret、backup/restore/erase、隐私/观测和剩余 Web 模块顺序 | 完成 ADR、初学者 walkthrough、正式 design 与原子 implementation plan、八维 coverage 路径、状态/计划/路线同步和 exact-SHA 公共设计闭环后停止；不实现 Auth/RSO、HTTPS、限流、备份、部署、入口/Timeline/完整 Training、OP.GG breadth、golden slice 或 8F，不读取 Secret、不调用外部服务。 |
| RQ-098 | 2026-08-24 | 生效；8E 视觉前置，不新增或重分类主阶段/检查点 | 用户确认三种方向中的融合版：`Rift Awakening / Cinematic Portal → Esports Intelligence / Broadcast Workbench`，并要求把概念图、Image2/Photoshop、MotionSites 与其他来源转成可执行的高级前端，而不是普通深色后台 | 采用 ADR-0064 与视觉合同实施计划：先做分层入口 preview、typed presentation state、动效 storyboard、来源/资产采用账本和桌面/移动/reduced-motion 门；母图只作 mood/可替换氛围层，真实 UI/数据/关系/产品状态仍由 React 合同提供。该前置不实施 Auth/RSO/部署、不调用外部服务、不购买付费 Prompt，不改变 canonical `8e-productization` 状态。 |
| RQ-099 | 2026-08-24 | 生效；用户要求视觉 Task 3 完成后连续推进后续 Batch E implementation，无需逐小步重新授权 | 用户明确要求先把机械感过强的入口继续 polish，完成当前视觉 Task 3 后正常进入后续任务并保持稳步、TDD、边界诚实 | Task 3 通过本地视觉/前端门后，按 Batch E E1→E5 顺序推进；E1/E2/E3 可先做本地实现和 focused evidence，但每批仍须八维持久材料、比例回归、独立提交与 exact-SHA 公共 CI。Auth/RSO、HTTPS、真实 Secret Manager、备份/部署和 8F 不因连续授权而被冒充完成。 |
| RQ-100 | 2026-08-24 | 已执行；production shell/Auth gate 由 `15a3a9e` / Actions `32663345737` exact-SHA 三 job 公共闭环 | 用户要求 Visual Task 3 完成后继续后续 8E 产品化工作，不把 preview 当成 production shell | 默认 live 路径已增加 provider-neutral same-origin `AuthGate`，typed opaque session 成功后才启动 controller；fixture/awakening preview 保持显式隔离。该批不声称 OIDC/RSO、真实 auth provider、PostgreSQL session repository 或 HTTPS edge 已采用。 |
| RQ-101 | 2026-08-24 | 已执行；Timeline DTO/UI 由 `794032f` / Actions `32682243568` exact-SHA 三 job 公共闭环 | 用户再次要求继续 polish，随后完成 Task 3 和后续 canonical 项，无需逐项等待授权；机械层不得喧宾夺主 | strict verified event/phase DTO、owner-scoped API、exact decoder/controller、responsive/a11y Timeline 与三张高保真 V1 截图已完成；公共 pytest、真实 PostgreSQL 和 Linux package 三 job 全绿。该闭环禁止伪造 Gold/CS/XP 曲线，也不把当前截图称为最终视觉成品；唯一下一原子项按 RQ-102 切到 bilingual product-surface foundation。 |
| RQ-102 | 2026-08-24 | 生效；新增 8E 横向双语产品表面要求 | 用户确认当前页面是英文，并要求后续正式支持中英双语 | Timeline exact-SHA 公共关闭后，先建立 `zh-CN/en` 双语产品表面基础，再扩建 Evidence/Trace 与 Training，避免后期整站返工。UI copy 用 locale catalog；API/status canonical code 不翻译；英雄/装备等 LoL 实体优先使用 Data Dragon locale；Coach report 语言绑定用户偏好，不用浏览器机翻冒充原始结论。必须验证语言切换持久化、缺词安全回退、长文本/移动布局、a11y 和 bundle budget。 |
| RQ-103 | 2026-08-24 | 生效；校准 8E 最终视觉验收边界 | 用户明确确认当前截图、UI、色调、背景、布局和细节设计都不是最终品，英雄名旁缺头像只是未穷举的例子；后续必须继续 polish | 当前 Timeline 只能表述为“严格功能合同 + 高保真 V1”，不得作为最终作品集视觉签收。Timeline 公共关闭后先完成 RQ-102 双语基础，再以独立原子批建立 Data Dragon version/locale/fallback 资产合同并补英雄头像、装备图标、目标图形、状态/焦点/选择联动与加载失败回退；8E 退出前还必须完成入口、工作台、Timeline、Evidence/Trace、Training 的跨模块色彩、背景、布局、动效、响应式和双语 final visual QA。不得直接拼接未锁版本/locale 的公网资产 URL，也不得把当前三张截图冒充最终成品。 |
| RQ-104 | 2026-08-24 | 生效；RQ-102 实施验收补充 | 用户明确否决英文直译式中文、`裂谷指挥中心` 等生硬命名，以及向最终用户解释实现/设计意图的 AI 式旁白；并要求两种语言都要修改 | `zh-CN` 与 `en` 必须分别按各自产品语境编辑，而不是互相逐字翻译；主产品表面不显示 reason/error/gap code、fixture/runtime/projection 等内部术语，也不放“为何这样设计”的说明。地区、状态和操作使用用户可理解的展示名；canonical code 只留在 wire/model/日志与测试。两种语言均通过 copy 审计、移动端 text expansion 与可访问名称测试。 |
| RQ-105 | 2026-08-24 | 生效；修正 RQ-098/ADR-0064 的入口信息架构 | 用户明确母图是最终动态开屏的视觉母版：先显示全屏电影感开屏，点击中央海克斯核心后才进入独立登录/账号绑定页，再进入工作台；AuthGate 不是开屏，账号表单也不能与开屏混在同一层 | 默认 `/` 必须是零提前 API/SSE 的 `Rift Awakening` Portal；中央核心是可点击、Tab/Enter/Space 可操作的真实控件。第二层才启动 provider-neutral session，并允许选择已有档案或通过现有 `POST /player-links` + 有界状态查询新增外服账号；明确选定 owner-scoped profile 后才启动 live Workbench。`?surface=awakening` 继续是 no-I/O 视觉预览，`?scenario=...` 继续是显式 fixture；不得把 preview handoff 偷渡成 fixture 成功，也不得把本地 session bootstrap 或 `self + unverified_claim` 冒充 Riot RSO/OIDC 登录/本人验证。 |
| RQ-106 | 2026-08-24 | 生效；强化 RQ-103/RQ-105 的 Portal 视觉资产边界 | 用户指出当前实现仍把较弱的旧 atmosphere plate 当主开屏，并要求不要围着少数旧图反复打磨；应按母图继续使用 Image2/图像工具生成新素材、新预想、新概念和新预览 | 旧 aperture 只能是加载失败 fallback，旧 instrumentarium 退出 runtime。母图的左 Rift/中央核心/右侧战术氛围必须转为可替换、无文字/无 UI/无核心的背景层，React 保持唯一真实核心、copy、状态和 handoff；关键帧只作设计证据。后续继续生成/编辑可分离 energy/core/route 层，不能把一张 flattened bitmap 冒充完成动效或最终视觉签收。 |
| RQ-107 | 2026-08-24 | 生效的产品目标；具体原子项顺序待用户裁决 | 用户提醒 RiftCoach 是 Agent 项目，不能只做静态复盘网页；Coach 应允许围绕复盘向 Agent 提问，Training 应结合复盘、玩家目标和反馈形成训练方案 | 最终产品必须暴露受质量门约束的 Agent 交互，而不只渲染已有 Report。推荐复用 Conversation/Message、conversation-bound task、AgentRuntime/Harness、terminal assistant、Memory Context 与 Training Candidate/accept，形成 review-grounded bounded Coach；不得临时伪造开放域聊天、token 流或自动写长期 Plan。是否在当前批闭环后、RQ-103 前插入该 8E 原子项，以及 observed 学习清单/流式边界，等待用户集中裁决前不改 canonical 顺序。 |
| RQ-108 | 2026-08-25 | 生效；全局 loop 由 RQ-112 校准，水晶细节由 RQ-118 取代 | 用户明确否决把静态 WebP、压暗/虚化场景、雷霆大标题和异画风 CSS/贴图水晶冒充电影化开屏；要求以已认可母图为基础，在场景内部重绘/调大中央海克斯水晶并让它随动态背景自然呼吸、流光和蓄能，不能另贴一颗独立水晶或做出常规按钮外观。水晶附近只用轻微闪烁点、短脉冲或小型符号提示可点击；点击后由画面内水晶汇聚、爆发并幕切到另一套动态账号场景。开屏不需要大段介绍，只保留少量与画面融合的自有字标；继续广泛参考 MotionSites、Riot 官方视觉语言、React Bits、Magic UI、Aceternity、21st.dev、Uiverse 等来源，但不得照搬或拼盘。 | RQ-102/104/105/106 foundation 必须先以独立 commit 和 exact-SHA 三 job 公共关闭；随后单独设计/实现 `portal-motion-polish`。本行保留原始意图；现行正常体验以 RQ-112 的全帧 loop 为准，原水晶/塔体/构图以 RQ-118 为准。透明原生 `<button>` 覆盖场景水晶，保留键盘、focus-visible 和读屏语义；Account 使用独立场景，Workbench 信息优先。媒体必须有同源版本化资产、codec/poster、移动安全区、reduced-motion、Save-Data、播放/解码失败 fallback、下载/解码/JS bundle 预算和许可/移除路径；不热链、不复制付费素材，不新增 Three/OGL/Anime 等运行时，除非新的 Bad Case 与采用门证明必要。当前静态 Portal 只能称为功能/构图 V1，不是最终视觉签收。本条只锁定 foundation 后的立即下一原子项；不裁决 RQ-107 bounded Coach 与 RQ-103 asset/detail/final-QA 在 RQ-108 之后的相对顺序。 |
| RQ-109 | 2026-08-25 | 生效；授权 RQ-108 | 用户在 foundation 与状态 exact-SHA 公共闭环后明确“开始” | `portal-motion-polish` 从 prepared 切为 authorized/in progress；先完成教学、现有 motion/media 接缝审计、方案比较、ADR-0068、正式设计、实施计划与素材采用门，再进入 runtime TDD。授权不扩大到 Coach、Data Dragon enrichment、OIDC/RSO、跨模块 final QA 或 8F。 |
| RQ-110 | 2026-08-25 | 生效；校准 RQ-108 的唯一视觉源与反例 | 用户明确上一轮暗化 Portal 截图绝不能沿用；应直接从确认的高清母图制作动态 background，可采用全屏循环视频方法；不得再给画面加阴影/模糊/暗幕，并要求保留此前关于少字、字体和广泛素材研究的全部意见 | `rift-portal-background-v2` 与当前暗化截图只保留历史 V1/anti-reference 身份，不得成为 RQ-108 最终 runtime、poster 或降级素材。正常模式以确认高清母图直接编辑/生成同构全屏循环媒体；原始高清母图或由同一母图无损导出的 poster 承担 reduced-motion、Save-Data、播放/解码失败 fallback。禁止全屏 dark overlay、vignette、blur 和大段文案；只允许融景小型自有字标、语言控件、不可见可访问名称和水晶附近的微光提示。MotionSites、Riot 官方语言、React Bits、Magic UI、Aceternity、21st.dev、Uiverse 等继续按许可/适配/性能门广泛筛选，不能依赖单一来源或做组件拼盘。 |
| RQ-111 | 2026-08-25 | 生效；Account 母图英雄表现纠正 | 用户确认核心内殿可固定为五个位置各选择一个英雄，固定与否不重要；但不能直接摆游戏英雄头像，需要把英雄自己的形象融入场景 | Account 第二幕以召唤师峡谷三路/野区/河道能量拓扑连接五个英雄共鸣位，分别表达上路、打野、中路、下路、辅助。英雄必须以全身能量幻影、晶体浮雕或建筑级全息剪影融入内殿，靠姿态、轮廓和标志性物件识别；禁止头像卡、原画墙、选角 UI、英雄名字标签或用户主玩英雄暗示。五个形象统一为同一蓝金晶体/能量材质、低对比纵深编排，正常 loop 有界轮流唤醒；右侧账号面板负空间不被侵占。具体固定阵容和许可/生成边界在 ADR-0068 与概念图审查中冻结。 |
| RQ-112 | 2026-08-25 | 生效；纠正 RQ-108 循环动态范围 | 用户明确所说的动态 background 是整张画面的全局循环动态，不只是水晶或少数局部元素 | Portal 正常体验必须是全帧循环视频：左侧 Rift/云雾/远景、整条能量道路与前景反射、中央建筑缝线/平台/水晶、右侧战术星图/节点/粒子和多层环境光都应持续形成无缝全局运动；点击再从全局 idle loop 收敛为一次蓄能/burst。Account 也必须让峡谷三路/河道、五英雄幻影、内殿光线、粒子和反射全局循环，只是节奏更安静。禁止用静态 poster 加少数 DOM 光点冒充动态成品；poster 仅限 reduced-motion、Save-Data、媒体失败和首帧加载。镜头可以锁定或极慢视差，但不得晃动、频闪或用 fade-to-black 掩盖 loop 接缝。全局动态导致的码率/解码预算须在 ADR-0068 重新测量，不得以旧局部运动预算降低视觉目标。 |
| RQ-113 | 2026-08-25 | 生效；拒绝 Account v2 一次性群像生成并改用分层制作 | 用户确认右侧留白用途后，指出 Account v2 整体怪异，金克丝右脚、千珏下半身等人物细节失真 | Account 右侧平整负空间继续服务真实 DOM Account/玩家选择面板；v2 群像图判为 rejected，不进入 runtime。后续必须先制作无英雄的干净核心内殿＋峡谷母图，再以官方原画为身份参考逐个场景化重塑青钢影、千珏、阿狸、金克丝、锤石的完整全身晶体幻影；每位单独验收解剖、手脚、武器、尾巴、面具及羊狼双体关系，随后用分层合成统一透视、光照、材质和动态。禁止直接提取/抠取官方原画，也禁止再让单次生成同时解决场景、地图、五人身份与最终合成或用整体“风格化”掩盖人物畸形。 |
| RQ-114 | 2026-08-25 | 生效；禁止英雄原画抠图式合成 | 用户指出 Account v2 几乎像把官方原画抠出后直接放进场景 | 官方 splash 只能作为身份参考，禁止直接抠图、沿用 splash pose、套蓝色滤镜或把人物当贴纸叠到地图上。五位英雄必须分别重新雕塑为内殿原生的全身能量回响：保留可识别脸型、发型、服装结构、武器和轮廓，但重新设计与对应峡谷路线、基座、建筑遮挡、接触、投影和反射相匹配的姿态；材质由内部晶体、金色结构骨架、雾化边缘和场景光共同形成。流程固定为“官方原画身份参考 → 单英雄场景化重塑与逐项解剖验收 → 分层透视/光照合成 → 全局 loop”，不得退回抠图换色。 |
| RQ-115 | 2026-08-25 | 生效；地形细节由 RQ-117 校准 | 用户指出无英雄 Account 底座几乎没有召唤师峡谷，只剩机械架子 | Account 左侧主体必须是一眼可辨识的召唤师峡谷立体地貌/沙盘，而不是抽象轨道：保留上中下三路、斜向河道、两片野区、男爵/小龙坑、双方基地、防御塔层级以及森林、岩壁、河水等地形关系，再把边缘、照明和能量材质融入蓝金内殿。必须使用 Riot 官方 Data Dragon Summoner's Rift map 作为地理身份参考；内殿建筑包围真实峡谷地貌，不能用金属路线装置代替地图。五英雄后续从各自对应地貌/路线中形成，不站在五个通用机械底座上。当前 `account-rift-attunement-base-v1` 判 rejected，不进入 runtime。RQ-117 后续明确：这些地貌类别只用于锁定拓扑身份，不要求伪造写实微细节。 |
| RQ-116 | 2026-08-25 | 生效；补齐 Account 峡谷红蓝阵营语义 | 用户指出峡谷底座没有区分红蓝方 | Account 峡谷必须按官方 map11 明确表达左下蓝方与右上红方：双方基地、塔、半区线路和接近中线的能量颜色可辨识，蓝方使用青蓝，红方使用绯红/暖橙；河道保持中性蓝，男爵坑保持紫色，小龙坑用受限暖色。禁止把两边统一成蓝色，也禁止给整张图套红蓝渐变或用阵营光效遮蔽真实地形。阵营是固定地图语义，不是当前用户队伍或比赛结果。 |
| RQ-117 | 2026-08-25 | 生效；校准 Account 地图真实性与抽象层级 | 用户指出红蓝峡谷候选宏观看似可用，但细看仍像模型瞎编的峡谷；公开参考不足以精确复刻全部树墙塔，因此宁可笼统一些，也不能一眼假 | Account 母图使用 Data Dragon `map11` 与 Riot 2024 near-final map concept 锁定官方拓扑：三路、斜河道、双野区、男爵/小龙坑、双方基地以及左下蓝方/右上红方必须稳定可辨。地形表现改为有意概括的 Hextech 战术地形投影，以 terrain masses、轮廓、层级、材质区和符号节点表达野区、墙体、塔与基地，不声称逐树、逐墙、逐塔或写实正射精度；禁止生成位置看似具体却错误的微型植被、建筑、道路或地貌。候选必须同时通过官方拓扑叠合审查和“无伪写实细节”人工门，未通过前不得成为 Account source、英雄合成底座或 runtime。RQ-117 只校准 RQ-115 的细节表达，保留其地图可辨识要求和 RQ-116 的阵营语义。 |
| RQ-118 | 2026-08-25 | 生效；取代 RQ-108 的水晶放大/重绘要求 | 用户在看到放大水晶候选后拒绝把独立或异画风水晶贴回母图；两次无 mask 放大 edit 都遮蔽塔体并破坏画面主次 | Portal source master 保留确认母图中的原始水晶、塔体和构图，不再以生成 edit 放大或替换水晶。全局 loop 只让画面内原水晶产生呼吸、折射、蓄能和点击 burst；透明原生 button 继续覆盖其真实位置。任何独立水晶、放大水晶、CSS 水晶或贴图按钮均为 rejected/anti-reference，不得成为 edit target、poster、loop、fallback 或 runtime。RQ-118 只取代 RQ-108 中“重绘/调大水晶”的视觉细节，保留 RQ-108 的电影化 Portal、全局动态、语义点击与幕切目标。 |
| RQ-119 | 2026-08-25 | 生效；Kimi image-to-video 降为未准入候选并增加多模型横评门 | 用户按教程把图片交给 Kimi 生成 12 秒循环视频并在 Chrome `localhost:7100` 预览，结果明显模糊、构图/水晶关系不符且不是期望的全局动态，因此要求考虑其他 image-to-video 方法 | 本次 Kimi `hero-loop.mp4` 只作为 rejected Bad Case，不进入 RiftCoach source、poster、loop、fallback 或 runtime；这不证明所有 Kimi 配置永久不可用，但 Kimi 不再是默认制片路径。正式资产前必须用同一获准 source、同一 motion brief 和冻结验收表横评至少三类可用路线，优先包含支持 first+last/keyframe/loop 控制的 Veo、Luma Ray、Runway/Firefly 多模型工作流，并可在硬件/许可可行时加入本地开源路线。先过 source/composition/geometry/texture/loop/flicker 硬门，再比较分辨率、码率、生成成本、延迟、许可和返工次数；不得以标称 1080p、网站预览或单次 Prompt 成功冒充视觉准入。任何付费生成、API Key 创建或批量调用在实际执行前仍单独确认费用与凭据边界。 |
| RQ-120 | 2026-08-25 | 生效；image-to-video 横评扩为生成式、确定性与混合式三路线 | 用户进一步提出 Seedance、通义万相以及 HyperFrames、Remotion 等模型/skills，要求寻找比 Kimi 教程更可靠的方法 | 横评不再只比较生成模型：A 线比较 Wan 2.7/Seedance/Veo/Luma/Runway 等 first/last/reference I2V；B 线以分层母图 + HyperFrames 或 Remotion 做 frame-driven 确定性 loop；C 线把生成模型限制在 Rift/雾/能量/星图/反射等有机层，建筑、水晶、道路拓扑与镜头由确定性合成锁定，作为推荐 primary candidate。HyperFrames/Remotion 是离线制片工具，不进入 RiftCoach browser runtime，也不能从一张扁平图凭空产生可信遮挡层；采用前必须证明可分层/inpaint 资产、运动覆盖、画质、loop、编码、许可、维护成本和可移除性。HyperFrames 因 Apache-2.0、HTML/seekable frame 和成熟主 skill 可优先做隔离 no-runtime spike；Remotion 保留 React 熟悉度对照，但须单独核对其 source-available/组织规模许可。任何 skill 安装前先安全审计；本条不授权安装、购买 credits、创建 Key 或调用付费模型。 |
| RQ-121 | 2026-08-25 | 生效；官方视频端点优先，中转站只作可验证 secondary transport | 用户补充其正规中转站可提供 Seedance 1.0/1.5/2/2.5、Kling 2.6/3、Grok Video、MiniMax Hailuo、Sora 2、Veo 3.1、Vidu Q3、Wan 2.6 等视频模型，允许合适时利用，但强调官方优先且站内目录可能缺少更新模型 | 截图中的 model slug、`official` 后缀、渠道说明和单次价格只作为候选目录，不自动证明厂商模型身份、能力或官方直连。横评仍优先官方 API/Console；中转只有在调用前可验证 model/version mapping、first/last/reference 能力、输入输出无静默压缩/水印、数据保留/删除/训练政策、地区与传输、错误/重试/计费语义、可冻结的调用上限和 body-free provenance 时才参加同一评分表。站内候选优先核验 Seedance 2/2.5、Veo 3.1 official、Kling 3、Sora 2 与 Wan 2.6；列表不是封闭集合，正式 bake-off 前重新查官方最新可用模型。不得把中转 Key、base URL、原始响应、上传母图或生成视频写入仓库；实际付费调用仍需冻结费用与敏感传输边界。 |
| RQ-122 | 2026-08-25 | 生效；扩展 RQ-121 的候选覆盖并修正 transport/版本推断 | 用户明确候选不能只盯 Veo/Seedance/Wan，Grok 和其他当前模型也应广筛；正规中转可信度不应被过度保守地否定。用户随后提供阿里云官方模型市场截图并确认 `wan3.0-video` 邀测已通过，又纠正 Grok 至少已有第三代 | Task 5 的调研池覆盖 DragonAPI 当前完整视频目录与上游官方最新模型，不把“首轮最多两个付费槽位”误写成“只研究两三款”。DragonAPI 未启用 Wan 3.0 只影响该 transport；官方百炼已成为 A1 且 access 已由用户证据证明。`grok-video-3` 的 relay 第三代存在性由模型广场、价格页和通用 `/videos` 示例证明，不能因 xAI 公开索引的命名差异降回 1.5；但专用 schema/upstream mapping 缺口仍须诚实记录。最终付费调用仍以 source fidelity、首尾帧/locked camera、1080p/时长、费用和数据边界择优，不按品牌热度或单一站点目录决定。 |
| RQ-123 | 2026-08-25 | 生效；授权 official 与正规中转实际有界横评 | 用户再次提醒不要因中途补充 Wan 或其它信息忘记前面候选，并明确 DragonAPI 中转同样可以实际试用；又要求继续保留此前设计的全局动效，而不是把生成目标缩成局部水晶/粒子动画 | Task 5 先用同一 source、同一 prompt digest、同一硬门分别执行官方 Wan 与 Dragon/Veo 单样本；中转不是 catalog-only。首轮失败后可按已冻结替补池继续必要单样本，无需逐候选重新请示，但必须一候选一调用、首错停止、不充值/订阅/批量重抽并记录实际费用。motion brief 固定覆盖左 Rift/云雾/远景、整条道路/反射、原水晶/塔体、右星图/节点/粒子/环境光的全帧闭环，镜头锁定且首尾位置/相位/曝光/速度一致。该授权不允许读取或持久化 Key，也不改变 Account、Workbench、Coach、Training、RQ-103 或 8F 的后续范围。 |
| RQ-124 | 2026-08-26 | 生效；Portal active source 从 v1 迁移到轻清噪 v2 | 用户在 Wan 上传前指出既有母图的密集点状碎光看起来不适、怪异，并质疑大/小水晶版本；hash 与持久证据确认小水晶 v1 是当时 canonical、大水晶 edit 已被 RQ-118 拒绝。用户查看原图/强清噪/轻清噪三联后确认轻清噪版“还可以” | v1 SHA `552a874...aada` 保持 byte-exact archival parent；轻清噪 v2 SHA `8134c0ca...1a06e` 成为当前 Portal bake-off/production source。强清噪 sibling 因 SSIM `0.753497` 和过度重绘 rejected；v2 parent SSIM `0.859593`，只采用约 35–40% 微点收敛，不推翻 RQ-118，不放大/替换水晶。所有 planned/adopted manifest、source SHA、provenance、prompt binding 和 public preflight 必须迁移到 v2 后才允许上传；此前绑定 v1 的 Kimi/HyperFrames 历史证据不得重写。 |
| RQ-125 | 2026-08-26 | 生效；Veo 样本拒绝，但 A 线不得因未充分利用而永久关闭；C 线改为优先 proof | 用户观察到 Veo 成片只让 Rift、道路/水晶、星图等少数焦点明显运动，离 MotionSites 类整幕动态仍远且生硬；随后指出当前 Wan/Veo 两次试用本身没有完美利用模型，担心过早切路线。用户接受 C 线仅在更理想、更可控时成为下一优先路线 | Veo 当前样本按 source/seam/full-scene motion/raw encoding/budget 门 rejected；不能把它写成模型能力上限。复核确认 `metadata.lastFrame` 映射正确，但冻结 prompt 同时重述场景并使用多个 `subtle/slow/restrained/almost imperceptibly` 词，与 Google I2V 的 motion-only 建议冲突。Task 5 下一步先做 no-paid-call 混合 C 线分层/mask/inpaint + deterministic frame-clock proof；生成模型只负责有机 plates。C proof 不合格时以新采用门恢复一次校正 A comparator：短 motion-only、明确整幕运动、首帧控制 + 后期 seam，不复制当前同图首尾/密集限制。Wan/Veo/Vidu/Kling/MiniMax/Seedance/Grok 均未被永久拒绝；不得继续用当前失败配置换模型抽卡。 |
| RQ-126 | 2026-08-26 | 生效；C-line overlay proof 明确失败，恢复校正 A comparator | 用户查看 C proof 后指出它只是“在母图上加一堆线条在诡异地动”，不是图自己动，也不是所需的全局循环动效 | C proof 裁决 `proof_fail_reopen_corrected_a`；不得用 seam、grid pixel change 或低体积绕过人工视觉失败。tracked composition/renderer 只保留负面工程证据，不进入 runtime；不继续调 SVG/HUD 覆层。下一次生成式对照必须 first-frame only、short motion-only、medium clearly perceptible full-scene environmental motion、禁止 object-by-object spotlight/HUD，locked camera 但不要求 pixel-stable；循环接缝在通过运动审查后确定性收口。 |
| RQ-127 | 2026-08-26 | 生效；校准 corrected A 的全幕运动幅度与 cool 目标 | 用户进一步明确不止三个主体动，而是整幅画都有呼吸感；可参考 MotionSites 案例与此前 B 站教程的全屏动态背景观感但不照搬。此前 Veo 即使比 Wan 明显仍太轻，需要更 evident、更 cool | corrected Veo comparator 必须从第一秒到最后一秒让 left/center/right 和 near/mid/far 同时持续参与：体积雾与空气深度、大尺度蓝金环境光、建筑/地面/反射、全道路能量、Rift 内部纵深、水晶折射和整片星空体积场共同运动。允许构图锚定的小幅顺滑 camera float/parallax cycle，不再要求完全 pixel-stable locked camera；禁止 static large regions、三个焦点轮流表演、HUD/线圈/节点贴层和 `subtle/almost imperceptibly`。目标强度为 medium-to-strong、clearly perceptible、dramatic/cool，同时禁止突发 zoom、reframe、shake、融化和新增物件。MotionSites/B站仅作观感与 pacing reference，不复制模板、付费 prompt 或源码。 |
| RQ-128 | 2026-08-26 | 生效；禁止单次失败后未经归因就换方法 | 用户指出当前开发经常在失败后没有确认是调用/处理、请求、transport、工具还是方法本身的问题，就过早判定路线错误并切换；同时认可不能无限死磕 | 每次失败必须按五层 fault tree 归因：local runner/download/parser → request/schema/parameter → relay/transport/upstream task → successful-output model quality → cross-sample method/architecture。没有 output 时质量保持 unknown，不能否定 Provider 或方法；单个质量 Bad Case 不能自动否定整条路线。切换工具/模型只能作为控制变量实验，并必须声明哪些变量保持/改变及它能区分什么。重试只在修复明确 client bug、证实 transient fault 或提出可证伪新假设时允许；blind retry 和无界死磕都禁止。Vidu 当前继续执行，但只作为同 transport/source/motion/first-only 下的 schema/model comparator；若也 generic failed，应优先停下审计 relay/request，而不是继续换模型。 |
| RQ-129 | 2026-08-26 | 生效；全局循环收紧为 locked-frame、精细的景内 living environment | 用户认为成功 Vidu 成片仍不理想，最大问题是整体漂移，动效本身也差；并指出先前 Veo 的场景内动效方向反而更接近目标，只是太轻、粗糙、层数不足。用户要求先复盘清楚目标，再决定 Veo/Seedance/Grok 等模型，不能把失败简单归因模型 | Portal loop 的 frame/lens/scale/composition/architecture/object positions 基本锁死；全局来自景物内部：near/mid/far 与 left/center/right 的体积空气/雾、大尺度蓝金 caustic、建筑/地面/反射、全道路能量、Rift 多层能量体、水晶内部折射/材质光、塔体暖金缝和整片星空体积场同时持续形成 medium/evident/intricate/cool motion，并在 8s 末回到同 phase/exposure/velocity。禁止整体 camera drift/pan/zoom/dolly/orbit/reframe/Ken Burns、全屏曝光脉冲、三主体轮流、HUD、cheap bloom、融化和纹理沸腾。下一最小变量实验保持已成功的 Veo first=last/model/transport/source，只重写 refined locked-scene storyboard；若仍太轻/局部/粗糙，才进入 Seedance 2.5，Grok 等待 exact mapping/schema。Vidu 样本仅按 drift/source/seam/provenance rejected，不外推模型。 |
| RQ-130 | 2026-08-26 | 生效；付费视频请求必须先完成内容与请求双重 preflight | 用户充值后要求只有“万事俱备”才继续烧钱，并明确万事俱备不仅指余额，也包括提示词、要求和约束已尽可能接近理想效果 | 每次付费视频 POST 前必须同时确认：余额高于可验证预扣金额；旧失败未暗建 task；源图/URL/SHA 正确；模型、first/last、时长、分辨率、音频、压缩与 resize schema 已验证；positive prompt 遵守 image-to-video motion-only/单场景原则并覆盖 locked frame、left/center/right、near/mid/far、simultaneous motion、八秒 phase/exposure/velocity 闭环；negative prompt 以 unwanted phenomena 描述 camera drift/局部轮流/HUD/融化等失败；runner parse、one POST/no retry、唯一 output/status 路径和 body-free evidence 全绿。余额足够不能绕过提示词质量审查；preflight 改动必须先持久化并完成 exact-SHA 公共门，随后才允许一次计费请求。 |
| RQ-131 | 2026-08-26 | 生效；API generic failure 后改由用户在 Studio 手动提交同一 Veo 对照 | 用户在明确理解 API task 已被 relay 接受但 generic failed/no output 后，选择“那就 Studio”，并已说明实在不行可由本人操作生成 | 保持 v5 source/model/first=last/8s/1080p/16:9 与运动目标；Studio 没有独立 negativePrompt 输入，使用合并但仍 motion-first 的单文本框版本，提示词增强关闭。Studio 固定生成音频不改变视觉目标，成功后本地移除。Codex 只预设参数、提供文件路径/提示词/检查表，不替用户点击预计 19.71 的最终生成；上传自动化若因 Chrome file URL 权限失败，不反复改权限，用户手动上传同一 v2 图两次。QQ support 草稿保持未发送。 |
| RQ-132 | 2026-08-26 | 生效；不走 QQ 支持，付费原样复现早期成功 Veo v1 | 用户拒绝联系支持，并追问为何早期 Veo 成功；在获知成功 v1 的运动约束远弱于后续 v2/v5 后明确“那消耗呗” | 不重发 v5、不换模型。Studio 使用同一 `Veo 3.1 Quality Official`、同一 v2 首尾帧、8s/1080p/16:9、enhancement off 和历史 v1 exact prompt SHA `f324264...36fc` 做一次最小变量复现。用户手动上传；Codex 仅在 2/2、预计 19.71、prompt/参数 readback 与 exact-SHA public gate 均通过后创建一次任务。v1 成功支持“运动包络跃迁过重”假设；v1 也 generic failed 支持“当前通道/上游变化”假设。任何结果都不自动重试。QQ 草稿继续不发送。 |
| RQ-133 | 2026-08-26 | 生效；Seedance 成功样本优先走文档化 video edit，而非直接重抽 | 用户认可当前 Seedance 成片方向有希望，但指出整体运动仍像雾层覆盖、希望保留三个主体正确运动并丰富静区，明确“那试试？” | 使用已成功 task `task_w6...ULvW` 的结果 URL 作为 `reference_video`，调用 Dragon 文档化 `seedance-2-5` + `video_operation=edit`；`duration=-1`、`aspect_ratio=adaptive`、720p、无音频；时间段 prompt 只编辑道路/地面/反射/建筑缝/远景空气/星图等静区，不重构镜头，不混用首尾帧。Studio 主编排器的“视频参考”input 实测只接受图片 MIME，因此不冒充 Studio 编辑。费用按 8.041667s 估算约 `$12.0191`，编辑计费/最低时长仍须以实际账单确认。公共 preflight 与余额门通过后只允许一次 POST；失败不盲重试或立即换模型。 |
| RQ-134 | 2026-08-26 | 生效；取代 RQ-133“只补静区、不增强三主体”的过窄解释 | 用户在查看豆包 Seedance 对照后明确提醒：左侧 Rift、中央水晶/平台、右侧星图/能量场三个主体的动效都要加强，尤其不能忘记右侧；同时必须继续完成整体场景动效优化，而不是只加某一种光轨 | 后续正式 edit 同时设两组硬门：A) 三主体各自升级且右侧单列不可遗漏验收；B) 建筑、道路、地面反射、云/空气、星空纵深等整体环境持续同步运动。允许三主体增强，但不得靠三处变亮冒充全局改善。豆包对照的大面积暖金轨迹只保留“沿真实结构/道路流动的光轨”这一动作语言；色彩改为 Rift/水晶一致的冷蓝、青蓝、电弧蓝为主，暖金只作低占比结构节点强调。光轨只是全局 motion stack 的一层，不能替代 Rift 内部多层涌动、水晶折射/能量蓄放、右场细丝/星尘/节点、建筑材质、地面焦散、反射与多层空气的共同呼吸。 |
| RQ-135 | 2026-08-27 | 生效；即梦文件选择改由用户执行，多参考必须有独立功能 | 用户观察到 Codex 操作系统文件选择器反复耗时，明确要求以后网页/客户端上传文件由本人选择；又提出即梦支持多参考，询问是否多生成几张图会更稳，同时要求 Codex独立判断而非照单全收 | 系统 file picker 一律由用户手动操作，Codex只提供逐字路径、素材角色和上传后 readback。第一轮即梦智能编辑只用成功 MP4 + immutable v2 母图；不为了数量生成审美概念图，因为新增构图/材质/光照会增加平均化和 source drift。若高级编辑可用，优先区域框选左/中/右及静区；只有纯文本确实无法识别右场/静区的真实 Bad Case 后，才另行设计不含新审美元素的功能性 motion mask，并先过采用门。 |
| RQ-136 | 2026-08-27 | 生效；高级编辑必须按时间戳帧标注，不使用单帧静态区域图冒充时序编辑 | 用户指出高级编辑具体到时间点，要求先查清真实用法再操作 | 高级编辑按 `00:00 / 00:04 / 00:07` 三个帧标注实施：每次先定位时间并暂停，再画区域/方向/定位、填写该时间点说明、点“添加至输入框”。`00:00` 启动三主体与全局，`00:04` 为左/中/右同级峰值且右侧不可遗漏，`00:07` 为持续运动下的循环回收。旧单帧五标注说明作废；每个时间点单独形成输入框帧标注，主 prompt 另以 `0–2/2–4/4–6/6–8` 描述连续控制。 |
| RQ-137 | 2026-08-27 | 生效；先完成当前 Portal Motion Polish，再重开模型迁移与其余 Agent 产品批 | 用户在复核 Agent 尚未完整收工、默认仍为 GLM-5.2 且 GLM-5.3/Flash 已正式可用后，明确要求“至少先把 portal 做好，先继续之前的任务” | canonical 保持 `8e-productization / portal-motion-polish / Task 5`：先完成当前 evidence public closure、零费用 identity fault split、针对性 source-side 生成/采用门、runtime integration 与 Portal 视觉验收。GLM-5.3/Flash adoption gate 记录为 Portal 闭环后的高优先横向项，不在当前脏批中插入；该顺序也不授权无界生成、降低 source/seam 门或把 Coach/Training/8F 说成已完成。 |
| RQ-138 | 2026-08-27 | 生效；Task 5 重新冻结静态方向与参考模式选择 | 用户明确指出现有拼接视频不要继续沿用，要求先重新梳理“全局持续、稳定、明显且 cool”的效果，并可先用 Image2 生成多张静态预览；同时要求解释即梦“全能参考”、首帧与首尾帧取舍 | AutoGLM 文生图稿仅作反例，不进入产品；Image2 只允许在代理可用后基于确认母图做同构 image-to-image preview。下一校正视频采用首帧单锚点；首尾帧保留为后置对照；全能参考定义为多图/文/音/视频融合入口，不等同严格 Video1 编辑。未完成静态方向门前不烧视频额度、不复制现有视频、不接 runtime。 |
| RQ-139 | 2026-08-27 | 生效；Image2 预览不作为 motion direction 证据 | 用户指出两张 Image2 结果看起来只是变色调，要求不要把调色误当成动效设计 | 两张预览标为 `direction-rejected`，确认母图仍是唯一 source；后续静态稿若再做，必须保持全局色调和几何，只改变可供 I2V 理解的材质、遮挡、反射与能量相位。第三次请求的余额不足不重试。 |
| RQ-140 | 2026-08-27 | 生效；跳过 Image2 静态稿，直接准备新视频请求 | 用户明确允许不使用 Image2，要求直接准备重新生成方案，同时不沿用当前视频 | Image2 调色型预览降为可选辅助；下一次视频只使用确认母图首帧，采用简短正向的全幕持续运动 brief，取消首尾帧和全能参考的默认资格。请求先完成参数/价格/source/prompt readback 与 exact-SHA preflight，再恰好一次调用；失败不盲重试，production media 继续为 0。 |
| RQ-141 | 2026-08-27 | 生效；v3 候选拒绝，下一次调用前必须重写运动合同 | 用户观看 Seedance 2.5 v3 后指出：左 Rift 先静止再突然画出几圈硬环；Rift 下方道路流动不应等到 burst 才出现；中段 burst 应是中央上下穿过、轻柔激发水晶的循环式呼吸，而不是横向窜线和突兀闪白；右侧必须在 burst 之外持续运动，整体也要有稳定的全幕动感 | v3 作为 research-only/rejected 保存，不进入 runtime。后续 motion brief 必须把基础层（道路/裂隙下方流、右侧星图/地形、建筑接缝、地面反射、云与空气）从首帧到末帧设为持续状态；burst 仅作为低占比、约 2–3 秒、中央纵向能量蓄放并自然回落的叠加，不改变几何、不跨画面画直线、不用过曝或全局闪烁制造变化。左/中/右与 near/mid/far 需在 burst 前后同样可感知，且末帧回到首帧相位后才重新评估付费生成。 |
| RQ-142 | 2026-08-27 | 生效；v4 候选拒绝并暂停首帧盲抽 | 用户观看 v4 后指出结果“很不理想”：左/右/整体几乎不动，中央平台被做成突兀的发光圆顶；并质疑 prompt 是否乱写。复核确认 v4 的正向词仍过于抽象/过载，模型以中心显著物变形替代全幕材质运动 | v4 保存为 `research-candidate-rejected`，Task 5 暂停继续首帧生成。后续必须先完成 prompt/mode fault split：把“景内运动载体”（道路流、表面反射、云层视差、星尘/节点相位、Rift 内宽幅流）写成可观察的局部行为，禁止平台体积变化/新圆顶；同时重新评估首帧单锚点是否适合全幕运动，不能仅靠增加抽象形容词或继续换模型。未完成新合同与方法裁决前，不再付费重抽、不接 runtime。 |
| RQ-143 | 2026-08-28 | 生效；禁止 source duplicate 纱罩并强化清晰度/材质流门 | 用户审看分层 proof 后指出画面像“往母图上蒙了一层纱”，清晰度下降，运动仍像乱动的半透明层；随后要求继续但不能沿用这种做法 | 所有后续 Portal motion proof 必须以 active v2 母图作为不移动、不过滤、不过度重采样的清晰底图；可移动层只能来自源图中可解释的高光/能量/反射像素或通过独立材质 plate 明确生成的透明层，禁止整张母图副本、全局 tint/veil、建筑边缘双影和低幅 shimmer 冒充全局运动。source→first SSIM、底图清晰度和无纱罩为硬门；三大区/near-mid-far 仍必须有可感知的真实材质流动。若只能得到像素均衡但观感廉价/过轻，直接 rejected，不继续堆 opacity。 |
| RQ-144 | 2026-08-28 | 生效；在分层 proof 被拒后，有界重开 Wan 3.0 官方 first-frame-only 对照 | 用户明确指出当前局部分层结果廉价且不对，要求不要继续在错误素材上调参，重新好好使用此前未公平利用的 Wan 3.0 官方通道 | 只允许一次新的 Wan 3.0 官方诊断：active v2 母图仅作为 `first_frame`，不再把同一张图同时当 `last_frame`；使用 `ratio=adaptive`、1080P、12 秒、audio=false、prompt_extend=false、watermark=false、固定 seed 127；motion-only brief 只描述持续的景内材质运动，暂不加入 burst。先核对同区 endpoint/账号余额或价格、source/prompt SHA 与 one-POST runner；失败不盲重试、不接 runtime、不进入 production_media。成功输出仍须通过 source identity、三大区与 near/mid/far 全幕运动、loop/编码和人工视觉门。 |
| RQ-145 | 2026-08-28 | 条件提案；Wan 重开仍不理想时切换为 Riot 官方壁纸/屏保素材路线 | 用户提出若本轮 Wan 仍廉价或运动错误，就停止自制整幕视频，改用 Riot 官方 League Displays 中按符文大陆地区选择的动态壁纸作为 Portal 背景，并以静态壁纸作为 Account 背景；Portal/Account UI 继续吸收 MotionSites 的交互结构 | 在 Wan 结果未审查前不替换当前路线。若触发条件成立，先核对 League Displays 官方素材的可下载格式、动态播放方式、来源/许可、体积、移动端和 reduced-motion fallback；用户选择地区后只加载对应的本地白名单素材，Account 使用独立静态素材，Portal 点击/登录过渡与媒体降级保持现有 journey 合同。不得把第三方壁纸热链或未经来源核验的转载素材直接放入 runtime。 |

| RQ-146 | 2026-08-28 | 生效；用户确认立即转入官方/授权壁纸路线 | 用户明确“转战”，放弃继续寻找 Wan endpoint，并提供已下载的 `animated-demacia.webm` 作为第一个真实动态壁纸候选；同时要求后续广泛寻找高质量地区壁纸和其它合法来源 | Wan first-frame reopen 停止在 HTTP 404/no-task 诊断，不再发送第二次 POST。当前先审计 Demacia 文件并建立 region wallpaper catalog/research preview；Portal 采用地区选择后加载对应本地动态壁纸，Account 使用独立静态壁纸。League Displays、Wallpaper Engine/Steam Workshop、Riot 官方素材或其它来源必须分别经过来源/许可、格式/体积、浏览器/移动端/reduced-motion 和 loop 门；未通过的文件不得进入 runtime 或公开仓库。 |
| RQ-147 | 2026-08-28 | 生效；补充班德尔城候选与静态图清晰度门 | 用户找到班德尔城动态壁纸，并在桌面 `RIFTCOACH` 文件夹整理了多地区动态/静态素材；其中 `runeterra-bandlecity-03.jpg` 只有 926×1080，存在明显压缩与低清感，要求评估高清修复 | 班德尔城动态 WebM 以独立 `research-candidate` 加入本地 catalog，并保留 WebM/无音频 MP4/poster 三件套；静态 Account 素材必须先做来源/许可、原始分辨率、构图保真和清晰度审查。AI 修复只允许非破坏性 sibling 输出，不能覆盖原图、伪造来源或在未通过人工审查前写入 adopted media manifest。 |
| RQ-148 | 2026-08-28 | 生效；纠正静态地区映射，不沿用错误猜测 | 用户复核指出 `ab3c2d4...-1920x726.jpg` 是暗影岛而非比尔吉沃特，`ef261f0...-1920x900.jpg` 是比尔吉沃特；同时确认其它暂定映射可继续复核 | 先将 `ab3c...` 记为暗影岛备选、`ef261...` 记为比尔吉沃特候选，以用户更正和官方 Universe JSON 索引优先；任何匿名图在用户最终检查前不得改名或进入 adopted manifest。以绪塔尔当前不分配静态候选。 |
| RQ-149 | 2026-08-28 | 生效；补齐以绪塔尔静态图，动态仍保持 pending | 用户新增 `1a75d072...-5000x2811.jpg` 作为以绪塔尔静态素材，并询问能否制作对应动态壁纸 | 以绪塔尔静态图加入本地 Account 候选；Portal 仍无现成动态文件，先做一次有界 first-frame-only preflight。正向 brief 只允许树叶/藤蔓、云雾、光束、悬浮晶体和远景雾层持续运动，锁定中心建筑/地形/构图，失败不盲目换模型或重绘。 |
| RQ-150 | 2026-08-28 | 生效；Ixtal 静态与动态职责分离 | 用户认可原创 Ixtal 静态概念方向，希望原 5000×2811 图用于制作动态壁纸 | 原 Ixtal splash 继续作为动态 first-frame source；原创 `ixtal-account-generated-v1.png` 仅作为 Account 静态候选。动态试验使用 first-frame-only，重点驱动叶片/藤蔓、云雾、光束、悬浮晶体和远景雾层的持续运动，锁定建筑/构图/色彩身份，先做 preflight 再决定是否调用模型。 |
| RQ-151 | 2026-08-28 | 生效；补齐虚空详细徽章的原创候选 | 用户确认旧 LoR 详细徽章资源中缺少虚空，希望自行绘制一枚匹配其它徽章的版本；随后指出首版过亮、二版过暗 | 以现有官方 Void crest 只作形状语义参考，生成一枚透明 RGBA 的深黑曜石/深靛材质徽章；当前平衡版保留可识别的低饱和紫色，仅作 selected-region hero 候选，不替换 13 个 Universe 小 crest，也不冒充 Riot 官方资产。 |
| RQ-152 | 2026-08-28 | 生效；优先用户提供的 Image2 Void 徽章方向 | 用户提供了一张 Image2 网页端生成的 Void 徽章，认为其结构和质感更接近目标；随后接受轻微降高光/降饱和处理，但要求保留可辨识紫色 | 将 `badge-void-image2-v1.png` 作为视觉主候选，`badge-void-image2-v2-muted.png` 作为低亮度 sibling；两者暂为 RGB 深色背景图，不冒充透明 badge。先做用户视觉裁决，再决定是否做真实透明 cutout 和 selected-region hero 接入。 |

| RQ-153 | 2026-08-29 | 生效；Ixtal Seedance 候选暂准使用但保留替换提醒 | 用户审看 Ixtal Seedance 2.5 候选后认为“差不多可以先用”，同时指出动效仍稍轻；这不是 Portal 母图强动效目标的最终视觉签字，也不是对来源/再分发权利的确认 | exactly-one-POST 的 10 秒 first-frame-only 候选通过当前研究候选门，可作为 Ixtal Portal 的可替换候选继续做布局/播放降级联调，但不得写入 adopted media manifest 或将 `production_media` 置为非零。待地区选择与 Portal/Account 真实布局完成后，保留显式回看点，由用户选择保留柔和版、做一次有界调优或替换更强官方动态源；不因“先用着”自动再次付费生成或覆盖本候选。 |

| RQ-154 | 2026-08-29 | 生效；两地区入口面板试水与扩大 MotionSites 研究范围 | 用户要求先做两个地区验证完整交互：选地区后立即切换对应背景，支持换区，点击进入账号页；同时强调 MotionSites 不能只看已保存样本，应继续广泛查找公开目录中的适配案例 | 在 `?surface=wallpaper-lab` 做隔离试水：Demacia/Bandle City 为 ready，其他 11 区保持 pending；地区通过 typed URL 传入 Account，Account 使用低干扰静态背景并在返回/继续时保留地区。MotionSites/React Bits/Motion 等只提供可迁移的层次、交互状态和动效参考，不整页照搬、不引入新依赖、不改变默认 `/`、production media 或来源/许可门。 |
| RQ-155 | 2026-08-29 | 生效；补充全量来源池复查与按消费者落地 | 用户提醒此前列出的资源远不止 MotionSites/React Bits，要求回看已整理的全部 source 并把合适内容用起来，同时本轮先专注 Portal/地区/Account，工作台各模块继续详细设计后再做 | 8E 当前 Portal slice 必须留下来源→机制→消费者的可审计映射：Riot/Universe 负责语义形状与 crest fallback；高级视觉目录负责构图/字阶/密度；动效库负责局部 spotlight、选择反馈、有限转场；OP.GG/电竞产品、Agent observability、Training 产品分别留给 Workbench、Trace、Training 消费者。不得因“广撒网”引入整库、重型引擎、付费 prompt、未经许可素材或假数据；采用机制必须可逆、可测试并保持 reduced-motion/移动端门。 |
| RQ-156 | 2026-08-29 | 生效；补齐历史 source 池的细粒度 provenance 与 Portal handoff 可恢复性 | 用户再次提醒不能只记住少数几个 source，要求回看额外整理的 Design Prompts、PPT/Photoshop、Radix/shadcn/ECharts、付费 UI 候选、Steam/League Displays 等，并把前端链路做完整 | 研究矩阵与采用 ledger 逐项记录这些来源的消费者、许可/预览状态和退出门：Design Prompts/PPT 只作 brief/展示工作流；Radix 保持行为基础，shadcn/ECharts/Tremor/visx 与 Tailwind Plus/Untitled UI deferred；壁纸来源单文件过来源/codec/移动端/loop 门。Portal 继续只用现有 CSS/React 机制；地区徽记对所有已有本地研究图渐进加载并回退 Universe crest。地区入口到 Account 的 URL 增加受限 `from=wallpaper-lab` 标记，刷新后仍能恢复“返回地区”语义；未知参数 fail-closed，production media 仍为 `0`。同批 UI hardening 同时固定显式 `surface=wallpaper-lab` 的地区 URL、旧 `?region=` 兼容别名、选区对当前 history entry 的 `replaceState` 同步、push/popstate 滚动复位、语义 main/skip focus、`aria` 状态、媒体 intrinsic 尺寸与 stale activation generation 防护；1000–1199px 短桌面使用三列卡片，<=420px 手机使用单列，长页面视觉层固定在 viewport。这些是可逆实现证据，不是 8E 完成声明。 |
| RQ-157 | 2026-08-29 | 生效；地区入口改为 13 区 Focus Rail，身份选择与动态媒体 readiness 分离 | 用户复核当前 Portal 后明确否决缩略图+13 宫格+右下角小 CTA，要求使用一排可滑动的简单金色地区图标，选中后在上方显示对应高细节徽章，并将大按钮放在轨道下方；随后纠正文案不能写“以某地区进入账号设置”，只写进入登录界面 | `RegionIconId` 作为 13 区 presentation identity，和 Riot API routing、动态视频 readiness 分离；13 区均可选择并同步 hero/URL/Account 背景，缺少动态 rendition 时使用 poster 而不禁用身份。Rail 使用现有 React/CSS/Motion、原生按钮、scroll-snap、左右键/Home/End/触摸和 reduced-motion；上方 hero 优先本地研究细徽章并回退 Universe crest；CTA 固定 `进入登录界面`/`Continue to sign in`，位于 rail 正下方且满足可读字号/触达面积。外部组件只借机制，不引新依赖、不碰 Workbench、不改变 `production_media=0`。 |
| RQ-158 | 2026-08-29 | 生效；Portal→Account 改为有因果的视觉交接并替换班德尔城 Account 静态图 | 用户要求当前 Portal→Account 切换更自然、更有范儿，并指定新找到的班德尔城静态图替换从动态壁纸截取的 Account 背景 | 点击 CTA 后按“selected hero/rail 收束→地区化 aperture 接管→Account 背景与登录内容进入”的有界 760–1000ms 转场实施，禁止突兀白闪、全屏 bloom 或无关几何爆炸；generation 防重入、Back/reload/focus 与 `from=wallpaper-lab` 继续可恢复，reduced-motion/Save-Data 立即提交并只做短交叉淡入。班德尔城使用 `4e498e9f..._fw1200webp.webp` 的本地无损 sibling，记录 SHA/尺寸/rights=unverified，正式发布前仍需更高分辨率与来源许可门。 |
| RQ-159 | 2026-08-29 | 已完成本地实现与回归；不表示 8E 或媒体采用完成 | 用户要求每个地区的主文案应有独立的地区气质，可受官方设定或代表性英雄语言启发；同时否决“动态素材、本地候选、分辨率/时长”等内部验收标注和人机式旁白，并要求登录交接继续自然化 | 为 13 区建立独立双语 presentation copy registry；当前句子均为 RiftCoach 自写氛围文案，不冒充 Riot/英雄逐字引语，未来只有在出处与使用边界核验后才标注官方引文。媒体 readiness 继续保留在 typed catalog、data attributes 与测试中，不进入正常用户文案。Portal handoff 使用 shared shell 的 `closing → background-handoff → idle` 状态、选中 rail 附近的有界 aperture、Account 分层进入和延迟 focus；reduced-motion 走即时提交。Workbench、Auth 语义、Riot routing、媒体权利和 `production_media=0` 均不变。 |
| RQ-160 | 2026-08-29 | 已完成本地实现、响应式视觉复核与回归；不表示 8E 完成 | 用户明确否决 Portal 继续保留“先选一处落脚点”等旧标题，也否决依赖容器宽度产生的随机折行；中文 Portal 必须固定为“从一方之地，”／“启程。”两行，Account 标题也要重新措辞并有意识地分行 | Portal 使用完整无障碍名称加显式视觉行：中文 `从一方之地，`／`启程。`，英文 `Begin from a region`／`of your choice.`；Account 改为中文 `选择一位`／`召唤师。`，英文 `Choose a`／`player.`。每行使用独立 block 与 nowrap，保留完整 heading accessible name；桌面和 390px 中英文 live DOM 均验证无标题溢出。该排版收口不改变地区选择、Auth、路由、Workbench、媒体权利或 `production_media=0`。 |
| RQ-161 | 2026-08-30 | 已完成本地实现与回归；不表示 8E 完成 | 用户指出 Account 页右侧表单整体偏低，并要求“查询区域”和“这是你的账号吗？”两个下拉控件与上方 Riot ID 输入使用一致、易读的字体；本轮继续只处理 Portal/Account UI hygiene，不碰 Workbench | 桌面 Account panel 使用不干扰 handoff transform 的 `top: clamp(-2rem, -2.5vh, -1rem)` 上移，移动端明确恢复 `top: 0`；三项原生控件统一 Manrope body 字体、560 字重、0.95rem，并将三条字段 caption 统一为 0.68rem/1.2。新增 E2E 断言桌面上移、移动端归零和 input/select computed typography 相等；focused unit `3/3`、完整 frontend unit `297/297`、E2E `50/50`、typecheck/build、live DOM 与治理门通过。未改变 Auth、路由、媒体采用、Workbench 或 `production_media=0`。 |
| RQ-162 | 2026-08-30 | 已完成本地实现与聚焦回归；不表示 8E 完成 | 用户要求接入其提供的官方虚空徽章，并指出 Portal/Account 右侧面板遮挡背景、Account 背景过暗；要求只做局部视觉修补，不扩展 Workbench | 将用户提供的 ARGB WebP（400×425，SHA-256 前缀 `2F82BC…`）接入 Void 详细徽章映射，Universe crest 仍保留为加载失败回退；Portal atlas 与 Account panel 降低不透明度并收窄 backdrop blur，Account 地区背景滤镜/氛围层提亮以恢复场景可读性。聚焦 badge unit `2/2`、相关 E2E `2/2`、typecheck/build、Impeccable detector 与治理门通过；未改变 Auth、路由、Workbench、媒体采用或 `production_media=0`。 |
| RQ-163 | 2026-08-31 | Agent 主线交接批已完成文档验收；不表示 8E/8F 完成 | 用户确认 Portal/Account 当前已“差不多”，要求把执行重心转回 Agent 主线，并先把前端之前的 Agent 讨论、README 任务和 8E 后续重新梳理清楚；明确不继续地区扩展、不擅自改 Workbench | 新增 Agent 主线交接计划与八维学习材料；README 改为如实说明 8A–8D 完成、8E 进行中、8F 待后续、GLM-5.3 尚未准入、Web 尚无受限 Review Coach 追问；canonical/活动计划/路线镜像把下一执行候选交接为 G53-0 无 I/O 审计。旧 RQ-154 两地区/第三地区动作由 RQ-157–162 supersede；本批不实现 Provider/Coach、不读取 Secret、不做真实外部调用，Workbench、Auth、媒体采用和 `production_media=0` 不变。 |
| RQ-164 | 2026-08-31 | G53-0 本地审计完成，但 GLM-5.3 采用保持 blocked/deferred；不表示 Provider 或领域准入 | 用户确认开始交回 Agent 主线后的第一批；要求先核对 GLM-5.3 的真实可用性边界，不要未经证据直接调用或切换默认模型 | 只审计仓库代码、模板和脱敏配置元数据：产品默认仍为 `zhipu`/`glm-5.2`，现有 Zhipu Adapter 固定 `thinking=disabled`；本机 `.env` 的非敏感 provider/endpoint/model 与 loader 合同不一致，账号类型、权限、正式 GLM-5.3 model ID 和 region 仍未知。未输出或记录 Key 值、未调用 Provider/Riot/OP.GG、未改 `app/`/`web/` 或默认配置。后续只有在获得可核验的非敏感账户信息后，才另行进入 G53-1 离线 profile TDD。 |
| RQ-165 | 2026-08-31 | 生效；G53-1 普通 API 适配档案离线 TDD 完成；不表示 exact-SHA CI、真实协议、领域准入或默认切换 | 用户核对官方资料并明确使用智谱开放平台普通 API，而不是 Coding Plan；要求开始第一批 GLM-5.3 适配工作，保留旧模型与项目边界 | 以官方 `glm-5.3-flash` 文档和普通 Chat Completions 端点为公开协议依据；新增按模型精确解析的不可变 Zhipu thinking profile，保留 GLM-5.2 disabled，增加 GLM-5.3/Flash enabled + low；Provider、受控 probe 和 CLI 共享档案，Flash reasoning 不外泄，工具回合不可回放时 fail closed，已知模型结果文件隔离。聚焦回归 `70 passed, 29 subtests passed`、compileall、diff/governance 通过；未读取/输出 Key、未真实调用、未改 `.env`、默认模型、Workbench、Auth、路由或 `production_media=0`。下一步为 G53-2 exact-SHA CI。 |
| RQ-166 | 2026-08-31 | 已完成；G53-2 exact-SHA 公共 CI 通过；不表示真实协议、领域准入或默认切换 | 用户明确继续 G53-2，要求把 G53-1 的隔离实现冻结在精确提交上，并以现有公共 workflow 验证，不扩大到前端、Workbench 或真实 API | 仅提交 9 个 G53 Provider/配置/probe/CLI/测试文件：`0f97b92683e4981842e745a695864deb611bb630`；Actions run `33325222755` 的 `pytest`、`postgres-migrations`、`packaging-smoke` 三 job 均成功，公共 pytest 为 `1912 passed, 145 skipped, 1 warning, 127 subtests passed`。未改 workflow、`.env`、默认 `zhipu`/`glm-5.2`、Workbench、Auth、路由或 `production_media=0`，未读取/输出 Key、未真实调用 Provider/Riot/OP.GG。下一步仅登记需用户单独授权的 G53-3（最多三次真实协议调用），G53-4 与完整 8E/8F 仍开放。 |
| RQ-167 | 2026-08-31 | G53-3 首次有界协议尝试未通过；因 `authentication_failed` 阻塞，不表示真实协议或领域准入 | 用户明确“继续”，授权普通 API 协议门最多三次；首个请求失败后要求停止，不重试、不盲目消耗剩余预算 | 进程级临时使用 `zhipu` + 普通 API + `glm-5.3-flash`，未修改 `.env` 或默认模型。A1 结构化合同第 1 次调用返回脱敏 `authentication_failed`，A2 按 runner 合同跳过；结果 `calls_used=1/3`、`admitted=false`，脱敏结果文件 SHA-256 为 `b10827f18dc810085a0d3883ebb7175709f4c244c30c937d5d220ab1ec1d0d9a`，schema 校验与聚焦回归 `36 passed`。未保存响应正文、reasoning 或 Key；错误码不能区分 Key 无效、权限不足或账户/端点接缝错误。下一步先由用户确认/修正凭证接缝，再另行决定是否重开同一 G53-3；不进入 G53-4。 |
| RQ-168 | 2026-08-31 | G53-3 重开仍未通过；旧 Key 已确认被删除，普通 API 端点预检无误后仍失败 | 用户确认普通 API Key 类型并明确“重开”；随后确认旧 Key 已删除 | 进程级临时使用普通端点 `https://open.bigmodel.cn/api/paas/v4/`、`zhipu`、`glm-5.3-flash`，未覆盖前次结果。预检确认 Key 存在且只记录非敏感格式；A1 第 1 次调用仍为脱敏 `authentication_failed`，A2 跳过，`calls_used=1/3`、`admitted=false`，结果 `zhipu_glm53_flash_adapter_protocol_retry1.json` 的 SHA-256 为 `dde918b17f8f93914ccf8e330fd96e936699f5fa8313c30dcb6d69f5ae19e66c`。不把该失败解释为模型未开放，不进入 G53-4；等待新 Key。 |
| RQ-169 | 2026-08-31 | G53-3 重开通过；普通 API Key 与 `glm-5.3-flash` 结构化/工具协议接缝已取得有界证据，但不表示领域准入或生产成熟度 | 用户在普通 API Keys 页面创建新 Key，更新 `.env` 后明确“改好了” | 预检确认 `LLM_PROVIDER=zhipu`、普通端点 `https://open.bigmodel.cn/api/paas/v4/`、`LLM_MODEL=glm-5.3-flash`，Key 值不输出，client 重试为 0。A1 结构化合同 1/1 通过；A2 Agent 工具往返 2/2 通过（1 次 ToolCall/执行）；总计 `calls_used=3/3`、`admitted=true`。脱敏结果 `zhipu_glm53_flash_adapter_protocol_retry2.json` SHA-256 为 `1273eab75d4e4b1357a555db3c7c4472c85797daaf48006b34b986380a06a65a`，schema 与聚焦回归 `36 passed` 通过。未保存正文、reasoning、Key 或完整请求标识；G53-4 需单独授权。 |

| RQ-170 | 2026-08-31 | 已完成一次本地真实执行但 G53-4 不准入；不表示模型一般质量或生产成熟度 | 用户明确授权在 G53-3 通过后执行一次 GLM-5.3-Flash 新鲜领域采用门 | 以独立匿名三案例、Dataset/Input Plan/Prompt-Context snapshot 和 no-I/O preflight 固定输入；真实门首案第 1 次响应触发 `unsupported_parallel_tool_calls`，Adapter fail closed，领域调用 `1/12`、规范化 Token `0/12000`，后两案按首错跳过；不可变结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_domain_adoption_v1.json` SHA-256 `ae4c54f421bd716f14d01e0fbf32a020f93b313d111b2ddb1832773ad53b7f45`；累计含 G53-3 为 `4/15` calls、`1115` tokens，费用 `unknown`。不重试、不放宽合同、不切换默认 `glm-5.2`；新 runner/资产尚无 exact-SHA 公共 CI，Workbench/Auth/前端/production_media 不变。 |

| RQ-171 | 2026-08-31 | 生效；GLM-5.3-Flash 适配器合同修复已完成本地实现，新的 G53-5 全能力真实验证待执行；不表示领域或生产准入 | 用户明确要求对已开放的 GLM-5.3-Flash 尽可能全范围测试，并先修复适配器在真实工具回合中的不兼容，而不是把旧 G53-4 首错直接归因于模型 | Flash profile 更新为 `thinking=enabled`、`reasoning_effort=max`、`clear_thinking=false`；中立 `reasoning_content` 仅供内部回放并计入上下文，不进入公开投影；Zhipu Adapter 接受多个 ToolCall 并保持原始顺序，由 AgentLoop 逐个受控执行，能力声明仍不谎称并发。旧 G53-4 结果和考卷保持不可变，G53-5 必须使用新的唯一输入/输出身份与有界预算。当前仅有离线实现/回归证据，真实 Provider 全范围测试为 pending；不改默认模型、`.env`、Workbench、Auth、前端或 `production_media=0`。 |

| RQ-172 | 2026-08-31 | 已完成 G53-5 本地真实矩阵观察但不准入生产；不表示 Stage 8/8E 完成 | 用户要求在适配器修复后尽可能发挥并全面验证 GLM-5.3-Flash，并据结果决定是否回到 Agent 主线下一项 | 新实验 `g53-5-fresh-flash-capability-matrix-v1` 在 dirty worktree、HEAD 与 `origin/main` 同为 `0f97b92683e4981842e745a695864deb611bb630` 的条件下执行 11/11 次调用，共 46,151 tokens；8 个案例中 7 个通过。adapter_core、AgentLoop、多 ToolCall/思考回放、domain development、vendor text stream 与 vendor multimodal 均观察通过；F7 vendor tool_stream 在 `max_tokens=512` 返回 `incomplete_chat_response`/`length`，不足以证伪能力；F4 cache 观察为 `cached_input_tokens=0`、`cache_status=unproven`，不能宣称缓存命中；F8 仅为 vendor_raw 观察，不进入中立生产合同。结果文件 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_g53_5_capability_matrix_v1.json` SHA-256 `BFFF564CF4C6E7B2DD05F88542FD7A872D1565442B6D35C795EC6892CC84BE0C`，`production_admitted=false`、`public_ci_confirmed=false`。下一步等待用户决定/Agent 主线下一项；不改默认模型、Workbench、Auth、前端、`production_media=0`，不把本地真实观察写成公共生产成熟度。 |

| RQ-173 | 2026-08-31 | 已完成独立 F7 工具流上限诊断但不准入；不表示 provider-neutral streaming、Agent 生产、领域采用或公共 CI | 用户要求继续核对 G53-5 的 F7 `tool_stream` 截断边界，并明确只诊断原 `max_tokens=512` 的 `length` 结果 | 独立实验 `49ddb2504c08d3d066366d53011a8185d0e5c5aa698138cd1b949e58a3de191b` 仅将 `max_tokens` 从 512 调至 2048，执行唯一 `1/1` 次真实调用、`557` tokens；`finish_reason=tool_calls`、1 个 ToolCall，reasoning 372 chunks、tool 15 chunks，source identity stable，`cached=0`。结果文件 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_g53_5_tool_stream_followup_v1.json` SHA-256 `105722b2af2a4cbccc1b45a29b67a0864545aeeebb18f815ae7b62d6ace1d1a56`，绑定父矩阵 experiment `4e2d14f9e2b294ec2898b22a4275dbbd706c28ca7f3b061a655d1a613a7aaefb`、父结果 SHA `bfff564cf4c6e7b2dd05f88542fd7a872d1565442b6d35c795ec6892cc84be0c`；结果为 `production_admitted=false`、`public_ci_confirmed=false`、`vendor_raw_transport_only`。该 follow-up 只用于诊断 512→2048 的上限变化，不把 vendor raw 传输观察写成中立 streaming/Agent/领域能力；Stage 8/8E 继续 `in_progress`，下一步等待用户决定 Agent 主线下一项，不改默认模型、Workbench、Auth、前端或 `production_media=0`。 |

| RQ-174 | 2026-08-31 | G53-6 正式领域采用门已执行但拒绝；不表示模型一般质量或生产成熟度 | 用户明确要求在 G53-5/RQ-173 后执行正式 GLM-5.3-Flash 领域采用门，并要求保留两次不可变结果、不要覆盖旧 G53-4 | 同一冻结 admission identity `4266388ef8ad2083cd59eacfd2c41364b151f286f6cd189334dacb4cb121bd10` 下保留两份结果：`data/evaluation/results/provider_capabilities/zhipu_glm53_flash_domain_adoption_g53_6_max_replay_v1.json`（SHA-256 `48d22c53f9231f3c03038d5047b8abf653450164e1f56bf2a08c90c9f48114ae`）使用 `glm-5.3-flash-enabled-max-replay` 与旧 `max_tokens=512` 上限，首案 `1/12` calls 以 `provider_response_invalid/incomplete_chat_response` 停止；随后仅将默认输出上限修正为 1024 并补传 `top_p`，保留 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_domain_adoption_g53_6_max_replay_1024_v1.json`（SHA-256 `7af819999f4e40810eacf925bcda8a2330cc8baf0e5ca763c84e6f43b58efc96`），首案累计 `2/12` calls、`2925` domain tokens 后因当前 30 秒 Skill deadline 返回 `provider_timeout/timeout`，后两案跳过。两份结果均 `admitted=false`、`production_admitted=false`，不覆盖旧 G53-4；未产生新的领域/生产准入或公共 CI 结论。Stage 8/8E 继续 `in_progress`，下一步等待用户决定 Agent 主线任务；不无授权重试、不改默认模型、Workbench、Auth、前端或 `production_media=0`。 |

| RQ-175 | 2026-08-31 | 生效；GLM-5.3-Flash 专属运行时档案离线实现完成；不表示 G53-7 领域准入或生产切换 | 用户质疑旧领域门的 30 秒执行截止与 512/1024 输出上限是否误伤 Flash，并明确要求为确定使用的 `glm-5.3-flash` 做专属适配 | 新增受信、版本化 `ModelRuntimeProfile`，仅对精确 `zhipu/glm-5.3-flash` 注入 Agent 90 秒、`llm.chat` 90 秒、传输 120 秒、`max_tokens=2048`、`temperature=1`、`top_p=0.95`；档案贯穿 Agent 编译、AgentLoop、`llm.chat`、G53 领域预算包装器和 Provider 构造，调用方不能升权，普通模型/无档案路径保留旧预算。旧 G53-4/G53-6 JSON 只读兼容，新增 profile 身份/元数据/摘要；真实领域运行要求新实现取得 exact-SHA 公共 CI、在新 SHA 上重新取得 G53-3 协议证据且拒绝 dirty worktree。聚焦新增/回归 `98 passed, 27 subtests passed`，compileall、diff/governance 通过；本批未真实调用、未改默认模型、`.env`、Portal、Account、Workbench、Auth、路由或 `production_media=0`。旧数据集的 30 秒仍是质量资源阈值而非新档案的 Provider 执行截止；若要调整它，另建版本化 Dataset/Plan，不改写旧证据。 |
| RQ-176 | 2026-08-31 | 生效；用户已授权 Flash-only 产品运行时晋级，本地接线完成；公共 CI、同 SHA G53-3、G53-7 领域门与生产准入仍待完成 | 用户明确“继续”，并选择不再等待 Pro/Flash 比较，要求把已完成的 Flash 专属适配推进到产品 Agent 主线，但保持 GLM-5.2 可显式回退、Portal/Account/Workbench 不变 | `ModelRuntimeProfile` 已从产品组合根贯通至 Worker、RuntimeExecutionFactory、Agent compiler/Loop、Harness `llm.chat`、Zhipu Provider、Runtime policy 与 Trace identity；Flash 固定 90 秒执行窗、120 秒传输、2048 输出上限、`temperature=1`、`top_p=0.95`、`max_retries=0`，Skill 30 秒质量门单独保留。产品 Worker 只接受已登记的 GLM-5.2/Flash，Flash 要求普通 API 标准基址、concrete profile 绑定，并将 lease/heartbeat 默认调为 360/60 秒（少于 300 秒拒绝）；`.env.example`/Compose 默认改为 Flash。当前本地聚焦回归通过，但工作树 dirty，不能复用旧 `0f97…` G53-3 证据；下一顺序是用户批准干净提交→新 SHA 公共 CI→同 SHA G53-3→独立 G53-7/完整黄金切片→安全部署合规/8F。未修改 Portal、Account、Workbench、Auth、路由或 `production_media=0`。 |
| RQ-177 | 2026-08-31 | 已完成；在 Flash 运行时实现 SHA 上取得新的 G53-3 有界协议证据；不表示 G53-7 领域准入或生产成熟度 | 用户在实现提交公共 CI 通过后要求继续，授权重取同一实现 SHA 的协议门 | 先修正协议探针：精确 `zhipu/glm-5.3-flash` 现在把已登记的 90 秒 Agent/工具窗、120 秒传输窗、2048 输出上限、`temperature=1` 与 `top_p=0.95` 传到结构化请求和 Agent 工具回合；旧模型无档案路径保持旧值。随后在干净实现提交 `f0d5ee270f9dac8137368239b85471eca3edf570` 上执行严格 `3/3` 次真实调用：A1 结构化合同 `1/1`、A2 Agent 工具往返 `2/2`，均通过，`admitted=true`，总计 `1400` tokens。脱敏结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_adapter_protocol_f0d5ee2.json` 的提交/canonical LF SHA-256 为 `1fda5b03d74514fe59c835e5783ff66bb4f16355f32c2adcf82a069bcf70984c`；Windows CRLF 工作副本原始摘要为 `6c6e552a1047942b2afce18a01d8adaa86e444615decaeba258e2abe18ae80ea`。独立证据提交 `407ee7559c46a84e82f81d5f43f435ad89013949` 的 Actions run `33373561017` 三 job 全部成功；实现提交的 Actions run `33372880364` 亦全部成功。结果未保存 Key、Prompt、响应正文、reasoning 或原始请求标识，旧 G53-3/G53-4/G53-6 证据未覆盖。下一步先设计并测试 G53-7 的实现提交 A/证据提交 B 身份绑定，不能把证据提交 SHA 冒充协议执行代码，也不直接绕过 dirty-worktree 与公共 CI 门。 |
| RQ-178 | 2026-08-31 | 已完成本地 A/B 身份绑定与无 I/O 预检实现；不表示 G53-7 领域准入或生产成熟度 | 用户明确“继续”，授权先解决 G53-7 中实现提交 A、协议代码 SHA 与证据提交 B 的自引用问题，再决定是否进入领域门 | 新增严格 `GLM53ABIdentityBinding`：实现 SHA/实现 CI 与协议 `code_sha` 必须同为 A，脱敏协议证据及其声明路径必须来自直接子提交 B，B 的公共 CI 另行记录；预检读取 B 的 Git blob、以仓库规范 LF 形式校验摘要、检查 A→B 直接父子关系、B 只新增声明证据文件、工作树内容一致性、证据路径白名单和当前 HEAD=B，schema 1.1 admission 才能携带绑定，历史 schema 1.0 结果仍可读。新增离线错配/篡改/路径/缺确认测试，相关聚焦 `53 passed`（身份绑定文件 `18 passed`）；未调用领域 API、未读取/输出 Key，Portal、Account、Workbench、Auth、默认模型和 `production_media=0` 不变。由于本轮工具代码尚在当前 dirty 工作树，不能直接把现有 `f0d5ee2→407ee` 配对当作下一次 G53-7 的最终身份；下一步应先冻结本轮实现为新的 A′、取得 A′ exact-SHA 公共 CI，再在 A′ 重取 G53-3 并只新增证据 B′，随后才可做 G53-7。协议 JSON 的 `6c6e…` 是 Windows CRLF 工作副本摘要，提交/跨平台 canonical LF 摘要为 `1fda5b…`，两者不混用。 |
| RQ-179 | 2026-08-31 | 已完成；G53-7 最终实现提交 A 已取得 exact-SHA 公共 CI；不表示新 G53-3、证据 B、领域准入或生产成熟度 | 用户继续当前 canonical 下一项，授权冻结 RQ-178 身份实现并取得精确提交公共验证，不提前运行领域门 | 最终实现 A 为 `9e6d78be51c3a5c512b67f83d2849f9b1261cf77`；Actions run `33378687984` 的 `pytest`、`postgres-migrations`、`packaging-smoke` 三 job 全绿，`head_sha` 精确等于 A。候选 `fe7d577…` 的测试把历史 B 当当前 HEAD，候选 `3ccd827…` 的公共浅克隆又缺少历史 A/B；失败 runs `33377864183`/`33378168043` 均保留。最终 workflow 为身份测试提供完整 Git 历史，生产 `HEAD=B` 校验没有放宽；本地相关回归 `53 passed`、governance/diff 通过。下一项仅从干净 A checkout 重取 G53-3，再由直接子提交 B 只新增新协议 JSON；本条没有模型 API 调用，不改 Portal、Account、Workbench、Auth、路由或 `production_media=0`。 |

| RQ-180 | 2026-08-31 | 已完成一次 G53-7 真实领域尝试但未准入；不表示模型一般质量、账号失败或生产成熟度 | 用户在 RQ-179 的 A 公共 CI、同 SHA G53-3 与 B 公共 CI 完成后连续授权“继续/授权”，要求执行 G53-7 | 在干净 LF checkout 的实现 A=`9e6d78be51c3a5c512b67f83d2849f9b1261cf77`、证据 B=`7cb66d218389c0e7d7aa7b2b1969a4678402f857` 上只执行一次真实尝试；协议 3/3 加领域 2/12，累计 5/15 calls、领域 3505 tokens。首例 `flash_gate_baseline_01` 经 2 次请求后以适配器安全聚合码 `provider_response_invalid` / `incomplete_chat_response` 失败，后两例按首错停止跳过，`admitted=false`。结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_domain_adoption_g53_7_runtime_profile_v1.json` 的 canonical-LF SHA-256 为 `21e664d57d53bfc48ad9e109be48a999f52e25a0060821d711ae915002484426`，experiment 为 `236525300ed9c432a9ad2ffcfdcd298168666676076e5efcb3ce4129a7cee2e0`，随后由本地承载提交 C=`9157cde9904677a352ba7792f170b8765f7fea83` 仅新增该脱敏结果；C 未推送且未取得公共 CI。底层供应商结束原因、正文、reasoning、Key 均未保存，不能把安全聚合码进一步断言为 `length`。旧 G53-3/G53-4/G53-5/G53-6 证据不覆盖；Stage 8/8E 仍 `in_progress`，`production_media=0`，Portal、Account、Workbench、Auth、路由和默认产品接线不变。当前停止自动重试，若要继续须另立版本化的 Flash 响应完成/截断诊断并取得新授权。 |

| RQ-181 | 2026-08-31 | 已完成一次独立 Flash 响应完成度诊断；不表示 G53-7 领域准入或生产成熟度 | 用户在 RQ-180 结果无法区分底层结束原因后授权执行一次正文零留存诊断，以确认 `incomplete_chat_response` 的具体路径 | 在产品实现基线 `7cb66d218389c0e7d7aa7b2b1969a4678402f857` 上，以独立诊断代码 `447c11e85b6da53fe678d68e25d96b589c0d6ca2` 只执行冻结首例 `flash_gate_baseline_01` 一次；供应商调用 `1/4`、SDK 不重试。首个 `agent_initial` 回合收到有效 Usage（input `2220`、output `2048`），原始 `finish_reason=length`，正文为空、reasoning 非空、ToolCall 为 0；现有适配器按设计抛出 `incomplete_chat_response`，`normalized=0/1`、`settled=0/1`，Agent 为 `failed/provider_error`。脱敏结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_response_completion_diagnostic_v1.json` 的 canonical-LF SHA-256 为 `050df3fc7afb2c2dc4e99fd2e731f8d9e6133d2806c65171f2dcdbd30834a000`，experiment=`b1e4a1fc51bed23803b5f94acbd2a652330d5847061dbb7b60022c88da4ff1b9`，由本地提交 `baa9cc756ff9e3dfc5eac19119315b7f9f0b56da` 承载，未推送、未取得公共 CI。该结果只确认本次“最大推理档案先耗尽 2048 输出额度”的失败路径，不把 RQ-180 的旧第二回合改写为同一原因；不保存 Prompt、正文、reasoning、Key、原始请求 ID 或工具参数。下一步先设计版本化响应完成策略并补离线 TDD，是否进入实现另待授权；不改 Dataset/Plan、默认模型、Portal、Account、Workbench、Auth、路由或 `production_media=0`。 |

| RQ-182 | 2026-08-31 | 已完成本地；版本化响应完成策略与离线 TDD 已落地，不表示恢复能力、领域准入或生产成熟度 | 用户在 RQ-181 后明确“继续下一步”，授权进入 canonical 指定的响应完成策略设计与离线实现；本批不得未经新授权发真实请求或扩大 Workbench/前端范围 | 新增不可变 `ResponseCompletionPolicy`、脱敏 `ResponseBoundarySnapshot` 与受信 `ResponseRequestContext`。当前唯一注册的 Flash 严格策略绑定 `zhipu/glm-5.3-flash` 与 `glm-5.3-flash-runtime-v1/1.0.0`，保持 `max_output_tokens=2048`、额外调用 `0`，对完整正文、工具回合、截断和非法组合给出可审计判定；另有 `activation_state=candidate` 的 8192/最多一次 fresh-recovery 候选，仅用于离线白名单测试，解析器不会返回且不发请求。聚焦 `41 passed`，编译、治理与差异检查通过；不改 `ChatResponse`、`LLMProvider`、AgentLoop、ToolRuntime、Trace、预算账本、Dataset/Plan、默认模型、Portal、Account、Workbench、Auth、路由或 `production_media=0`。后续若要启用候选，必须另建 runtime/attempt/预算/Trace 合同、取得 exact-SHA CI 与新鲜协议证据，并单独授权真实诊断。 |
| RQ-183 | 2026-08-31 | 已完成本地；候选 runtime/attempt/预算/Trace 合同已落地，不表示候选已启用、领域准入或生产成熟度 | 用户明确继续 RQ-182 所列的唯一下一项，授权先把候选恢复的运行时身份、每次底层调用计数、预算预留/结算和脱敏 Trace 定义清楚；本批仍不得直接发真实请求 | 新增 `ResponseRecoveryRuntimeProfile`（精确绑定 `zhipu/glm-5.3-flash` 的未注册 `glm-5.3-flash-runtime-v2-candidate/2.0.0`）、`primary`/`fresh_recovery` 两个有序尝试槽位、最多一次额外调用的 `ResponseRecoveryBudget` 与 `ResponseRecoveryLedger`，以及独立 schema 1.0 的 `ResponseRecoveryTrace`。账本每次预留/结算只允许一次，失败、Usage 缺失、单次或累计超限均消耗槽位并 fail closed；判定由候选策略重算，不能伪造 eligibility；合同和 Trace 不接受 Prompt、正文、reasoning 原文、工具参数、Key 或 request ID。聚焦 `30 passed`，相邻响应策略/Flash runtime/Runtime/Observed Provider/领域门 `128 passed`，compileall、diff/governance 通过；没有 Provider/SDK/网络调用，不改统一 `RuntimeTrace`、AgentLoop、ToolRuntime、默认模型、Portal、Account、Workbench、Auth、路由或 `production_media=0`。后续仍需候选 exact-SHA 公共 CI、同 SHA G53-3、单独真实诊断授权及成本/延迟审查。 |

| RQ-184 | 2026-08-31 | 已完成公共证据链；候选仍未启用、未进入领域准入或生产成熟度 | 用户明确“继续”，授权完成 RQ-183 候选合同的 exact-SHA 公共 CI，并在同一实现 SHA 重取 G53-3；本批到此为止，不自动执行 fresh-recovery 或 G53-7 | 实现提交 A=`e25c3579e8c37724b76505ad028e066a7e28e654`，Actions run `33405110692` 的 `pytest`、`packaging-smoke`、`postgres-migrations` 三 job 全部成功；从同一 A 干净 checkout 执行 G53-3，严格 `3/3` 次调用，A1 结构化合同 `1/1`、A2 Agent 工具往返 `2/2`，`admitted=true`、SDK retries `0`。脱敏结果写入 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_adapter_protocol_rq183_candidate_v1.json`，其 `code_sha` 为 A；只新增该结果的直接子提交 B=`eca01ce1393286dbbe83992c2985f600ea2b30b0`，Actions run `33405881172` 三 job 全部成功。A/B 无 I/O 身份预检通过，结果文件 canonical-LF SHA-256=`275e3a091a37dc12604143e6890f0ce899fb3d9007cef8c2aa46a51bdb9c8e72`；结果与合同均不保存 Prompt、正文、reasoning、工具参数、Key 或 request ID。该证据只证明候选合同的公共可复现性与同 SHA 协议接缝，不激活 `glm-5.3-flash-runtime-v2-candidate/2.0.0`，不改变严格 Flash v1 的 2048/零额外调用、默认模型、AgentLoop、RuntimeTrace、Portal、Account、Workbench、Auth、路由或 `production_media=0`；下一步需另行授权一次有界候选恢复诊断，并审查成本、延迟、失败与脱敏 Trace。 |

| RQ-185 | 2026-08-31 | 已完成两次有界候选恢复诊断启动但未形成供应商结果；候选仍未启用、未进入领域准入或生产成熟度 | 用户在 RQ-184 后明确“继续”，授权重开一次候选恢复诊断；第一次无响应后再次明确继续，本条记录第二次有界启动 | 隔离诊断代码提交 `76de589a128b7a71f1def3316da3f30ebdd3a4c8`，实现基线为候选证据提交 `eca01ce1393286dbbe83992c2985f600ea2b30b0`。两次独立启动都只进入 `primary` 首回合，SDK `max_retries=0`，没有发送 `fresh_recovery`：首次沿用候选合同的 120 秒传输边界，调用方在约 60 秒无返回时中止；第二次改用全新结果名并临时将客户端传输上限收窄为 20 秒，仍在约 60 秒内未结束后终止。两次均无可观察响应、Usage、finish reason、Trace 或结果 JSON，不能推断请求是否抵达供应商，费用/计费状态为 `unknown`。未保存 Key、Prompt、正文、reasoning、工具参数或原始 request ID；严格 Flash v1 仍 2048/零额外调用，候选保持 `activation_state=candidate`、`execution_allowed=false`。本条不改变默认模型、AgentLoop、RuntimeTrace、Portal、Account、Workbench、Auth、路由或 `production_media=0`；下一步改为传输/代理边界复核，需新的明确授权，不自动重试或进入 G53-7。 |

| RQ-186 | 2026-09-01 | 已完成请求级截止修复及一次有界真实诊断；候选仍未启用、未进入领域准入或生产成熟度 | 用户要求不要继续卡住并明确继续 canonical 的诊断复核；本条只授权定位 RQ-185 截止未生效的原因、修复隔离诊断器并执行一次新结果名的有界请求 | 根因是 `ZhipuProvider` 将 `ChatRequest.timeout_s=90` 放入每次 SDK 调用，覆盖先前仅在客户端层设置的 20 秒。隔离诊断器新增并校验 `--request-timeout-s`，让 primary 与唯一可能的 fresh-recovery 同受请求级硬截止约束；聚焦及相邻测试 `82 passed`、compileall 与 diff check 通过，诊断代码提交 `94629161c5d3230629210444b5a1a38212799997`。随后在实现基线 `eca01ce1393286dbbe83992c2985f600ea2b30b0` 上只发出 1 次 primary，payload 明确为 8192 输出上限、30 秒 timeout、SDK retries 0；约 30.141 秒后以 `sdk_error_class=timeout` / `adapter_error_stage=transport` 安全关闭，无供应商响应、Usage、finish reason、request ID 或 fresh-recovery，费用状态 `unknown`。脱敏结果由本地提交 `a7874b0` 承载，路径 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_response_recovery_diagnostic_rq186_request_deadline_v1.json`，canonical-LF SHA-256=`0a0b6d058badf3d5001369cef9c4a66a582f0837bd1d645655555196ca8b324c`。30 秒低于候选 90 秒 Agent 窗口，因此这不是模型能力拒绝；候选仍 `candidate` / `execution_allowed=false`，严格 Flash v1、默认模型、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一项为候选延迟预算裁决，未经新授权不自动重试或进入 G53-7。 |
| RQ-187 | 2026-09-01 | 已完成一次完整候选窗口诊断但未形成供应商响应；候选仍未启用、未进入领域准入或生产成熟度 | 用户明确“继续”，授权执行 RQ-186 之后唯一的完整候选窗口复核；本条只执行一次 90 秒请求级 primary，不自动扩大调用范围 | 在同一隔离诊断代码 `94629161c5d3230629210444b5a1a38212799997` 与实现基线 `eca01ce1393286dbbe83992c2985f600ea2b30b0` 上，payload 为 `max_tokens=8192`、`timeout_s=90`、SDK `max_retries=0`。唯一 primary 在 90.188 秒后以 `sdk_error_class=timeout` / `adapter_error_stage=transport` 安全关闭；无响应、Usage、finish reason、request ID 或 `fresh_recovery`，`provider_calls_attempted=1`、`candidate_eligible_observed=false`、`terminal_state=fail_closed`、`budget_exceeded=true`（截止后的计时结算）、费用状态 `unknown`。脱敏结果由本地提交 `50ce5be` 承载，路径 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_response_recovery_diagnostic_rq187_full_window_v1.json`，canonical-LF SHA-256=`3d8d4744da3286b921d894684bfffcbf19d56d2c945821703ae1d4282fd80263`。该结果排除“30 秒过短”，但不能仅凭无响应区分代理/连接/读取与服务端生成延迟，也不构成模型能力失败；候选仍 `candidate` / `execution_allowed=false`，严格 Flash v1、默认模型、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一项为候选传输/生成路径拆分诊断，未经新授权不自动重试或进入 G53-7。 |
| RQ-188 | 2026-09-01 | 已完成一次有界传输/生成路径拆分诊断；候选仍未启用、未进入领域准入或生产成熟度 | 用户新授权可以连续推进一批候选诊断；本条固定最多 3 次真实调用，不重跑 RQ-187，不改产品默认或 Workbench | 隔离诊断代码与运行时 source identity 均为 `b67b4500ebdbff934e470fd92c1461184aa7c49b`；SDK `max_retries=0`，依次执行合法 `thinking=enabled`/`reasoning_effort=low` 的 16 token 最小控制、冻结上下文 256 token `max` 同步请求、冻结上下文 8192 token `max` 流式首块请求。三路均 `observed`：前两路有效 Usage 且 `finish_reason=length`、正文为空、reasoning 非空；流式路约 `687ms` 观察到首个 `delta_reasoning` chunk 后按探针合同关闭。共 3 calls、输入 `1993`、输出 `272`、缓存输入 `1920`、总计 `2265` tokens、累计延迟 `17172ms`。正式结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_transport_generation_split_diagnostic_rq188_final_v1.json` 的 experiment 为 `41901515decc6d8768abd56ee3fd49ac1d1a4402f3cc1cef497720995fa80c8e`，canonical-LF SHA-256=`60073a5f0d0d0324d0fe4deb588d4a49becc607ebfe6b1d008bf04d60a2faf51`；首次 disabled-thinking 控制和带代码 SHA 输入笔误的中间结果均保留为不可变审计文件，不作正式证据。该结果只确认普通 API 的 Flash endpoint/model 路径可达并已开始生成，提示小额度同步请求会先耗尽 reasoning；不证明完整流式合同、长请求根因、模型一般质量、领域采用或生产成熟度。严格 Flash v1 仍为 2048/零额外调用，候选保持 `activation_state=candidate`、`execution_allowed=false`；下一项为 evaluation-only 的 `candidate-output-budget-calibration`，比较合法推理档位与可见正文完成度。 |
| RQ-189 | 2026-09-01 | 已完成三路独立输出额度/推理档位校准；候选仍未启用、未进入领域准入或生产成熟度 | 用户授予连续推进授权；本批固定同一冻结上下文和采样参数，分别执行 `low+2048`、`low+8192`、`max+8192` 各一次，SDK `max_retries=0`；不改产品默认、不注册候选、不改 Workbench | `low+2048` 一次调用 `28.344s` 返回 `finish_reason=stop`，正文/ reasoning 均非空，Usage 输入 `1973`、输出 `724`；`low+8192` 在 `45.594s` 请求截止内无响应；`max+8192` 在 `45.500s` 请求截止内无响应。后两路无 Usage、正文或 request ID，安全记为 transport timeout，费用 `unknown`。脱敏结果分别为 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_output_budget_calibration_rq189_probe1_v1.json`（SHA-256=`1e001b49370f734404bc56896610d73d94057203aebf8de172d54787728e7c32`，诊断 SHA=`b46d5e39e1d44293452b1b893c91feff13f57b02`）、`data/evaluation/results/provider_capabilities/zhipu_glm53_flash_output_budget_calibration_rq189_probe2_v1.json`（SHA-256=`42339af9af71db3e63f2ba8e8773898a7f6b60cd8e5ceab06269ec6aca37f32`）和 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_output_budget_calibration_rq189_probe3_v1.json`（SHA-256=`fc54d9479db60cef585b216d0b11dd36e511180b485ea00c2ebced60d528379f`）；后两份诊断 SHA=`21bc38b211e596f933223aa9a871a5b10f62267f`，三份 source identity 均稳定且 public CI 未宣称。该结果只说明低推理档位在 2048 上限可完成这份冻结上下文，而 8192 同步请求在 45 秒内未完成；不能归因模型质量/账号权限，严格 Flash v1 仍为 2048/零额外调用，下一项改为 evaluation-only 的流式可见正文与 `clear_thinking` 组合探针。 |
| RQ-190 | 2026-09-01 | 已完成两次单路流式首个可见正文探针；候选仍未启用、未进入领域准入或生产成熟度 | 用户授予连续推进授权；同一冻结上下文固定 `thinking=enabled`、`reasoning_effort=low`、`max_tokens=2048`、`stream=true`，分别选择 `clear_thinking=true/false` 各一次，SDK `max_retries=0`；首个非空可见正文出现即主动关闭，不改产品默认、不注册候选、不改 Workbench | `clear_thinking=true`：首块 `1813ms`、首个可见正文 `2547ms`、18 chunks（17 reasoning）；`clear_thinking=false`：首块 `1500ms`、首个可见正文 `3875ms`、50 chunks（49 reasoning）。两路均 `observed`，终态和 Usage 未观测，`within_token_budget=null`、费用 `unknown`。最终脱敏结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_stream_visible_completion_rq190_clear_true_v2.json`（SHA-256=`23e3954c2be65d70b24186a3deba35047e3925b2fc2fde1eb3cfeec82631141a`）与 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_stream_visible_completion_rq190_clear_false_v2.json`（SHA-256=`fae64899daaffbd2e9a2a5369ee8d396ea912065f2b7351a782a91eb74a0c77e`），实现/诊断 SHA=`5ec622c4b651f9aa5e12f54b1e5a4a0dc253a4c7`，source identity stable，public CI 未宣称。早期 v1 因预算未知字段表达不严谨而保留为审计、不作正式证据。该批只证明单轮两种请求形状均能打开流并出现首个可见正文，不证明 `clear_thinking` 因果效果、跨轮语义、完整 stream/终态 Usage、成本、候选或生产能力；严格 Flash v1 仍 2048/零额外调用，下一项为 evaluation-only 的完整流式终态/Usage 探针。 |

| RQ-191 | 2026-09-01 | 已完成一次完整流式终态/Usage 探针；候选仍未启用、未进入领域准入或生产成熟度 | 用户继续授权候选边界内的有界诊断；沿用当前产品形状 `clear_thinking=false`、`thinking=enabled`、`reasoning_effort=low`、`max_tokens=2048`、`stream=true`，只发 1 条真实请求，SDK `max_retries=0`，完整读到流结束，不改 Provider-neutral 接口、不注册候选 | 首块 `2203ms`、首个可见正文 `3531ms`、完整流 `24140ms` 以 `finish_reason=stop` 结束；642 chunks（30 reasoning、571 visible、41 other），Usage valid：输入 `1973`、输出 `652`、缓存输入 `0`，预算 within，费用 `unknown`。脱敏结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_stream_terminal_completion_rq191_v1.json`，experiment=`dba57e5316058336dbc0e497d01b115e337ce6367acbb967b5e6760e270b3f46`，SHA-256=`a57fec105859241ea71e32eb8073b4c33b934262a7793b6a47a7b6e4efb4b3c9`，实现/诊断 SHA=`2a01edf58e9f5b11619553a9eeb4448a4cdb87d0`，source identity stable，public CI 未宣称。该结果只证明一份冻结上下文的原始完整流可结束，不证明一般质量、长上下文、高预算、跨轮语义、工具流、provider-neutral 接入、候选/领域/生产准入；严格 Flash v1 仍 2048/零额外调用，下一项改为离线 provider-neutral 流式装配合同。 |

| RQ-192 | 2026-09-01 | 已完成本地；提供商无关流式装配合同冻结为候选接缝，不表示 streaming 产品接入、领域准入或生产成熟度 | 用户给予连续推进授权，要求把 RQ-191 的原始流观察落成可核验的候选适配合同；本批只做离线实现、聚焦测试和持久记录，不改产品默认或 Workbench | 新增纯 Python `app/providers/stream_adapter_contract.py`、`ProviderStreamEvent`、`StreamToolCallDelta`、独立 `ProviderStreamAdapter` 协议、单次 `ProviderStreamAssembler`、`StreamAssemblyTrace` 与 `StreamAssemblyResult`，并从 `app/providers/__init__.py` 导出。合同要求底层 EOF 后显式 `mark_exhausted()`，终止与有效 Usage 同时存在才可交付；终止后最多一个 Usage-only 尾块；持续校验序号、model、可选请求 SHA-256，工具片段连续索引/无重复键/有限数字/深度与数量上限，输出/正文/reasoning/事件上限和失败毒化；工具状态采用 copy-on-write 与增量字符计数，结果默认 repr 不含正文或工具参数；错误类型事件也会毒化，不能借 TypeError 打开隐式恢复。`tests/test_stream_adapter_contract.py` 聚焦 `29 passed`；编译与相邻 Provider/响应策略/恢复合同/Runtime 回归通过。Trace 只保留白名单状态和计数，不保存正文、reasoning、Prompt、工具参数、Key 或原始 request ID。无 SDK、网络、真实 API、重试或产品接线，`capabilities.streaming=False`、候选未注册、严格 Flash v1 2048/零额外调用、Portal/Account/Workbench/Auth/路由和 `production_media=0` 均不变；下一项为同一新实现 SHA 的公共 CI 与供应商适配器一致性测试。 |

| RQ-193 | 2026-09-01 | 已完成本地与同 SHA 公共 CI；智谱 provider conformance 仍是候选接缝证据，不表示产品 streaming、候选/领域准入或生产成熟度 | 用户授权继续推进，并要求在不改生产接线的前提下验证智谱分块到 provider-neutral 装配合同的一致性；本批只增加测试内 fake translator、严格历史结果 schema 回归和公共可复现验证 | 提交 `8bcbaa5ba467fcaad76193d3790d34a106a47d72` 新增测试内 `_FixtureZhipuStreamAdapter`，从代表性的 OpenAI-compatible 智谱分块提取安全中立字段，再与现有 `ZhipuProvider.chat_stream()` fake-client 结果逐字段对照；覆盖正文/reasoning、工具别名 `knowledge_search`→`knowledge.search`、跨分块参数、坏形状/未知工具/空 choices、model/终止边界、迭代器异常 `abort()`、正文空白保留及 Trace 脱敏。conformance 聚焦 `13 passed`；该提交的 GitHub Actions run `33489903978`（`pytest`、`postgres-migrations`、`packaging-smoke`）三 job 均 `completed/success`，且 `head_sha` 精确匹配。测试不读取 Key、不发网络、不修改生产 `ZhipuProvider` 或 `capabilities.streaming`；候选未注册，严格 Flash v1 仍 2048/零额外调用，默认模型、AgentLoop、ToolRuntime、Runtime Trace、预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一项改为公共证据完成后的候选接线裁决：先明确 runtime 接入范围、预算/Trace/回退与失败门，再决定是否实现，不自动启用、执行 G53-7 或黄金切片。 |

| RQ-194 | 2026-09-01 | 已完成公共闭环（候选级显式适配）；仍未进入产品 streaming、候选/领域准入或生产成熟度 | RQ-193 公共 conformance 后，用户授权把候选接线边界落成真实但显式调用的本地 adapter；本批实现与 fake/local 聚焦验证，不改默认产品链路，保留早期占位设计作为历史过程 | `app/providers/zhipu_stream_adapter.py` 的 `ZhipuStreamAdapter` 实现独立 `ProviderStreamAdapter`；`ZhipuProvider.stream_adapter(*, tool_stream=False)` 为显式工厂，`stream_events(request)` 翻译一条 Zhipu OpenAI-compatible 流，`assemble(request, *, max_output_tokens=None, require_request_identity=True)` 只打开一次流并交给 `ProviderStreamAssembler`。`_open_stream_for_adapter(...)` 负责请求校验、thinking/runtime profile 约束、工具 alias 编码和 SDK open；输出 cap 限制 `1..8192`，runtime/explicit/request cap 取最小并同时传给 payload/assembler；provider 必须为 `zhipu`，event model 必须匹配，request identity 默认要求且 Trace 只保存 request ID SHA-256。只有正常 EOF 才 `mark_exhausted()/finalize()`；异常/取消/翻译错误走 `abort("stream_aborted")` 或 typed provider error，close 失败映射安全码，iterator/raw stream 在 finally 关闭，body-free 且无 retry/recovery/ToolRuntime。`tests/test_zhipu_stream_adapter.py` fake/local 聚焦 `20 passed`；提交 `a7580e861cd986c026040c7fcfcc3fa577737961` 的 Actions run `33496237588` 三 job exact-SHA 全绿；这只证明候选接缝公共可复现，不能声称产品或生产准入。`capabilities.streaming` 仍 `False`，严格 Flash v1 仍 2048/零额外调用，默认模型、AgentLoop、Workbench、Portal、Account、Auth、路由、统一 Trace/预算和 `production_media=0` 均不变，不注册候选或 recovery、不调用真实 API。下一门是独立裁决候选 runtime 接线范围。 |
| RQ-195 | 2026-09-01 | 已完成候选 runtime 接线架构评审；不含实现、注册、真实调用或产品准入 | 用户“继续下一轮”授权推进 RQ-194 后的唯一候选接线评审；本批只冻结未来调用方的边界，不扩大到 Agent 主线、Workbench 或前端 | 评审确认 `ZhipuStreamAdapter.assemble()` 只交付完整 `stop`/`tool_calls` 流，对 `length`、缺终止或缺 Usage 安全拒绝；不能把异常当作候选恢复资格。未来若获单独授权，推荐在 `app/evaluation/` 设计隔离的 `CandidateStreamEvaluationHarness`，先校验 `zhipu`、`glm-5.3-flash`、candidate runtime profile v2 与 fresh-recovery policy v1 四元身份，再通过只输出字段状态、finish code、Usage 数字、耗时和安全错误码的 `BoundaryObservation` 接缝分类，最后由 ledger/策略结算。该观察绝不返回或持久化部分正文、reasoning、工具参数；候选最多 2 attempts、1 次额外调用、32,000 input、16,384 output、180,000ms，当前 `execution_allowed=false`，不得发 recovery。新增 ADR-0075、评审计划和学习 walkthrough；治理与差异检查通过。`LLMProvider`、AgentLoop、Worker、统一 Trace/预算、默认模型、`capabilities.streaming=False`、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一门改为 `candidate-runtime-wiring-design / pending`。 |
| RQ-196 | 2026-09-01 | 已完成候选 runtime 接线设计；仍不含实现、注册、真实调用或产品准入 | 用户在确认继续推进且基本决定采用 GLM-5.3-Flash 后，授权完成当前 canonical 的 `candidate-runtime-wiring-design`；该授权只覆盖设计门，不等于静默把候选提升为全产品唯一默认 | 冻结 `CandidateRuntimeBinding` 的 provider/model/profile/policy/attempt 四元身份、body-free `BoundaryObservation` 字段与状态聚合、完整流/不完整流分流、共享 chunk 校验核心、独立 v2 candidate transport、reserve→open→settle 生命周期、未知 Usage 不当零、最多 2 attempts/1 次额外调用/32,000 input/16,384 output/180,000ms 账本，以及独立 `CandidateStreamTrace` allow-list 投影。明确 v1 产品 `ZhipuProvider` 的 2048 cap 不得被候选 v2 偷换；未来 transport 只在 `app/evaluation/`、不实现 `LLMProvider`、不注册 Provider/Runtime、`execution_allowed=false` 时不发 recovery。新增 ADR-0076、设计计划和学习 walkthrough；本批未改 `app/`、Portal、Account、Workbench、Auth、默认模型、`capabilities.streaming` 或 `production_media=0`，治理与差异检查需在实现提交前通过。下一精确门为 `candidate-boundary-observation-contract-implementation / pending`，先做 fake/local 合同与同 SHA 公共 CI，再另行裁决 harness、fresh-recovery、G53-7 和生产准入。 |
| RQ-197 | 2026-09-01 | 本地实现完成；同 SHA 公共 CI 待验证；仍不含注册、真实调用、fresh-recovery 或生产准入 | 用户继续授权并基本决定采用 GLM-5.3-Flash；本批只执行 RQ-196 指定的候选边界观察合同，不扩大到 Agent 主线、Workbench 或前端 | 新增隔离 `app/evaluation/candidate_stream_contract.py`：精确 candidate binding、body-free `BoundaryObservation`、不可变状态快照、字段 presence/状态聚合、共享事件级校验、8192/90/120 秒候选 transport port、资源关闭和独立 Trace allow-list；`ProviderStreamEvent`/智谱翻译保留显式 null 与缺失的区别。覆盖完整 stop/tool-call、length reasoning-only、缺 EOF/terminal/Usage、身份/序号/工具/预算/时钟/关闭失败矩阵；观察器失败毒化且不构造 `ChatResponse`，用户中断类异常不被吞掉。候选仍 `activation_state=candidate`、`execution_allowed=false`，严格 Flash v1 2048/零额外调用、`capabilities.streaming=False`、默认模型、AgentLoop、Worker、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。聚焦与相邻回归 `163 passed`，compileall、diff check、governance 已通过；全量本地首错为未配置 PostgreSQL `RIFTCOACH_TEST_DATABASE_URL`。下一精确项为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-boundary-observation-contract-public-ci / pending`，取得同 SHA 公共 CI 后再另行裁决候选 harness、fresh-recovery、G53-7、黄金切片和生产准入。 |

| RQ-198 | 2026-09-01 | 候选边界观察合同公共 CI 已闭环；仍不含注册、真实调用、fresh-recovery 或生产准入 | 用户要求本轮在公共 CI 结束后收口并暂停；本条只记录 RQ-197 的 exact-SHA 公共证据与下一设计门，不扩大到产品 Runtime 或前端 | 实现提交 `127e6da43ef1b71b284a7e8d4198547b04c556d8` 的 GitHub Actions run `33507627615` 三 job（`pytest`、`postgres-migrations`、`packaging-smoke`）均 `completed/success`，`head_sha` 精确匹配；公共 pytest 为 `2178 passed, 145 skipped, 1 warning, 127 subtests passed`。该证据只证明候选边界合同可公共复现；候选仍 `activation_state=candidate`、`execution_allowed=false`，`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、Worker、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变，没有真实 API/Key、recovery、G53-7 或黄金切片。当前唯一下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-evaluation-harness-design / pending`；只做隔离 harness/ledger/Trace 设计，之后再等用户决定实现或执行真实门。 |
| RQ-199 | 2026-09-02 | 候选评估台设计完成；实现、注册、真实调用与生产准入仍未开始 | 用户“继续”只授权当前 canonical 的 `candidate-evaluation-harness-design`；本批只做架构设计与持久学习材料，不写产品运行时代码、不读取 Key、不发 API、不执行 fresh-recovery/G53-7 | 新增 ADR-0077、实现计划和学习 walkthrough，冻结 `CandidateEvaluationHarness` 的两阶段 staged ledger（primary 先预留，真实观察后再建 recovery plan）、一次 normalized event pump（observer + assembler）、显式 activation gate、独立 body-free `CandidateEvaluationReceipt`、Usage unknown/预算/取消/关闭/重复结算失败语义和 fake/local 测试矩阵。拒绝 sentinel snapshot、首回合后才 reserve、隐式 `LLMProvider`/AgentLoop streaming 分支；保留严格 Flash v1 2048/零额外调用、候选 `execution_allowed=false`、`capabilities.streaming=False`、默认模型、Portal、Account、Workbench、Auth、路由与 `production_media=0`。没有真实 API/Key、recovery、G53-7、黄金切片或媒体采用。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-evaluation-harness-implementation / pending`，只待后续明确继续后实现 fake/local harness。 |
| RQ-200 | 2026-09-02 | 候选评估台 fake/local 实现完成；同 SHA 公共 CI 待验证；仍不含注册、真实 recovery 或生产准入 | 用户“继续”授权当前 `candidate-evaluation-harness-implementation`；本批只实现 RQ-199 冻结的 staged ledger、单次事件泵、临时装配/consumer 与 body-free receipt，不读取 Key、不发真实 API、不改产品 Runtime 或前端 | 新增 `app/evaluation/candidate_evaluation_harness.py` 与聚焦测试，导出显式 RunSpec、sealed disabled activation、候选专用账本、一次性 normalized stream 协调器和独立回执。primary 在 I/O 前预留，open/read/close/clock 失败仍结算；完整 stop/tool 流才可短暂交给显式 consumer，`length`/未知 Usage/身份错误 fail-closed 或 awaiting recovery；Usage unknown 保持 `None`/`unknown`，不把旧 ledger 的零值当余额。聚焦 `15 passed`，与边界观察、流装配、旧恢复合同相邻回归 `102 passed`，compileall 通过；严格 Flash v1 2048/零额外调用、`capabilities.streaming=False`、默认模型、AgentLoop、Workbench、Portal、Account、Auth、路由和 `production_media=0` 不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-evaluation-harness-public-ci / pending`，先取得同一干净提交的 exact-SHA 公共 CI；真实 recovery、G53-7、黄金切片、生产准入与 8F 仍需独立授权。 |
| RQ-201 | 2026-09-02 | 候选评估台 exact-SHA 公共 CI 已闭环；仍不含注册、真实 recovery 或生产准入 | 用户“继续”授权完成 RQ-200 后的公共验证；本条只记录隔离实现提交的公共可复现证据，并把下一步收紧为需单独授权的候选 recovery 诊断边界复核 | 实现提交 `f2a80320123d80a6441f3fcac310014a9bd4550e` 的 GitHub Actions run `33536168224` 已 `completed/success`，`pytest`、`postgres-migrations`、`packaging-smoke` 三 job 均成功且 `head_sha` 精确匹配；公共 pytest 为 `2193 passed, 145 skipped, 1 warning, 127 subtests passed`。该证据只证明 fake/local 候选评估台的公共可复现性，不注册 Provider/Runtime，不打开 `capabilities.streaming`，不改变严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由或 `production_media=0`；没有真实 API/Key、recovery、G53-7、黄金切片或 8F 证据。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-recovery-diagnostic-review / pending-user-authorization`，先复核传输、预算、失败与脱敏边界，之后是否建立新诊断版本仍需单独授权。 |
| RQ-202 | 2026-09-02 | 候选 recovery 诊断边界复核与最小离线加固完成；不建立新诊断版本、不执行真实 recovery 或生产准入 | 用户“授权继续”仅覆盖当前 canonical 的 `candidate-recovery-diagnostic-review`；本条把复核结论固化为可恢复的状态，不能据此扩大授权 | 在隔离候选评估台中修补三类可伪造投影：顶层终态/下一动作与安全错误必须由最后观察推导，attempt 的决定/原因/装配状态与观察绑定，budget projection 与候选硬上限绑定；observer 的 elapsed 上限改为单次 attempt 90 秒与累计 180 秒的较小值，账本累计语义不变。复核确认旧 `glm53_flash_response_recovery_diagnostic.py` 直接持有 SDK/真实 I/O、复用把未知 Usage 当零的旧账本，拒绝将其作为新诊断版本基础；旧文件未改。新增/回归 harness `18 passed`，相邻候选集合 `127 passed, 1 deselected`，compileall、diff check、governance 通过；唯一 deselected/旧诊断阻断来自 Windows 隔离工作树 CRLF fixture 与计划 canonical-LF SHA 不一致（不改 fixture/plan）。候选仍 disabled、未注册、不打开 streaming，严格 Flash v1、默认模型、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变，没有真实 API/Key、recovery、G53-7、黄金切片或 8F 证据。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-recovery-diagnostic-version-design / pending-user-authorization`。 |
| RQ-203 | 2026-09-02 | 版本化候选 recovery 诊断协议设计完成；实现、真实调用、候选注册与生产准入仍未开始 | 用户在完成 RQ-202 后明确授权继续；本条只覆盖协议设计门，不授权实现、读取 Key、真实 recovery、G53-7、黄金切片或产品 Runtime 接线 | 新增 ADR-0079、设计计划和学习 walkthrough，冻结 `glm-5.3-flash-candidate-recovery-diagnostic-v2` / schema `2.0.0`、候选身份与 SHA 绑定、body-free 请求摘要、`reserve→observe→settle` 生命周期、fresh-recovery 作为完整新请求、单次/累计预算、Usage/费用三态、六段单调延迟、失败第一现场和原子 create-only 脱敏回执。明确拒绝复用旧同步诊断器、把 v2 包装成 `LLMProvider`、修改 AgentLoop/默认模型或写统一 Runtime Trace。设计材料仅为 evaluation evidence；候选仍 activation disabled、未注册，`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变，Stage 8/8E 继续 `in_progress`、8F 未开始。RQ-202 加固提交 `67031145d3b3e5c864e881576c69e2fda931e950` 的 Actions run `33582049836` 三 job exact-SHA 全绿（公共 pytest `2193 passed, 145 skipped, 1 warning, 127 subtests passed`）。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-recovery-diagnostic-version-implementation / pending-user-authorization`，实现仍需再次明确授权。 |
| RQ-204 | 2026-09-02 | 版本化候选 recovery 诊断已完成本地实现；同一实现的 exact-SHA 公共 CI 与协议 dry-run 待执行；不含注册、真实 recovery 或生产准入 | 用户连续明确“继续/授权”，并明确缺少测试工具可直接安装；本条只实现 RQ-203 冻结的 candidate-only fake/local 诊断接缝，不读取 Key、不发真实 API、不改变产品 Runtime 或前端 | 新增 `app/evaluation/candidate_recovery_diagnostic_v2.py` 与聚焦测试，导出评估 API 但不注册 Provider。实现 `reserve→open→observe/assemble→settle→receipt`、一次 normalized event pump、body-free 请求/回执 allow-list、候选身份与 SHA 绑定、Usage/预算/费用三态、六段延迟、八类安全失败和 create-only canonical JSON；disabled gate 始终拒绝第二次调用。新模块聚焦 `22 passed`，候选相关回归 `67 passed`，流式/适配器/恢复合同相邻回归 `82 passed`，compileall、静态 no-I/O/import 检查通过；系统 Python 3.13 用户环境已安装 `pytest 9.1.1`，项目验证仍使用仓库 `.venv`（含 `pydantic` 等依赖）。严格 Flash v1 2048/零额外调用、`capabilities.streaming=False`、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-recovery-diagnostic-version-public-ci / pending`；公共 CI 通过后仍须另行授权真实 recovery、G53-7、黄金切片、生产准入与 8F。 |
| RQ-205 | 2026-09-02 | 版本化候选 recovery 诊断的 exact-SHA 公共 CI 与 fake/local 协议演练已闭环；仍不含真实 recovery、候选注册或生产准入 | 用户“继续”授权推进 RQ-204 的下一检查点；本条只验证同一实现提交的公共可复现性和本地协议生命周期，不授权真实模型请求、第二次 recovery 或 G53-7 | 提交 `90242822df0e47304700644572bc12f0a3aa88ad` 的 GitHub Actions run `33598541029` 已 `completed/success`，`pytest`、`postgres-migrations`、`packaging-smoke` 三 job 均成功且 `head_sha` 精确匹配；公共全量 pytest 为 `2218 passed, 145 skipped, 1 warning, 127 subtests passed`，PostgreSQL 控制面为 `201 passed, 1 warning`，前端契约/typecheck/unit/build/E2E、RAG、治理与打包冒烟均通过。独立 fake/local 协议演练完成一次 primary 调用，写入临时 canonical body-free 回执（`calls=1`、`body_free=true`、`3900` bytes），未读取 Key、未发真实 API、未生成持久诊断结果。候选仍 `activation_state=disabled`、`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-recovery-diagnostic-real-call / pending-user-authorization`；真实 recovery、G53-7、黄金切片、生产安全/部署、公共产品准入和 8F 仍需独立裁决。 |
| RQ-206 | 2026-09-02 | 已完成一次有界真实候选主请求观察，但未形成可交付完成响应；候选仍未启用、未进入领域准入或生产成熟度 | 用户“继续下一轮/授权”覆盖 RQ-205 指定的单次真实调用；本条只执行一个 primary，不自动 recovery、不重试、不改默认模型或产品 Runtime | 在干净隔离工作树 `HEAD=0b2342c240cfdc1801e673e830c9a7f30bed3fbd` 上，提交 `0b2342c240cfdc1801e673e830c9a7f30bed3fbd` 的 Actions run `33603143606` 三 job exact-SHA 全绿；实现基线为 `90242822df0e47304700644572bc12f0a3aa88ad`。按普通智谱 API `https://open.bigmodel.cn/api/paas/v4/`、`zhipu/glm-5.3-flash`、`thinking=enabled`、`reasoning_effort=max`、`clear_thinking=false`、`max_tokens=8192`、请求级 90 秒/传输 120 秒、SDK `max_retries=0` 只发出 1 次 primary。结果 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_recovery_diagnostic_v2_rq206_v1.json` 为 canonical body-free receipt，字节数 `4355`、SHA-256=`2ead059ea22f035e6201bee6f3638c8e7a113baed3bf51b55fbbd17e42f862e6`；`calls_reserved/settled=1/1`、`run_state=fail_closed`、`terminal_reason=elapsed_limit`、`error_stage=budget`、`recovery=disabled`、`cost=unknown`。流已观察到 model/request identity、reasoning 与可见正文、`finish_reason=stop` 和 EOF；首事件 `3078ms`、首个可见正文 `151453ms`、总延迟 `175875ms`，但 Usage 缺失且 close 状态失败，未构造完整可交付响应，候选资格为 false。该事实说明当前 `max+8192` 冻结上下文没有满足候选单次 90 秒完整交付门；不能解释为 API/Key 失败、模型一般质量失败或生产能力，也不能把 `stop` 单独当作完整成功。它同时暴露 SDK 读超时并不等于总墙钟截止，后续若继续必须先设计/验证硬墙钟取消与 Usage/终态尾帧处理。候选仍 `activation_state=disabled`、`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变；未执行第二请求、G53-7、黄金切片或 8F。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-real-call-timeout-usage-followup / pending-user-authorization`；需先完成离线硬截止/Usage 处理设计和测试，再另行授权真实重测。 |

| RQ-207 | 2026-09-02 | 候选流硬墙钟、取消、关闭和 Usage 尾帧的离线实现完成；同 SHA 公共 CI 待验证，真实重测与候选启用仍未授权 | 用户“继续下一轮”覆盖 RQ-206 后唯一的离线 follow-up；本批只修候选评估接缝，不发新的真实请求、不执行 recovery/G53-7、不改默认模型、产品 Runtime、Workbench 或前端 | 新增显式 `CandidateStreamSession`（可迭代、非阻塞 `cancel`、幂等 `close`）与 `CandidateStreamDeadlineSupervisor`，从 attempt 起点按绝对单调墙钟监督；没有显式 `session_opener` 时，在调用 legacy opener 前以 `hard_deadline_unsupported` 失败关闭；若显式 opener 返回旧 iterable，则在 opener 返回后立即验证并失败关闭。`ZhipuStreamSession` 仅在候选显式调用时发送 `stream_options={\"include_usage\": true}`，拥有 SDK 迭代器并支持 `close`/`__exit__` 回退；旧 `stream_events()` 与产品 payload 不变。晚到事件不能复活成功，取消/关闭失败保留安全次级证据。新增/更新离线测试覆盖阻塞读取、绝对起点、迟到事件、异常脱敏、Usage opt-in、关闭失败与 legacy transport 拒绝；本地聚焦集合 `67 passed`，compileall、diff check 通过，当前不计任何新 API 调用。实现仍是 candidate-only 8-Advanced evidence，不打开 `capabilities.streaming`，不注册候选，不改变严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由或 `production_media=0`。同步 opener 若永久阻塞、或 SDK close 不能非阻塞唤醒 `next()`，仍无法由普通 Python 安全强杀，必须保持 fail closed；下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-stream-deadline-usage-public-ci / pending`，公共 CI 后再另行决定是否授权一次真实观察。 |

| RQ-208 | 2026-09-02 | RQ-207 候选流硬墙钟与 Usage 尾帧实现已完成同 SHA 公共 CI；真实重测、候选注册和生产准入仍未授权 | 用户“继续下一轮”授权推进 RQ-207 的公共可复现性验证；本条只记录 exact-SHA CI 证据并把下一步收紧为一次新的真实观察授权，不自动读取 Key、重试或切换产品默认 | 提交 `015b022bfce6d03452f753794ac126a377f8355b` 的 GitHub Actions run `33613113829` 已 `completed/success`，`pytest`、`postgres-migrations`、`packaging-smoke` 三 job 均成功且 `head_sha` 精确匹配；公共 pytest 为 `2241 passed, 145 skipped, 1 warning, 127 subtests passed`，PostgreSQL 控制面为 `201 passed, 1 warning`，网页契约/生产包、媒体审计工具链、治理、RAG v1 与独立 4M holdout、compileall、Harness dry-run 均通过。RQ-207 本地四文件聚焦仍为 `67 passed`，本轮没有新的真实 API 调用。该公共证据只证明候选接缝可复现，不证明供应商 SDK `close()` 非阻塞/能唤醒挂起 `next()`，也不构成模型一般能力、领域采用或生产成熟度证据；同步 opener 永久阻塞限制仍保留。候选继续 `activation_state=disabled`、`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-real-call-timeout-usage-followup / pending-user-authorization`；只有新的明确授权才可执行一次真实观察，之后仍需独立 G53-7、黄金切片、生产安全/部署/合规与 8F 裁决。 |

| RQ-209 | 2026-09-02 | 已完成一次候选真实 provider 硬墙钟观察；硬截止生效但未形成终态、EOF 或 Usage，候选仍未启用、未进入领域准入或生产成熟度 | 用户“继续”覆盖 RQ-208 指定的一次真实观察；本条只发送 1 次 primary，不自动 recovery、不重试、不改产品 Runtime、Workbench 或前端 | 在干净隔离工作树 `HEAD=cc5d5c82ddefd4e9932514634d53d1629e563655` 上，以公共闭环树 SHA `015b022bfce6d03452f753794ac126a377f8355b` 作为 implementation/diagnostic identity，使用普通智谱 API `zhipu/glm-5.3-flash`、`stream_options.include_usage=true`、`max_tokens=8192`、单次 attempt 90 秒、传输 120 秒、SDK retries=0，仅发出 1 次 primary。回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_recovery_diagnostic_v2_rq207_v1.json` 为 canonical body-free JSON，`4342` bytes，SHA-256=`56794fc171c959bbc9f4be6bcb12c5b9300b373dd0a2d270678db81c450c7c6a`；该回执由本地证据提交 `0b276cc1c07ff2cfdb1dfd339e8dc66ab6aff40c` 保存，公共 CI 尚未宣称；`calls_reserved/settled=1/1`、`run_state=fail_closed`、`terminal_reason/error=elapsed_limit`、`error_stage=budget`、`usage=missing`、`cost=unknown`、recovery 未执行。首个流事件与 opener 计时约 `3421ms`，观察到非空 reasoning；在 `90015ms` 触发硬墙钟，未观察到可见正文、terminal、EOF 或 Usage，组合会话 `close_state=failed`，`eof_observed=false`，未构造成功响应。观察投影中的 `observation.elapsed_ms=0` 只是截止前未结算的初始值，真实时序以 latency 的 `90015ms` 为准；单次 `attempt.budget_state=exceeded` 与累计 token 未知导致的 `budget.overall_state=unknown` 不矛盾。该结果证明诊断层在 attempt 墙钟到点 fail closed；`close_state=failed` 仅是组合会话清理结果，不能归因到供应商 SDK response、迭代器或其他具体资源，也不能证明底层 close 非阻塞或能唤醒挂起的 `next()`，更不能解释模型一般能力、API/Key、领域采用或生产成熟度；回执不含正文、Key、Bearer 或完整 request ID。候选继续 activation gate `disabled`、`activation_state=candidate`、`execution_allowed=false`、`capabilities.streaming=False`，且未注册；严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。当前子阶段仍未关闭，下一精确 checkpoint 保持 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-real-call-timeout-usage-followup / pending-user-authorization`；后续若要拆分 provider response cancel、迭代器清理或重新观察，必须另行取得明确一次性授权，不能自动注册候选、进入 G53-7 或 8F。 |
| RQ-210 | 2026-09-03 | 候选会话分资源关闭报告已完成本地实现与 exact-SHA 公共 CI；回执 schema 未变；provider close/wakeup 仍未证实 | 用户“继续”授权当前候选流后续的离线边界拆分；本批只补候选内部、内存态的资源可观测性，不发新的真实请求、不注册候选、不改产品 Runtime | `ZhipuStreamSession` 新增不可变、body-free 的 `ZhipuStreamCloseReport`，分别记录迭代器、外层 SDK stream wrapper、组合状态及资源是否为同一对象；关闭过程逐资源最多一次、控制类异常在尝试其他资源后继续抛出，并保留旧 `close_failed` 投影。新增 distinct/shared/失败/无 hook/context-manager 测试；adapter/deadline/v2/real 聚焦共 `73 passed`，扩展相邻回归共 `182 passed, 27 subtests passed`，compileall、diff check、governance 本地通过。实现提交 `15026a8abeeb2f343fbf893e55e2d94c512a86f6` 的 Actions run `33657368435` 三 job 均 `completed/success` 且 head SHA 精确匹配；公共 pytest 为 `2241 passed, 145 skipped, 1 warning, 127 subtests passed`，PostgreSQL 控制面为 `201 passed, 1 warning`。RQ-209 v2 receipt/schema 2.0.0、canonical JSON/SHA 原样不变，未新增持久字段；`cancel()` 仍同步走 SDK close，报告不表示底层 HTTP response 可取消、close 非阻塞或唤醒挂起 `next()`。候选仍 activation gate `disabled`、未注册、`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一步改为等待明确授权的 provider-level close/wakeup 观察，不自动真实重测或注册候选。 |
| RQ-211 | 2026-09-03 | 已完成一次有界的候选 provider close/wakeup 真实观察；本次没有形成挂起读取，因而不能证明取消唤醒；候选仍未启用、未进入领域准入或生产成熟度 | 用户“继续”授权 RQ-210 后的精确观察门；本批只运行已冻结的 candidate-only 探针一次，不重试、不 recovery、不注册候选，也不改产品 Runtime、Workbench 或前端 | 探针实现/诊断/输入计划身份均为 `c31127b3c780fe4c493966d8b60f942d3b773fd4`；该 SHA 的 Actions run `33661910096` 三 job `completed/success`。在该干净快照上只发送 1 次普通智谱 `zhipu/glm-5.3-flash` 请求，SDK retries=0，父进程硬边界 30 秒；回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_close_wakeup_observation_rq211_v1.json` 为 schema `1.0.0` 的 canonical body-free JSON，`908` bytes，SHA-256=`9c86b72561b9c9eb40ab083e326b0386b3572e6d4d684a40f66b54908d2613d2`。观察为 `not_pending`：会话已打开、调用数 1、首段读取 `78ms`，只记录 `reasoning_seen/content_seen` 类别；没有观察到 pending reader，因此 `cancel_status=not_attempted`、`reader_woke=false`。退出码为 0、子进程未被强制终止；关闭报告为 iterator/SDK stream/composite 全部 `closed`、`shared_resource=false`。这只说明该有限窗口没有进入可测试的 pending-read 分支，不能据此宣称 provider close 非阻塞、能唤醒挂起 `next()` 或底层 HTTP response 已被取消；回执不含 Key、Authorization、request ID、正文、reasoning 原文或 body。后续测试加固提交 `5b0ce15d9d4a4c3e413d53032b9f529d20e18f6c` 的公共 run `33662730304` 被外部取消，不冒充通过，也不改变本次 c311 exact-SHA 回执身份。候选继续 activation gate `disabled`、未注册、`execution_allowed=false`、`capabilities.streaming=False`；严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-close-wakeup-follow-up-decision / pending-user-decision`；不自动追加真实请求、G53-7、黄金切片或生产准入。 |

> RQ-211 公共验证补充（2026-09-03）：提交 `1c669e0` 已为上述新回执加入 provider capability
> 目录的显式 schema 分派；Actions run `33666132282` 三 job exact-SHA 全绿，公共 pytest 为
> `2268 passed, 145 skipped, 1 warning, 127 subtests passed`，PostgreSQL 控制面为 `201 passed, 1 warning`。
> 该修复只验证回执合同可被公共扫描识别，没有新增真实 API 调用，也不改变 c311 的真实观察身份。

| RQ-212 | 2026-09-03 | 候选 close/wakeup 离线 pending-read 回放已完成本地与 exact-SHA 公共闭环；只验证本地观察合同，不形成 provider-level 结论 | 用户“继续、每一步迈大一点”授权推进 RQ-211 暴露的观察条件缺口；本批新增 evaluation-only 离线协议与固定五场景，不追加真实 API、recovery、retry、候选注册或产品接线 | 新增 `glm-5.3-flash-candidate-close-wakeup-replay` / schema `1.0.0`、Event 闸门式 `ReplaySession`、严格离线回执和 writer 路径隔离；回执强制 `evidence_origin=offline_fake`、`real_provider_observed=false`、`provider_call_count=0`、`network_used=false`，并区分 `fake_session_open_count=1` 与 `observer_call_count=1`。五场景固定 SHA=`8a389a9796b0407b3e209ddaab5134b140d4c8379ba659380ae031229011fe26`。最终回执 `data/evaluation/results/offline/zhipu_glm53_flash_candidate_close_wakeup_replay_rq212_v2.json` 为 `2220` bytes、SHA-256=`a4477258735c5f217f1c328830e8453e4c686a9b386e1e04e0f37b6d777876f2`，三个身份 SHA 均绑定实现提交 `1a32012d9dc6424aa012f160d48c8847e21b00ec`；v1 仅为绑定旧 HEAD 的提交前演练。实现提交 Actions `33707313651` 三 job exact-SHA 全绿（pytest `2284 passed, 145 skipped, 2 warnings, 127 subtests passed`；PostgreSQL `201 passed, 2 warnings`；packaging-smoke 通过），本地聚焦 `37 passed`、compileall、diff check、governance 通过。离线结果不能证明供应商 SDK close 非阻塞、底层 HTTP response 可取消或真实 pending `next()` 能唤醒；候选仍 disabled/未注册、`capabilities.streaming=False`，严格 Flash v1、默认模型、AgentLoop、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 均不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-close-wakeup-real-observation / pending-user-authorization`，新的真实观察需单独授权。 |
| RQ-213 | 2026-09-03 | 已完成一次新的有界真实 close/wakeup 观察；本次仍未形成 pending-read，provider 唤醒能力保持未知 | 用户要求连续推进并允许把普通检查合并成较大批次；本条在 RQ-212 公共闭环后执行一次真实候选观察，不重试、不 recovery、不注册候选、不改产品链路 | 在 exact-SHA 公共绿灯提交 `a396412f7cd0f2e923536cf55f715dd56251aae5` 上，只发送 1 次普通智谱 `zhipu/glm-5.3-flash` 请求，SDK `max_retries=0`、父进程 30 秒边界。新回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_close_wakeup_observation_rq213_v1.json` 为 schema `1.0.0`、909 bytes、SHA-256=`8b2b645bc79785cec6520759d63c530d1b6d6a7d06b192b472334df543706f7b`；`call_count=1`、`session_opened=true`、首段 `172ms`、事件类别 `reasoning_seen/content_seen`，`observation_state=not_pending`、`pending_reader_observed=false`，因此 cancel 未尝试、`reader_woke=false`；子进程正常退出，iterator/SDK stream/composite 均 `closed`、`shared_resource=false`。回执不含 Key、Authorization、request ID、正文、reasoning 原文或 provider body。该结果只说明本次窗口没有进入可测试的 pending-read 分支，不能证明或否定 provider close 非阻塞、取消唤醒或 HTTP response 已取消；候选仍 disabled/未注册、`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1 2048/零额外调用、默认模型、AgentLoop、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。下一精确 checkpoint 改为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-close-wakeup-follow-up-decision / pending-user-decision`，先裁决是否另立能稳定制造 pending-read 的新版协议。 |

| RQ-214 | 2026-09-03 | 候选 SDK/HTTP transport gate 离线预检已完成本地实现；两阶段均形成 pending-read 并在 response close 后唤醒，但暴露本地并发关闭竞态；真实 transport-gated 观察仍未执行 | 用户“继续”授权先把观察条件缺口做成更大、可复现的零调用批次；本条不读取 Key、不联网、不注册候选、不改产品 Runtime 或 Workbench | 新增独立 `glm-5.3-flash-candidate-close-wakeup-transport-gate` / schema `1.0.0`，用真实 OpenAI SDK、Zhipu 候选适配器和内存 `MockTransport` 固定 `after_first_event`、`before_first_event` 两阶段；每阶段仅 1 次本地 transport 请求，receipt 固定 `evidence_origin=offline_sdk_transport_fixture`、`provider_call_count=0`、`network_used=false`，只保存事件类别、读取/取消状态、gate 生命周期、close 投影和 fixture 描述 SHA，不保存正文、Key、headers、request ID 或异常文本。离线观察发现 reader 可唤醒，但当前并发 iterator close 可能为 failed，独立结论码为 `client_wakeup_close_race`；这不是 provider-native 结论，适配器关闭顺序不在本条静默修复。聚焦测试、compileall、diff check、governance 与同 SHA 公共 CI 完成后，下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-transport-gated-real-observation / pending-user-authorization`；届时最多发 1 次官方 TLS transport 包装的真实请求，不重试、不 recovery、不追加第二请求。 |

RQ-214 回执已在实现提交 `4c220c5751288ad77c589d2e0e581690085803c0` 上生成：
`data/evaluation/results/offline/zhipu_glm53_flash_candidate_transport_gate_rq214_v1.json`，
`1693` bytes、SHA-256=`9a952bd6d2798af8796e156d1922f214e6264b67dee12cd86a96b3f886c76bdb`，
canonical round-trip 通过。Actions run `33712055286` 三 job exact-SHA 全绿：pytest `2292 passed, 145 skipped, 2 warnings, 127 subtests passed`；PostgreSQL `201 passed, 2 warnings`；packaging-smoke 通过。

| RQ-215 | 2026-09-03 | 候选 transport-gated 一次真实观察完成；真实流启动后的本机受控客户端 reader 唤醒已观察到，但出现 `client_wakeup_close_race`；不形成 provider-native 或生产 capability | 用户在 RQ-214 离线预检和同 SHA 公共 CI 完成后以“继续”授权本批；只发 1 次真实 `glm-5.3-flash` 请求，不 retry、不 recovery、不发送第二请求、不注册候选、不改产品 Runtime/Workbench/前端 | 实现/观察器/输入计划身份均为 `2acdf795881733e70c9246c48f7147d5136821b5`；Actions run `33721483490` 三 job exact-SHA 全绿，公共 pytest `2296 passed, 145 skipped, 2 warnings, 127 subtests passed`、PostgreSQL `201 passed, 2 warnings`、packaging-smoke 通过。回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_transport_gate_real_rq215_v1.json` 为 schema `1.0.0`、`1305` bytes、SHA-256=`732e870bbb0163d354006434c091bd7f15773ffa4e041b25edfc2a5d17739e59`，canonical round-trip 通过且 body-free。`provider_call_count=1`、`transport_request_count=1`、`network_used=true`、`gate_entered=true`、`pending_reader_observed=true`、`reader_woke=true`（`31ms`）；取消安全码 `zhipu_stream_close`，iterator/composite=`failed`、SDK stream=`closed`。该结果仅说明真实流启动后本机受控停顿的客户端行为；候选仍 disabled/未注册、`capabilities.streaming=False`，默认模型、产品 Runtime、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-transport-gated-real-observation / completed-real-observation / pending-next-decision`。 |

| RQ-216 | 2026-09-03 | 候选适配器的跨线程迭代器关闭竞态已完成本地修复与 exact-SHA 公共 CI；不含新的真实调用、候选注册或产品准入 | 用户在 RQ-215 观察后要求继续；本批只处理已定位的 `client_wakeup_close_race`，不再发真实请求、不改产品 Runtime/Workbench/Portal/Account/Auth | `ZhipuStreamSession` 现在跟踪活跃读取：有活跃 `next()` 时先关闭外层 SDK response，将 iterator 延后到读取线程自己的 `finally`；无活跃读取时保持逐资源最多一次关闭。新增阻塞读取回归，并将 RQ-214 两阶段断言收紧为 `cancel_status=returned`、reader 唤醒且 iterator/SDK/composite 均 `closed`；延迟期间仍保持 `not_observed`。候选聚焦 `61 passed`，compileall、`git diff --check`、governance 通过，本批真实 API 为 0。实现提交 `3740cdbe2d02b140780ea2b8834793df268e6ac1` 的 Actions run `33726209532` 三 job exact-SHA 全绿，公共 pytest `2297 passed, 145 skipped, 2 warnings, 127 subtests passed`，PostgreSQL 与 packaging-smoke 通过。RQ-215 旧回执/schema/SHA 不回写；候选仍 disabled/未注册、`capabilities.streaming=False`，严格 Flash v1、默认模型、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。当前下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-transport-gated-real-observation / completed-adapter-close-order-fix / pending-next-decision`；是否重新执行一次真实观察需单独决定。 |

| RQ-217 | 2026-09-03 | 关闭顺序修复后的候选 transport-gated 一次真实观察已完成；客户端 reader 唤醒与本层资源关闭投影为 clean，但不形成 provider-native、产品或生产 capability | 用户在 RQ-216 exact-SHA 公共 CI 通过后明确授权下一轮；本批只发送 1 次真实候选请求，不 retry、不 recovery、不发送第二请求、不注册候选、不接入产品 Runtime/Workbench/Portal/Account/Auth | 实现、观察器、输入计划身份均绑定 `3e028b1217f1274152ba161993287f29188a1b73`；Actions run `33727163550` 三 job（pytest、postgres-migrations、packaging-smoke）均 `completed/success`。观察阶段固定 `before_first_event`、SDK/HTTPX retries=0、父进程 30 秒；真实回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_transport_gate_real_rq217_v1.json` 为 `1284` bytes、SHA-256=`ad4b920e94f019dae0b08c166e248c12349bdee0d73bf14b8ab2342e6b428ef3`，canonical round-trip 通过且 body-free。恰好 `provider_call_count=1`、`transport_request_count=1`、`network_used=true`、`gate_observation_valid=true`；`pending_reader_observed=true`、`reader_woke=true`、`cancel_status=returned`、`observation_state=pending_cancel_returned`，上游事件/闸门进入/下游关闭/上游 stream 关闭均观察到，iterator/SDK/composite close report 全为 `closed`、`shared_resource=false`，结论为 `client_wakeup_clean`。`gate_released=false` 是首帧前受控停顿协议的预期条件，不是泄漏信号。回执不含 Key、Authorization、headers、request ID、请求/响应正文、reasoning、工具参数或异常文本。该结果仅证明真实流启动后本机受控客户端的唤醒和 reader-owned 收尾，不证明 provider-native close/wakeup、底层 HTTP response 独立可取消、模型一般能力、成本/延迟稳定性、G53-7、黄金切片、生产准入或 8F；候选仍 disabled/未注册、`execution_allowed=false`、`capabilities.streaming=False`，严格 Flash v1、默认模型、产品 Runtime、AgentLoop、统一 Trace/预算、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。当前下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-transport-gated-real-observation / completed-clean-client-observation / pending-next-decision`；没有新的独立决策/授权前不再发送真实请求。 |
| RQ-218 | 2026-09-03 | 最新实现的 GLM-5.3 Flash G53-3 协议复核完成，3/3 通过；只确认普通 API 与基础工具协议可达，不改变产品准入 | 用户授权继续 Flash 主线并要求有影响的结论同步文档；本批精确 3 次调用、SDK retries=0、只写脱敏回执，不注册候选、不改默认模型或产品链路 | 实现 `aa22cea0daeb443b635706144ccbfa66185670c4`；证据提交 `4b6cd5807f40f6a8dd469f21c688be861261d20c`、Actions `33735039437` 三 job exact-SHA 全绿。A1 用时 `20234ms`、A2 用时 `14938ms`；回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_adapter_protocol_aa22cea.json` 为 `2145` bytes，SHA-256=`feeb7fd7eec2643ca692bd6182fd94a04abed354b17b892029402c0217641e99`。候选仍 disabled/未注册，严格 Flash v1 仍 2048/零额外调用，`capabilities.streaming=False`，production_media=0。 |
| RQ-219 | 2026-09-03 | 候选 8192 输出的单次真实诊断在 90 秒硬墙钟以 `fail_closed / elapsed_limit` 收口；未执行 recovery、retry 或第二请求，公共 CI 已闭环 | 用户授权继续并允许较大步幅；本批只允许 1 次 primary，候选 evaluation-only，不能把超时解释为模型整体不可用或生产 streaming 失败 | 在公共绿灯提交 `4b6cd5807f40f6a8dd469f21c688be861261d20c` 上使用 `glm-5.3-flash-runtime-v2-candidate/2.0.0`，8192 输出、Agent 90 秒、传输 120 秒、retries=0。回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_recovery_diagnostic_v2_rq218_v1.json` 为 `4341` bytes，SHA-256=`21350d7883b4d2eea30e0467a7b8c23eed3a3ad5a9deeb309c44f8ded5cf3f84`；证据提交 `3f35d150b2f17f919f2be1597c08c6db0178c461` 的 Actions run `33735717434` 三 job `completed/success`。下一步转为零网络的档位、流终态、Usage 尾帧与恢复决策拆分；候选继续 disabled/未注册。 |
| RQ-220 | 2026-09-03 | Flash 响应档位—终态—Usage—恢复离线拆分矩阵完成本地与 exact-SHA 公共闭环，9/9 场景通过；只增加 evaluation-only 归因能力，不改变产品接线 | 用户要求继续 GLM-5.3 Flash 并要求有影响的文档同步；本批零网络、provider calls=0，候选不注册、不追加真实请求 | 新增 `app/evaluation/glm53_flash_response_profile_split.py`、离线 CLI、create-only body-free receipt 和聚焦测试；复用既有候选 observer/strict policy/candidate policy，覆盖 low/max、2048/8192、clear_thinking、stop、tool_calls、length、缺/非法 Usage、elapsed timeout。实现提交 `14254048f6ad2faea5c7b15801e5c7c11e0ceba4` 的 Actions `33738050233`，回执提交 `ebb09a525b3340f31ba71821b894b4a142dfb4e7` 的 Actions `33738673832` 均三 job `completed/success`；回执 `data/evaluation/results/offline/zhipu_glm53_flash_response_profile_terminal_recovery_split_rq220_v1.json` 为 `6209` bytes，SHA-256=`32965cbe06fc122c8ed436dbab0e4100fdf9b6f51510e2a69849b3cc4c2c8f8a`。候选仍 disabled/未注册，严格 Flash v1 仍 2048/零额外调用，默认模型、Workbench、Portal、Account、Auth、路由和 `production_media=0` 不变；下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / response-profile-terminal-recovery-offline-split / completed-public / pending-next-decision`。 |
| RQ-221 | 2026-09-03 | 显式 GLM-5.3 Flash 低思考候选 profile 与一次真实无工具探针完成；只确认冻结上下文中的响应可完成，不改变产品准入 | 用户要求继续 GLM-5.3 Flash，并授权在候选隔离边界内继续；本批只允许一次真实请求，禁止 retry/recovery/AgentLoop/工具，不注册候选、不改产品 Runtime 或前端 | 新增 `app/evaluation/glm53_flash_candidate_profile.py`、候选 Provider 显式构造、`app/evaluation/glm53_flash_candidate_profile_probe.py` 及 CLI/测试。profile 固定 `thinking=enabled`、`reasoning_effort=low`、`clear_thinking=false`、4096 输出、Agent/工具 90 秒、传输 120 秒、`temperature=1`、`top_p=0.95`，并保持 `activation_state=candidate`、`execution_allowed=false`。实现提交 `c3de5555d0b00d77f402c41a842d00df53f46865` 的 Actions `33746833148` 三 job exact-SHA 全绿；聚焦 `25 passed`，本次相关候选/流/智谱回归 `357 passed`。随后真实请求恰好 1 次，回执提交 `ef8d4b4133eeb952963e9e5cc112ec1fc458c671`，路径 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_low_4096_profile_probe_rq221_v1.json`，canonical SHA-256=`c2946954181b10da2dcb726e186a2800d4381da24d04395b7baef4eecebd1957`；`status=observed`、`finish_reason=stop`、`usage=valid`、输入/输出 token `1973/498`、延迟约 `20735ms`、`provider_call_count=1`、`network_used=true`。回执 body-free/create-only；该窄观察不证明领域质量、工具多轮、成本/延迟稳定性、G53-7、黄金切片、生产准入或 8F。严格 Flash v1 仍 2048/零额外调用，候选仍 disabled/未注册，默认模型、产品 Runtime、AgentLoop、Trace/预算、Portal、Account、Workbench、Auth、路由与 `production_media=0` 不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-probe / completed-real-observation / pending-next-decision`；下一步先设计/裁决独立低档候选 held-out 领域门，不自动追加真实请求。 |
| RQ-222 | 2026-09-03 | 低思考候选独立领域门完成设计裁决，进入离线实现；尚未改产品代码、创建新考卷或发真实请求 | 用户要求继续 GLM-5.3 Flash；本批把 RQ-221 的窄响应观察与领域准入分开，拒绝旧考卷换档重跑和全局注册，先做候选专用评测作用域/共享请求策略接缝的离线 TDD | 新增 [ADR-0091](docs/adr/0091-design-glm53-low-profile-heldout-domain-gate.md)、[设计计划](docs/plans/2026-09-03-glm53-low-profile-domain-gate-design.md) 和[学习 walkthrough](docs/learning/8e-glm53-low-profile-domain-gate-design-walkthrough.md)。采用私有能力令牌绑定的 evaluation-only profile，正常 Runtime resolver 继续拒绝候选；后续使用全新 oracle-blind 三案例 held-out 资产。门预算固定为低思考/4096 输出、Agent/工具 90 秒、传输 120 秒、每案 4 次/全域 12 次、无 retry/recovery/revision、首错停止、24,000/72,000 token 墙，评测关闭 deterministic fallback；本设计 provider calls=0。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-domain-gate-offline-implementation / pending`，候选仍 disabled/未注册，默认模型、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变。 |
| RQ-223 | 2026-09-04 | 低思考候选领域门的评测作用域、共享请求策略和最后一层预算墙完成离线实现；等待同 SHA 公共 CI | 用户要求继续并希望加快；本批只在隔离分支用 Fake Provider 完成 TDD，不读取 Key、不发真实请求、不注册候选、不改默认模型或 Portal/Account/Workbench/Auth | 新增 `CandidateEvaluationRequestPolicy` 私有签发/身份校验、显式 `request_policy` 接缝（Agent 编译、LLM 工具、Draft/Domain executor）、`CandidateEvaluationBudgetedProvider`（每案 4 次/全域 12 次、24,000/72,000 token 墙、reserve-before-I/O、首错停止）及候选执行的 deterministic fallback 关闭。聚焦新测试 5 passed；候选/Runtime/Agent/Provider/工具/Harness 相邻回归 118 passed；compileall、diff check、governance 通过。实现与学习材料见 [离线实现计划](docs/plans/2026-09-04-glm53-low-profile-domain-gate-offline-implementation.md) 和 [walkthrough](docs/learning/8e-glm53-low-profile-domain-gate-offline-implementation-walkthrough.md)。候选仍 `candidate-only/disabled`，严格产品 Flash v1 2048/零额外调用、`production_media=0`、8F 与生产准入边界不变；下一步为同一 SHA 的公共 exact-SHA CI。 |
| RQ-224 | 2026-09-04 | RQ-223 离线控制面取得 exact-SHA 公共 CI 闭环，进入新的 G53-3-L 与新鲜 held-out 资产前置 | 用户要求继续并加快；本批只记录已完成实现 `d823cc40c3fcafb7167edccded87e185be4cae8a` 的公共验证，不发真实请求、不注册候选、不改默认模型或 Portal/Account/Workbench/Auth | Actions `33781369322` 的 pytest、PostgreSQL migrations、packaging-smoke 三 job 均成功；公共 pytest `2326 passed, 145 skipped, 2 warnings, 127 subtests passed`；provider calls=0。候选仍 `candidate-only/disabled`，严格产品 Flash v1 2048/零额外调用、`production_media=0`、8-Core/生产准入/8F 边界不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-g53-3l-and-fresh-assets / pending-user-authorization`。 |

| RQ-225 | 2026-09-04 | 低思考 G53-3-L 协议接缝与全新三案例资产离线实现完成，等待同 SHA 公共 CI | 用户要求继续并加快；本批只在隔离树完成 evaluation-only 协议/资产控制面，不读取 Key、不发真实请求、不注册候选、不改默认模型或 Portal/Account/Workbench/Auth | 显式 `request_policy` 已接入协议切片；最多 3 次低思考协议组合器输出 body-free/create-only 报告；新 Dataset、V1.1 Input Plan、Prompt/Context Snapshot 和两个合成 fixture 通过交叉身份/marker 校验，资产准入 `external_provider_calls=0`。协议/资产及相邻回归 `20 passed`，compileall、diff check、governance 通过。候选仍 `candidate-only/disabled`，严格 Flash v1、`production_media=0`、8-Core/生产准入/8F 边界不变。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-g53-3l-and-fresh-assets / completed-local / pending-public-ci`。 |

RQ-225 后续公共验证更新：为修复新模块顶层导入环新增提交
`411753c1d4b89fe0c4ce9098caf380c45e10fa0f`；Actions `33787508488` 的 pytest、PostgreSQL
migrations、packaging-smoke 三 job 均成功，公共 pytest 为 `2332 passed, 145 skipped, 2 warnings,
127 subtests passed`。当前 checkpoint 已进入
`8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-g53-3l-and-fresh-assets / completed-public / pending-user-authorization`，
provider calls=0，仍需明确授权才执行真实协议门。

| RQ-226 | 2026-09-04 | 低思考 G53-3-L 真实协议门完成，候选协议 `admitted=true` 但仍未注册或生产准入 | 用户在 RQ-225 公共 exact-SHA CI 闭环后以“继续”授权一次最多 3 次真实协议调用；本条不自动进入 held-out 领域门、不 retry/recovery/revision、不改默认模型、产品 Runtime、Portal/Account/Workbench/Auth | 在实现/协议 SHA `ac63bf4ee70d61fca78813b200cf7775e5ca61d8` 上，`zhipu/glm-5.3-flash` 候选 `low + 4096` 严格完成 A1 结构化合同 `1/1` 与 A2 工具往返 `2/2`，总 `3/3` calls，输入/输出/总 token `1007/84/1091`，累计 `12062ms`。脱敏、body-free、create-only 回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_low_4096_g53_3l_rq225_v1.json` 为 `2511` bytes，SHA-256=`a3077ce6d4729e676d0c0ce0d9a6429153075ca59e0850529dee4e29c0376e35`；`candidate_registered=false`、`production_admitted=false`。该结果只证明固定协议可达性，不证明 held-out 领域质量、成本/延迟稳定性、streaming 生产能力、黄金切片、安全/部署/合规或 8F。下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-g53-3l-protocol / completed-real-observation / pending-next-decision`，独立领域门需另行授权。 |
| RQ-227 | 2026-09-04 | 低思考三案例 held-out 领域门真实观察完成但未准入；首个安全失败停止后不得重跑同一考卷 | 用户在 RQ-226 协议门完成后以“继续”授权一次独立领域门；本批只允许冻结的低思考候选策略和 12-call/72,000-token 资源墙，不扩大到产品 Runtime、默认模型、Portal/Account/Workbench/Auth 或 8F | 入口修复提交 `659757eca7ff1b658dfd164631512d3964c5a2ff` 的 Actions `33826568517` 三 job exact-SHA 全绿。真实回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_candidate_low_4096_domain_gate_rq227_v1.json` 为 `7537` bytes、SHA-256=`b9fbebacf5c277c6b2cd57f018ff58cfb2646dbad95f6cdc9e90822646a68400`，body-free/create-only；领域调用 `6` 次，累计（含协议）`9/15`，领域 token `17834`、累计 token `18925`，`network_used=true`。`low_gate_baseline_17` 通过（Evaluation 96）；`low_gate_user_boundary_23` 虽回答完成（Evaluation 97），但没有证据来源且注入检查失败，触发 `evidence_missing/unsafe_publication`，全局首错停止；`low_gate_knowledge_boundary_31` 按规则 skipped。最终 `admitted=false`、`candidate_registered=false`、`production_admitted=false`；候选仍未注册，不能由此声称领域质量或生产成熟度。不得重写/覆盖该回执或同一 held-out 资产，不得因该失败静默修改产品适配器或默认模型；下一精确 checkpoint 为 `8e-productization / candidate-explicit-zhipu-neutral-stream-adapter-seam / candidate-low-profile-heldout-domain-gate / completed-real-observation / pending-next-decision`，先等待失败归因/是否另立版本的裁决。 |

| RQ-228 | 2026-09-04 | 生效并完成公共闭环；候选领域门证据与注入边界加固，等待下一版本裁决 | RQ-227 的第二案同时出现无可归因来源与不安全指令回显；用户要求继续把 GLM-5.3 Flash 做成专属候选适配，但不应靠放宽门槛掩盖失败 | 新增版本化 `glm53-flash-domain-quality-v1` 候选入口：最低来源数硬门、可信 system policy 附录、marker 无关的拒绝性脱敏和 body-free `EvidenceDiagnostics`。相关/相邻回归 102 passed；实现 `e2efe8fd75e8cf27cbee7e90484fc90d288ce065` 的 Actions `33832025848` 三 job exact-SHA 全绿，公共 pytest 2344、PostgreSQL 201、packaging-smoke 通过。默认产品 Runtime、GLM-5.2 手动兼容/应急路径、Portal、Account、Workbench、Auth、路由与 `production_media=0` 不变；本批不读取 Key、不发真实请求、不重跑 RQ-227。下一步另立全新协议/资产版本并先做 no-I/O 准入，真实观察仍需新的明确授权。 |

| RQ-229 | 2026-09-04 | 生效并完成公共闭环；等待真实观察授权 | RQ-228 修复后不得重跑或改写 RQ-227 考卷；必须另立全新版本的协议、问题、合成数据、Context identity 和 marker，并先完成零外部调用准入 | 新 V2 准入绑定 `glm53-flash-domain-quality-v1`、至少一个来源、低思考/4096、原有调用/token 墙、零 retry/revision 和首个不安全失败即停；实现 `c50cf231957bc54201d0207b99110fcf4b2897b3` / Actions `33843064715` 三个任务 exact-SHA 全绿，公共 Python 2349、PostgreSQL 201、前端 270。本批不读 Key、不发请求、不注册候选、不改 GLM-5.2 应急路径或产品/前端链路；下一步仍需明确授权才可执行一次新的 V2 有界真实观察。 |

| RQ-230 | 2026-09-04 | 生效并完成一次 V2 有界真实观察；首案质量/终态不匹配而拒绝，后两案按首错停止；候选未注册、生产准入 false | 用户“继续”明确授权一次新的 V2 有界真实领域观察；旧 RQ-227 运行器身份不能用于新考卷，因此先补专用执行入口再运行；本条不重跑或改写 RQ-227/RQ-229 | 实现 SHA `5fe8606f205d49ca5dde969a5823a0eb75587c35` 的 Actions `33846260144` 三任务 exact-SHA 全绿；no-I/O preflight 通过后仅执行首案 3 次调用，领域/累计 token `10993/12084`，`network_used=true`。`hardened_form_control_41` 的 Provider/工具回合和证据检索完成（来源数 2、注入检查通过），但 `fact_check_failed`、`quality_gate_failed`、`terminal_status_mismatch` 导致 `rejected / revision_budget_exhausted`，首错 `domain_case_outcome_mismatch`，其余两案 skipped。脱敏 body-free/create-only 回执 `data/evaluation/results/provider_capabilities/zhipu_glm53_flash_hardened_domain_v2_rq230_v1.json` 为 7156 bytes、SHA-256=`d1739c5d76da21c1109808b128e8ef82df251df32ea7355836f202d850e01c18`；schema/canonical round-trip 通过。该结果是领域质量/发布合同拒绝，不是 API/适配器崩溃；不注册候选、不改默认 Runtime、GLM-5.2 回退、Portal、Account、Workbench、Auth、路由或 `production_media=0`。下一步只做失败归因与是否另立版本裁决。 |

| RQ-231 | 2026-09-04 | 生效并完成设计；建立 V3“最多一次受控修订 + body-free 安全诊断”方案，等待离线实现 | RQ-230 已确认首案为事实核验失败且 `80<85`，但 V2 固定 `max_revisions=0`；用户随后以“继续”授权验证新的版本化假设。保持 85 分及事实/引用/注入/来源硬门，不原题重考、不降低门槛、不直接切换产品默认模型 | 接受 ADR-0094：V3 显式使用 `max_revisions=1` 并由同一 Harness 完成首评、最多一次修订和复评；公开诊断只保存评测轮次、分数、结论、通过项数量、问题类别计数与严重度计数，不含正文或自由文本。调用墙按控制流推导为 9 次/案、27 次/域；Token 墙须由离线请求包络证明后冻结，不能沿用 24,000/72,000。V3 必须使用全新问题、合成数据、case/run ID、marker、Context、协议和回执身份；真实运行前还需 no-I/O 实现/资产准入、exact-SHA 公共 CI、新鲜 G53-3-L 证据及分别明确的真实调用授权。本批 provider calls=0，不改适配器、默认 Runtime、GLM-5.2 应急路径、Portal、Account、Workbench、Auth、路由或 `production_media=0`。下一步只做 V3 离线实现。 |
| RQ-232 | 2026-09-04 | 生效并完成 V3 离线实现及公共闭环；等待新鲜 G53-3-L 的明确授权，不含领域真实调用或候选准入 | 用户在 RQ-231 设计后以“继续”授权离线实现，并在 `pending-public-ci` 检查点继续公共验证；本批只验证最多一次修订、body-free 诊断、可达资源墙、全新资产准入和公共可复现性，不进入 V3 领域观察 | 初始实现 `730c32d074269fb45e5a5351b1af591ecaa35de1` 新增 V3 预算墙、可达性报告、三案例匿名资产和候选专用入口；默认 `max_revisions=0`、旧 V2 `4/12` 与 `24000/72000` 保持不变。首个公共运行 `33894351184` 暴露旧输入计划零修订隔离和 V2 回执分流两处遗漏；修复提交 `f99c142c269df765deb592c463ce6e2555bcc3fe` 明确只有 V3 可使用一次修订，并按协议 ID 解析 V2 回执。相关与相邻回归 `93 passed`，compileall、`git diff --check`、governance 通过；Actions `33895602378` 三任务 exact-SHA 全绿，公共 pytest `2379 passed, 145 skipped, 2 warnings, 127 subtests passed`，PostgreSQL `201 passed, 2 warnings`，packaging-smoke 通过。exact-SHA 预检返回 `pending_protocol_evidence`、provider calls=0。候选仍 disabled/未注册，默认 Runtime、GLM-5.2 兼容/应急路径、Portal、Account、Workbench、Auth、路由和 `production_media=0` 不变；下一步只有用户明确授权的新鲜 G53-3-L，V3 领域观察仍需之后的另一项授权。 |
| RQ-233 | 2026-09-05 | 新鲜 G53-3-L 已按授权尝试但回执未生成；延迟口径缺陷完成本地修复，等待公共 CI | 用户以“继续”授权 RQ-232 后的新鲜协议门；该授权只覆盖一次最多 3 次、SDK 零重试的真实协议运行，不包含失败后自动重跑或 V3 领域观察 | 运行绑定 `f99c142c269df765deb592c463ce6e2555bcc3fe`，在回执构造阶段被 `latency total does not match protocol` 拒绝，create-only 文件不存在；精确调用数因未落盘不可持久核验，只能确认最多 3 次。根因是预算层只计 Provider I/O 延迟、协议案例计端到端延迟，而回执使用了错误口径；现改为协议案例延迟之和并新增推进时钟回归。聚焦 `18 passed`、相关相邻 `32 passed`。候选仍未注册，默认 Runtime、GLM-5.2 回退和产品/前端边界不变；下一步只做修复提交的 exact-SHA 公共 CI，公共通过后仍须重新授权真实协议。 |

## 新条目格式

后续新增长期要求时，使用新的 `RQ-xxx` 行，并注明日期、状态以及它如何改变
执行方式。若取代旧条目，将旧条目标为“被 RQ-xxx 取代”，不要删除旧记录。
