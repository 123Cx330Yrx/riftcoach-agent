# ADR-0004：优先建设轻量 RAG

- 状态：接受
- 日期：2026-07-16

## 背景

当前知识规模较小，直接部署 Milvus、Elasticsearch、Neo4j 等基础设施会显著增加运维成本，但不一定提高现阶段报告质量。

## 决策

阶段 4 先使用本地 BM25/全文检索与 Embedding 混合方案，补齐元数据、引用、重排和检索评测。只有在知识规模、并发或关系检索需求达到明确阈值后，才评估迁移重型基础设施。

## 影响

早期部署简单、便于测试；未来迁移时必须保持统一 `KnowledgeProvider` 接口，避免业务代码绑定具体存储。
